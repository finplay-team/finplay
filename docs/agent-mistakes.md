# Agent 실수 로그

AI 에이전트가 실제로 저지르고 재현·확인된 실수만 기록한다. **추측성 예방 규칙 금지** — 실제 발생하지 않은 것은 적지 않는다. 구현 시작 전 이 파일을 읽고, 같은 실수를 재현·확인하면 행을 추가한다.

| 날짜 | 실수 | 증상 | 수정 | 재발 방지 |
|---|---|---|---|---|
| 2026-08-10 | `CryptoWatchLockConcurrencyIntegrationTest`가 MySQL·snapshot Redis 픽스처만 정리하고 실제 감시 락 키 `feedback:crypto-watch:lock:{instrumentId}`는 정리하지 않았다 | 문서 전용 PR #315 CI에서 3,661건 중 `watchSkipsEntirelyWhenTheRealLockIsAlreadyHeld`만 실패했다. 첫 `cryptoWatchLock.tryLock()` 결과가 이미 empty여서 241행 `isPresent()` 단정이 깨졌다. 공유 Redis가 Spring Context보다 오래 살고 다른 테스트 DB가 같은 instrument ID를 다시 쓰면 이전 Context의 TTL 락이 새 테스트와 충돌한다 | `@BeforeEach`에서 새 instrument ID의 감시 락 키를 먼저 삭제하고 `@AfterEach`에서도 같은 키를 정리했다. 해당 Testcontainers 클래스 전체 재실행으로 통과 확인 | **공유 Redis를 쓰는 통합 테스트는 결과 데이터 키뿐 아니라 분산락 키도 `@BeforeEach`와 `@AfterEach`에서 정리한다.** DB PK를 Redis 키에 넣었다고 전역 유일하다고 가정하지 않는다 — Context별 DB 초기화·컨테이너 생명주기 차이로 같은 PK가 다시 사용될 수 있다. |
| 2026-07-22 | Spring Boot 4에서 Flyway를 `flyway-core` 의존성으로만 추가 | 에러 없이 마이그레이션이 조용히 스킵됨 (Boot 4 모듈 분리로 자동설정 미적용) | `spring-boot-starter-flyway`로 교체 | Boot 4에서 인프라 의존성은 스타터 존재 여부부터 확인. 마이그레이션 실행은 `flyway_schema_history` 테이블로 검증 |
| 2026-07-22 | 한글 포함 경로에 프로젝트 생성 | 컴파일은 되는데 테스트만 전부 `ClassNotFoundException` (Gradle 테스트 워커가 한글 클래스패스를 못 읽음) | 영문 경로(`Desktop\tradeclass-api`)로 이전 | 프로젝트/클론 경로는 항상 영문. `-Dfile.encoding=UTF-8`로는 해결 안 됨 |
| 2026-07-23 | Windows에서 `gradlew`를 실행 비트 없이(100644) 커밋 | 로컬(Windows)은 전부 정상인데 GitHub 푸시 후 Linux CI가 전 PR에서 `./gradlew: Permission denied` (exit 126) | `git update-index --chmod=+x gradlew` 후 커밋 | Windows는 파일 권한이 없어 git 인덱스 모드로만 관리됨. CI 첫 실행 전 `git ls-files -s gradlew`가 100755인지 확인 |
| 2026-07-23 | Spring Boot 4.1에서 `@WebMvcTest` import를 구 경로(`org.springframework.boot.test.autoconfigure.web.servlet`)로 작성 시도 | 컴파일 실패 또는 슬라이스 미적용 (Boot 4.1이 테스트 자동설정 패키지를 `org.springframework.boot.webmvc.test.autoconfigure`로 이동) | 새 패키지 경로로 import 교체 | `@WebMvcTest` 등 웹 슬라이스 테스트 작성 시 Boot 4.1 새 패키지 경로 사용. `@DataJpaTest` 등 다른 슬라이스도 이동 여부 확인 후 착수 |
| 2026-07-25 | 같은 worktree에서 implementer와 tester의 Gradle 검증을 겹쳐 실행 | QueryDSL 생성물 `EOFException`, 테스트 결과 `NoSuchFileException`, main class `ClassNotFoundException`이 번갈아 발생 | 모든 에이전트 Gradle 프로세스 종료 후 `--no-daemon --max-workers=1`로 단독 재실행 | 같은 worktree의 compile/test/build는 역할 보고만 기다리지 말고 실행 프로세스 종료까지 확인한 뒤 순차 실행 |
| 2026-07-27 | 테스트에서 `ObjectMapper`를 Jackson 2 경로(`com.fasterxml.jackson.databind`)로 import해 주입 | 컴파일은 통과하는데 `@SpringBootTest`가 `NoSuchBeanDefinitionException: No qualifying bean of type 'com.fasterxml.jackson.databind.ObjectMapper'`로 전 케이스 실패 (Boot 4.1은 Jackson 3 `tools.jackson`을 쓰고 Jackson 2는 annotations만 전이 의존으로 남음) | import를 `tools.jackson.databind.ObjectMapper`·`tools.jackson.core.type.TypeReference`로 교체 | Jackson 타입은 항상 `tools.jackson.*`을 쓴다. `com.fasterxml`는 클래스패스에 있어 컴파일이 통과하므로 컴파일 성공을 근거로 삼지 말 것 |
| 2026-07-26 | 셸 호출 제한으로 `build`가 종료된 직후 Gradle 자식 프로세스 종료를 확인하지 않고 같은 명령을 재실행 | 단일-use Gradle 빌드 3개가 같은 `build/test-results`를 동시에 갱신해 테스트 assertion 실패 없이 `in-progress-results-generic.bin` `NoSuchFileException` 발생 | Gradle daemon 로그로 세 빌드의 실행 시간 중첩을 확인하고, 모든 관련 프로세스 종료 후 단일 빌드로 재실행 | 빌드 호출이 timeout이면 실패나 종료로 간주하지 않는다. Java/Gradle 자식 프로세스와 daemon 로그를 확인해 기존 빌드가 끝난 뒤에만 재실행 |
| 2026-07-27 | 이 하네스 환경(bash/PowerShell 모두)에는 `JAVA_HOME`이 비어 있고 `docker` 명령이 PATH에 없음 | `gradlew`가 JDK를 못 찾아 즉시 실패, Testcontainers 기반 테스트는 `DockerClientProviderStrategy`에서 `IllegalStateException`으로 전부 실패 (구현 결함 아님) | `JAVA_HOME=/c/Users/pmsal/.jdks/ms-17.0.20`을 명시적으로 지정해 Gradle 실행. Testcontainers 테스트/`./gradlew build`는 이 환경에서 실행 불가 — 결과 확인은 Docker 있는 환경(사용자 로컬, CI)에 위임 | Gradle 명령 전 `JAVA_HOME`을 항상 명시적으로 지정. Testcontainers 통합 테스트 실패 시 스택트레이스에 `DockerClientProviderStrategy`가 있으면 코드 문제로 오판하지 말고 Docker 가용성부터 확인 |
| 2026-07-27 | 문서에 남은 오래된 `JAVA_HOME` 경로 `C:\Users\PMS\.jdks\ms-17.0.20`을 사용 | 유효하지 않은 디렉터리로 인해 Gradle 실행 전에 wrapper가 경로를 찾지 못하고 실패 | 설치된 JDK를 확인해 실제 경로 `C:\Users\PMS\.jdks\corretto-17.0.18`을 사용 | `JAVA_HOME` 설정 전 설치된 JDK 경로와 Java 설정을 확인하고, 오래된 장비별 경로를 하드코딩하지 않는다 |
| 2026-07-28 | `@Component`(`OAuthStateGenerator`)에 `@Value` 생성자와 테스트용 생성자 2개를 두면서 `@Autowired`를 붙이지 않음 | `compileJava`·단위 테스트는 전부 통과하는데 `@SpringBootTest` 전체가 `Failed to instantiate ...: No default constructor found`로 실패 (Spring이 생성자 후보를 못 고르고 기본 생성자로 폴백). 기존 `OAuthNicknameGenerator`는 no-arg 생성자가 있어 같은 형태여도 문제가 없었다 | 주입 대상 생성자에 `@Autowired` 명시 | 생성자를 2개 이상 둔 빈은 하나에 `@Autowired`를 붙인다. 컴파일·단위 테스트 통과를 컨텍스트 기동 근거로 삼지 말고, 빈 생성자 시그니처를 바꿨으면 컨텍스트를 띄우는 테스트를 한 번은 돌린다 |
| 2026-07-29 | `List<T>` 필드를 가진 record DTO(`MarketSnapshotEvent`)에서 생성자·접근자로 전달받은 리스트를 그대로 저장·반환 | `compileJava`·`test`는 통과하지만 `./gradlew build`의 `spotbugsMain`이 `EI_EXPOSE_REP`/`EI_EXPOSE_REP2`(내부 표현 노출)로 실패 — record라도 컬렉션 타입 필드는 자동으로 불변이 되지 않는다 | compact 생성자에서 `prices = List.copyOf(prices);`로 방어적 복사 | `List`·`Map`·`Set` 등 컬렉션 필드를 가진 record를 새로 만들 때는 compact 생성자에서 `List.copyOf` 등으로 방어적 복사를 기본으로 넣는다. `compileJava`·`test` 통과를 spotbugs 통과 근거로 삼지 말 것 — `spotbugsMain`은 `./gradlew build`(또는 `check`)에서만 돈다 |
| 2026-07-29 | `SseEmitter`를 반환하는 컨트롤러(`StockPriceSseController`)의 `@WebMvcTest`/`@SpringBootTest`에서 `MvcResult.getAsyncResult()`(인자 없는 버전) 또는 `request().asyncResult(...)` 매처를 호출 | 테스트가 40분 넘게 응답 없이 멈춤(`CountDownLatch.await`에서 `TIMED_WAITING`). 원인: `getAsyncResult()`는 내부적으로 `MockAsyncContext.getTimeout()`을 읽어 `-1`이면 `Long.MAX_VALUE ms`로 대기한다(바이트코드로 확인). 이 앱은 `spring.mvc.async.request-timeout: -1`을 SSE 무제한 연결 유지를 위해 의도적으로 설정했고(이슈 #18), 그 값이 `MockAsyncContext`에도 그대로 전달된다. 게다가 `getAsyncResult()`가 기다리는 "asyncDispatch"는 emitter가 `complete()`/`completeWithError()`로 완료돼야 발생하는데, 무제한 스트림용 `SseEmitter`는 테스트에서 의도적으로 완료시키지 않는 한 그 디스패치 자체가 오지 않는다 | `request().asyncStarted()`(플래그만 동기 확인, 안전)만 쓰고 `getAsyncResult()`/`asyncResult(...)`는 호출하지 않음. emitter 인스턴스가 필요하면(예: 컨트롤러가 register()의 반환값을 그대로 돌려주는지 확인) `@WebMvcTest`에서는 Mockito `ArgumentCaptor`로 다음 협력 객체 호출 인자에서, `@SpringBootTest` 통합 테스트에서는 실제 `SseEmitterRegistry` 싱글턴 빈에서 직접 조회 | `spring.mvc.async.request-timeout: -1`(또는 매우 긴 타임아웃)로 설정된 앱에서 `SseEmitter`/무제한 스트리밍 컨트롤러를 MockMvc로 테스트할 때는 `getAsyncResult()`·`request().asyncResult(...)`를 절대 쓰지 않는다(둘 다 내부적으로 같은 무한대기 경로). `request().asyncStarted()`까지만 검증하고, 실제 emitter 참조가 필요하면 mock 협력 객체 캡처나 실제 레지스트리 조회로 대체한다. 혹시 몰라 테스트 클래스에 `@Timeout`도 함께 걸어 회귀 시 무한 대기 대신 실패로 드러나게 한다 |
| 2026-07-29 | `KisRealtimePriceProvider`에서 `ObjectMapper`·`RestClient` 등을 받는 생성자를 (Lombok 없이) 손으로 작성 — `RestClient.Builder`로부터 타임아웃 적용 `RestClient`를 만드는 등 파생 로직이 있어 `@RequiredArgsConstructor`를 못 쓴다고 판단했음 | `./gradlew build`의 `spotbugsMain`이 `EI_EXPOSE_REP2`(생성자가 가변 객체 `objectMapper`를 필드에 그대로 저장)로 실패. 같은 패키지의 `RestAuthenticationEntryPoint`·`RestAccessDeniedHandler`·`SecurityConfig`도 동일하게 `ObjectMapper` 필드를 갖지만 경고가 없어 원인이 손으로 쓴 생성자인지 확실히 하기 위해 임시 프로브 클래스 2개(동일한 `ObjectMapper` 필드, 하나는 손으로 쓴 생성자, 하나는 `@RequiredArgsConstructor`)로 `spotbugsMain`을 재현 실행해 확인함 — 손으로 쓴 생성자의 필드 저장만 `EI_EXPOSE_REP2`로 잡히고, Lombok이 생성한 생성자의 동일한 필드 저장은 잡히지 않았다(단, 손으로 쓴 게터의 필드 반환은 두 프로브 모두 `EI_EXPOSE_REP`로 잡힘 — Lombok 예외는 생성자에만 해당) | 파생 로직이 필요한 필드(`URI` 변환, 타임아웃 적용 `RestClient` 빌드)를 생성자 밖으로 옮겨 모든 필드가 파라미터 직접 대입만 되도록 리팩터링(`URI`는 사용 시점에 `URI.create()`, `RestClient`는 `StockFeedConfig`가 완성해서 주입)한 뒤 클래스를 `@RequiredArgsConstructor`로 전환. exclude.xml에 예외를 추가하는 대신 구조를 바꿔 해결 | `EI_EXPOSE_REP`/`EI_EXPOSE_REP2`가 나는 클래스는 먼저 "필드가 전부 파라미터 직접 대입뿐인가"를 확인하고, 그렇다면 손으로 쓴 생성자를 Lombok `@RequiredArgsConstructor`로 바꾸는 것만으로 SpotBugs가 더 이상 잡지 않는다(단, 손으로 쓴 게터/세터가 가변 필드를 그대로 반환·저장하면 Lombok 여부와 무관하게 여전히 잡힌다 — 이 예외는 생성자의 필드 저장에만 적용됨). 파생 로직(URI 변환, 빌더로 완성하기 등)이 있어 `@RequiredArgsConstructor`가 안 될 것 같으면, 그 로직을 호출부(예: `@Configuration` 클래스의 `@Bean` 메서드)로 옮겨 완성된 값을 그대로 주입받는 형태로 바꿀 수 있는지부터 검토 |
| 2026-07-30 | 새 `@SpringBootTest`(비-`@Transactional`) 통합 테스트(`AccountSummaryIntegrationTest`)에서 `instrumentRepository.saveAndFlush`·`stockReplaySessionRepository.saveAndFlush`로 실제 커밋되는 테스트용 종목·재생세션을 생성 | 이 테스트 클래스가 패키지명(`com.finplay.api.account...`)상 알파벳순으로 `market` 패키지보다 먼저 실행되어, 공유 MySQL 컨테이너(ADR-0003)에 커밋한 종목이 이후 실행되는 `InstrumentRepositoryTest`의 "정확히 28건(주식16·코인12)" 단정과 `StockReplaySessionRepositoryTest`의 유니크 제약 검증을 깨뜨림(개별 클래스 단독 실행 시엔 통과, `./gradlew build` 전체 실행 시에만 재현) | 테스트 클래스에 `@Transactional`을 추가해 각 테스트 메서드 종료 시 자동 롤백되게 함(Redis는 JPA 트랜잭션에 안 걸리지만 카운트 단정과 무관해 문제 없음) | 실제 커밋(비-`@Transactional`) 방식의 `@SpringBootTest`를 새로 추가할 때, `Instrument`·`StockReplaySession`처럼 다른 테스트가 전체 개수/유니크 제약을 단정하는 테이블에 데이터를 넣는다면 `@Transactional`로 롤백시키거나 명시적으로 정리한다. 새 테스트 클래스만 단독 실행해 통과를 확인하는 것으로 충분하다고 판단하지 말고 `./gradlew build` 전체 실행까지 반드시 확인한다 |
| 2026-07-30 | Bash 도구(Git Bash)에서 `.\gradlew.bat build` 실행 | 백슬래시가 이스케이프로 먹혀 `.gradlew.bat: command not found`가 남. 여기에 `\| tail -60`을 붙이면 파이프 종료 코드 때문에 exit 0으로 보고돼 빌드가 통과한 것처럼 보임 — 실제로 빌드가 안 돌았는데 통과로 오인해 이전 실행이 남긴 테스트 결과(build/test-results)를 근거로 쓸 뻔했다 | Windows에서 gradle은 PowerShell 도구로 실행하고, 절대 경로 + 호출 연산자를 쓴다: `& "C:\Users\user\Desktop\tradeclass-api\gradlew.bat" build --console=plain` | 빌드 결과는 exit code가 아니라 출력의 `BUILD SUCCESSFUL` 문자열과 `build/test-results/test/*.xml`의 타임스탬프로 확인한다 |
| 2026-07-30 | PowerShell 도구를 run_in_background로 쓸 때 상대 경로 `.\gradlew.bat` 사용 | `CommandNotFoundException`으로 실패 (cwd는 정상인데도) | 절대 경로 + `&`로 호출 | run_in_background PowerShell 호출은 상대 경로 대신 절대 경로 + `&`를 쓴다 |
| 2026-07-30 | 여러 서브에이전트가 각자 `--no-daemon`으로 `./gradlew`를 반복 실행하는 동안 종료되지 않고 남은 `GradleWorkerMain` 프로세스가 10개 넘게 누적된 상태에서 `./gradlew build`(1106+건 전체 테스트) 실행 | `HoldingLotRepositoryTest`·`HoldingRepositoryTest`·`TradeAllocationRepositoryTest`(이번 세션에서 건드리지 않은 무관한 기존 파일) 3개 클래스, 총 9건이 `SQLTransientConnectionException: Connection is not available, request timed out after 30005ms`로 실패. 해당 3개 클래스만 단독 실행하면 100% 통과, 동일 전체 빌드를 재실행해도 매번 같은 3개 클래스가 재현됨(코드 결함 아님) | `ps aux \| grep GradleWorkerMain`으로 idle 상태(CPU 0.0%, 오래된 시작 시각)인 프로세스를 모두 `kill`한 뒤 `./gradlew build --no-daemon --max-workers=1 --rerun-tasks` 재실행 → BUILD SUCCESSFUL(회귀 없음) | `./gradlew build` 전체 실행이 항상 같은 소수 클래스에서만 커넥션 타임아웃으로 실패하는데 해당 클래스를 단독 실행하면 통과한다면, 코드가 아니라 누적된 idle `GradleWorkerMain` 프로세스가 호스트 리소스(메모리·CPU)를 잠식해 공유 MySQL Testcontainer(ADR-0003)의 커넥션 풀 확보가 지연되는 것일 수 있다 — 실패한 파일을 고치기 전에 `ps aux \| grep GradleWorkerMain`으로 idle 프로세스 누적 여부부터 확인하고 정리한 뒤 재실행한다 |
| 2026-07-30 | 이슈 #104 작업 중 같은 호스트에서 여러 git worktree(다른 세션들)가 동시에 각자 `./gradlew`를 실행 중인 상태에서 `./gradlew build` 3회 연속 실행(`ps`로 idle `GradleWorkerMain`을 매번 정리했음에도) | 매번 정확히 같은 3개 클래스(`HoldingLotRepositoryTest`·`HoldingRepositoryTest`·`TradeAllocationRepositoryTest`, 총 9건)가 `Connection refused`/`CannotCreateTransactionException`으로 실패. `ps aux \| grep GradleDaemon`으로 확인해보니 다른 worktree들이 소유한 활성 `GradleDaemon` 프로세스 3개(0.0%가 아니라 실제로 동작 중)가 함께 떠 있었고 `top` 기준 물리 메모리 여유가 1.2GB 수준으로 낮았다 — 즉 이번엔 "내 세션이 남긴 idle worker"가 아니라 "다른 세션들의 활성 daemon과의 호스트 리소스 경합"이 원인이었다 | idle worker 정리만으로는 해결되지 않아 그냥 `./gradlew build --no-daemon --max-workers=1`을 추가로 재시도(4번째 시도)했더니 별다른 조치 없이 BUILD SUCCESSFUL. 실패한 3개 클래스를 단독 실행하면 항상 즉시 통과하는 것도 재확인 | 이 3개 클래스가 실패하는 패턴은 코드 문제가 아니라 호스트 리소스 경합의 신호로 확정 취급한다. `ps aux \| grep GradleWorkerMain`으로 idle 프로세스를 정리했는데도 같은 3개 클래스가 반복 재현되면, `ps aux \| grep GradleDaemon`으로 다른 worktree의 활성 daemon 존재 여부와 `top`의 여유 메모리도 함께 확인하고, 그래도 뚜렷한 조치가 안 보이면(다른 세션의 정당한 작업을 죽일 수 없으므로) 코드를 의심하지 말고 전체 빌드를 몇 차례 더 재시도한다 — 이 세션에서는 3번째 실패 후 4번째 시도에서 특별한 조치 없이 통과했다 |
| 2026-07-30 | `@Value`가 붙은 필드를 가진 빈(`LocalForcedOpenStockPriceProvider`)에 Lombok `@RequiredArgsConstructor`를 사용 — 필드에 `@Value("${finplay.dev.stock.force-market-open:false}")`를 달고 생성자는 Lombok에 위임 | `compileJava`가 통과한다. 그러나 `javap -v`로 확인하면 Lombok이 생성한 생성자에 `RuntimeVisibleParameterAnnotations`가 **없다** — Lombok은 Spring의 `@Value`를 필드에서 생성자 파라미터로 복사하지 않는다. 그래서 Spring은 프로퍼티를 주입하는 대신 `boolean` 타입 빈을 찾아 헤매다 컨텍스트 기동에 실패한다 | 생성자를 손으로 작성하고 `@Value`를 **파라미터**에 붙였다. 손으로 쓴 생성자는 2026-07-29 행의 `EI_EXPOSE_REP2` 위험이 있어 `spotbugsMain`을 단독 실행해 확인했다 — `Clock`과 리포지토리 프록시는 `ObjectMapper`와 달리 가변 객체로 잡히지 않아 통과했다 | 빈에 `@Value` 주입 필드가 하나라도 있으면 `@RequiredArgsConstructor`를 쓰지 않고 생성자를 손으로 쓰고 `@Value`를 파라미터에 붙인다. `compileJava` 통과를 주입 성공 근거로 삼지 말 것 — 이 실수는 컨텍스트를 띄우는 테스트에서만 드러난다. 반대로 손으로 쓴 생성자로 바꾼 뒤에는 `spotbugsMain`(`./gradlew build`·`check`에서만 돈다)을 반드시 확인한다 |
| 2026-07-30 | `bootRun`을 하네스 백그라운드 태스크로 띄운 뒤 태스크만 종료(TaskStop)하고 새 `bootRun`을 실행 | 태스크는 Gradle 래퍼만 죽이고 `bootRun`이 띄운 Java 애플리케이션 프로세스는 살아남아 8080을 계속 점유한다. 새 `bootRun`은 포트 충돌로 즉시 실패하는데, `curl /actuator/health`는 **살아있는 구 프로세스** 때문에 200을 반환하므로 "재기동 성공"으로 오인한다. 그 결과 새로 추가한 `@Profile("local")` 컨트롤러가 없는 구 프로세스에 요청해 404를 받고 원인을 컨트롤러 등록 문제로 오판했다(실제로는 코드가 정상) | `netstat -ano \| grep :8080`으로 LISTENING PID를 확인해 로그의 PID와 대조 → 구 프로세스임을 확인하고 `taskkill //PID <pid> //F`로 종료한 뒤 재기동 | 애플리케이션 재기동 후에는 health 200을 근거로 삼지 않는다. 로그의 새 `Started ...Application` 줄과 `netstat`의 LISTENING PID가 **새 프로세스인지** 대조한다. 새로 추가한 빈이 404/미등록으로 보이면 코드를 의심하기 전에 어느 프로세스가 응답하는지부터 확인한다 |
| 2026-07-30 | Bash 도구에서 `git commit -m @'...'@` (PowerShell here-string 문법)로 여러 줄 커밋 메시지 작성 | 커밋은 성공하고 exit 0이라 정상으로 보인다. 그러나 Bash는 `@'`를 리터럴로 처리해 제목 줄이 `@`가 되고 본문 첫 줄이 실제 제목으로 밀리며, 끝에도 `@` 줄이 남는다. `git log --oneline`에는 `@ fix: ...`처럼 보여 눈치채기 어렵다 | `git log -1 --format='%B' \| cat -A`로 실제 바이트를 확인해 앞뒤 `@$` 줄을 발견하고, `git commit --amend --file=- <<'EOF'` heredoc으로 재작성 | 이 저장소는 Bash(Git Bash)와 PowerShell 도구를 모두 쓴다. **여러 줄 문자열은 도구에 맞춰 쓴다** — Bash는 heredoc(`<<'EOF'`), PowerShell은 here-string(`@'...'@`). 커밋 직후 `git log -1 --format='%s'`로 제목이 타입 접두사(`fix:` 등)로 시작하는지 확인한다 |
| 2026-07-31 | 테스트 결과 XML을 읽으려고 Bash에서 `cd build/test-results/test`를 한 뒤, 같은 세션에서 PowerShell 도구로 `gradlew.bat build` 실행 | 셸 작업 디렉터리가 호출 간 유지돼 Gradle이 그 디렉터리를 프로젝트 루트로 해석하고 `Project directory '...\build\test-results\test' is not part of the build defined by settings file ...`로 **6초 만에** 실패. exit code 1에 `BUILD FAILED`까지 찍혀 직전 실행에서 보던 테스트 실패가 재발한 것처럼 보였다 — 출력을 끝까지 읽지 않았으면 코드를 의심할 뻔했다 | `gradlew.bat -p "<프로젝트 루트 절대경로>" build`로 프로젝트 루트를 명시해 재실행 | Gradle은 항상 `-p <절대경로>`로 프로젝트 루트를 고정한다. 작업 디렉터리는 도구 호출 간 유지되므로 파일을 읽으려고 `cd`한 것이 나중 빌드까지 오염시킨다. 빌드가 **비정상적으로 빨리**(1분 미만) 실패하면 테스트 실패로 넘겨짚지 말고 `What went wrong` 절부터 읽는다 |
| 2026-07-31 | 이슈 본문(#108)이 근거로 든 기술적 전제("HTTP 데모는 `OAUTH_STATE_COOKIE_SECURE=false`로 내려주면 된다")를 코드로 확인하지 않고 배포 문서(`deploy/README.md`)에 그대로 지시로 옮겨 적음 | 작성한 배포 스택을 실제로 띄우자 앱 컨테이너만 `IllegalStateException: prod 또는 oauth-real 프로필에서는 OAuth state 쿠키의 Secure 속성을 끌 수 없습니다`로 기동 실패. nginx·compose·Dockerfile은 전부 정상이었고 원인은 내가 쓴 문서가 지시한 환경변수 값이었다 | `OAuthStateCookieFactory.java:39`의 fail-fast 가드를 확인하고, 문서를 "기본값 `true` 유지 + HTTPS 붙기 전까지 OAuth 로그인 불가"로 정정. spec에도 같은 제약을 기록 | 이슈·PRD 본문에 적힌 기술적 전제는 설정 파일이나 문서로 옮기기 전에 해당 코드에서 1회 확인한다. 특히 "이렇게 내려주면 된다" 류의 우회 방법은 그 값을 막는 fail-fast 가드가 있는지 먼저 grep한다. 이슈 작성자가 팀원이어도 전제가 코드보다 앞서지 않는다 |
| 2026-07-31 | 다른 worktree가 compose 컨테이너(`tradeclass-api-mysql-1`·`-redis-1`)를 띄워 둔 상태에서 이 worktree에서 `bootRun` 실행 | `spring-boot-docker-compose`가 자기 프로젝트 이름(`mvp-*`)으로 컨테이너를 새로 만들려다 `Bind for 0.0.0.0:16379 failed: port is already allocated`로 기동 실패. compose.yaml의 포트가 고정 매핑이라 worktree가 둘 이상이면 항상 충돌한다 | 다른 worktree의 컨테이너를 죽이지 않고, `SPRING_DOCKER_COMPOSE_ENABLED=false` + `SPRING_DATASOURCE_URL=jdbc:mysql://localhost:13307/finplay`·`SPRING_DATA_REDIS_HOST/PORT`로 이미 떠 있는 컨테이너에 붙여 기동. 실패한 시도가 만든 `mvp-*` 컨테이너 3개는 직접 정리했다 | 여러 worktree를 동시에 쓰는 환경에서 `bootRun` 검증을 할 때는 compose 자동 기동을 끄고 기존 컨테이너에 붙인다. 다른 세션이 쓰는 컨테이너를 내리지 않는다. 실패한 compose 시도가 남긴 컨테이너는 본인이 만든 것만 정리한다 |
| 2026-08-01 | 비-`@Transactional` `@SpringBootTest` 통합 테스트에서, 저장 직후 메모리에 들고 있던 엔티티의 `LocalDateTime` 값과 DB에서 다시 읽은 값을 `isEqualTo`로 비교 | `expected: ...859584900 but was: ...859585`처럼 하위 자릿수만 어긋나 실패한다. Windows `LocalDateTime.now()`는 100ns 해상도인데 MySQL `DATETIME(6)`은 마이크로초로 반올림해 저장하기 때문이다 | 기준값을 DB에서 다시 읽어 비교하거나 `isCloseTo(..., within(1, ChronoUnit.MILLIS))`로 비교 | Issue #114의 `PasswordChangeIntegrationTest`와 Issue #115의 `PasswordResetIntegrationTest`에서 각각 독립적으로 발생했다. 실제 MySQL에 저장되는 `LocalDateTime`은 메모리 값과 정확히 같지 않다 — 저장 전 값과 조회 값을 `isEqualTo`로 비교하지 않는다 |
| 2026-08-03 | 이슈 #119 반복 검증 스크립트에서 PowerShell `Start-Process -FilePath "cmd.exe"`에 `/c`와 중첩 인용(`"`"$gradlew`" -p `"$proj`" ..."`)으로 Gradle 호출 | 6회 반복이 전부 **0분 만에** exit 1로 끝났다. stdout은 비어 있고 stderr에 `The filename, directory name, or volume label syntax is incorrect.` 한 줄뿐 — Gradle이 실행조차 안 됐는데 요약만 보면 "6회 전부 실패"라 검증 대상 수정이 실패한 것으로 오독하기 쉽다 | `Start-Process -FilePath <gradlew.bat 절대경로> -ArgumentList "-p", <프로젝트 절대경로>, "build", "--console=plain"`으로 .bat을 직접 호출 (인용 중첩 없음). 로그 파일은 `-RedirectStandardOutput`/`-RedirectStandardError`로 받는다 | PowerShell에서 Gradle을 비대화식·반복 실행할 때 `cmd.exe /c` + 중첩 인용을 쓰지 않는다. 2026-07-31 행("비정상적으로 빨리 실패하면 `What went wrong`부터 읽는다")과 같은 증상이므로 **반복 실행 스크립트에는 "정상 소요 시간보다 훨씬 빨리 끝나면 루프 중단" 가드를 넣어** 잘못된 실행으로 회차를 태우지 않는다 |
| 2026-08-03 | Write 도구로 PowerShell 스크립트를 만들면서 **첫 줄에 한국어 주석**을 두고 그 다음 줄에 `param([int]$Pairs = 3)`을 배치 | 스크립트가 **오류 없이 0초 만에 정상 종료**했다. `Get-Content`로 확인하니 한국어 주석과 `param(...)`이 **한 줄로 붙어 있어** param 블록이 주석에 삼켜졌고, `$Pairs`가 `$null`이 되어 `for ($i=1; $i -le $Pairs; ...)`가 0회 돌았다. 시작·종료 로그는 정상 출력돼 "측정이 끝났다"로 오독하기 쉽다 (Write는 LF로 저장하는데 PowerShell 5.1이 BOM 없는 파일을 ANSI로 읽으며 특정 다국어 바이트열에서 줄바꿈이 소실됨) | `param(...)`을 **파일 첫 줄**로 옮기고 스크립트 주석을 ASCII로 작성. `Get-Content -TotalCount 2`로 param이 제 줄에 있는지 확인 | Write 도구로 만드는 PowerShell 스크립트는 **`param()`을 반드시 첫 줄에 두고 주석은 ASCII로 쓴다**. 한국어 주석은 Java·Markdown에만 쓴다(프로젝트 규칙은 소스 파일 대상이며 임시 스크립트는 해당 없음). 반복 실행 스크립트가 **소요 시간 0으로 성공하면 성공이 아니라 미실행을 의심**한다 — 2026-08-03 `cmd.exe` 인용 행과 같은 부류다 |
| 2026-08-01 | Write 도구로 새 Java 테스트 파일을 생성 — 이 저장소의 기존 파일은 전부 CRLF인데 Write는 LF로 저장한다 | `spotlessJavaCheck`가 내용은 정상인 그 파일을 줄바꿈 때문에 파일 전체 포맷 위반으로 잡는다 | `spotlessApply`로 정규화. 적용 전 `git status`로 더티 파일이 본인이 만든 파일뿐인지 확인했다 | Write 도구로 새 Java 파일을 만들었으면 `spotlessApply`로 줄바꿈을 정규화한다. `spotlessApply`는 워킹 트리 전체를 건드리므로 실행 전 `git status`로 더티 파일이 본인이 만든 것뿐인지 확인한다. **2026-08-07 추가 — Git Bash의 `sed -i`도 같은 일을 한다**(기존 CRLF 파일을 LF로 다시 쓴다). `spotlessJavaCheck`는 `build`·`check`에서만 돌아 7분짜리 전체 빌드의 **마지막에** 터지므로, 파일을 `sed -i`로 고쳤으면 빌드를 걸기 전에 `spotlessApply`를 먼저 돌린다 |
| 2026-08-03 | 메인 세션이 사용자에게 준 예시 셸 명령에 Windows 역슬래시 절대경로(`C:\Users\user\Desktop\tradeclass-api\.env`)를 그대로 적음 — 실행 셸이 Git Bash라는 것을 명시하지 않았다 | Bash가 역슬래시를 이스케이프로 먹어 경로 구분자가 전부 사라지고, 리다이렉트 대상이 **`C：UsersuserDesktoptradeclass-api.env`라는 단일 파일**이 됐다(콜론은 Windows에서 파일명에 못 쓰므로 U+F03A로 치환됨). **명령은 오류 없이 성공**했고, 그 안에 실제 `OPENAI_API_KEY` 원문이 들어갔다. `.gitignore`의 `.env` 패턴은 **정확히 `.env`인 파일만** 매치하므로 이 파일은 걸리지 않아 `git status`에 untracked로 뜬다 — **`git add .` 한 번이면 시크릿이 커밋된다.** 리뷰가 아니라 planner가 작업 끝에 `git status --short`로 워킹 트리를 확인하다 발견했다 | 파일을 열지 않고(키 원문 우려) 메인 세션에 보고 → 메인 세션이 내용을 확인하고 삭제 | 경로를 담은 셸 명령을 만들거나 사용자에게 건넬 때는 **어느 도구로 실행할지 정하고 그 문법으로 쓴다** — Bash 도구면 슬래시(`/c/Users/...` 또는 `C:/Users/...`), PowerShell 도구면 역슬래시. 2026-07-30 `.\gradlew.bat` 행과 같은 부류다. 시크릿을 다루는 명령은 성공 종료를 근거로 삼지 말고 **의도한 경로에 파일이 생겼는지 확인**한다. 커밋 전 `git status --short`로 untracked 목록을 훑는다 — `.gitignore`는 이름이 뭉개진 시크릿 파일을 막지 못한다 |
| 2026-08-03 | Spring AI 2.0.0 OpenAI 스타터(`spring-ai-starter-model-openai`)를 `build.gradle`에 추가하면서, 쓰는 것이 chat 하나뿐이므로 API 키가 없어도 컨텍스트는 뜬다고 전제 (ADR-0011의 "키가 없어도 기동한다"를 의존성 추가만으로 만족한다고 봤다) | `compileJava`는 통과하는데 컨텍스트를 띄우는 순간 전체가 `IllegalStateException: At least one of apiKey, credential, azureServiceVersion... must be set`(openai-java-core 4.39.1)로 죽는다. 스타터가 chat 외에 embedding·image·moderation·audio 모델 빈까지 자동 등록하고, 그중 `openAiSdkAudioSpeechModel`이 **지연 초기화가 아니라 기동 시점에** 키를 요구한다. Boot 4.1.0 × Spring AI 2.0.0 조합 자체는 정상이었다(스타터 POM이 `spring-boot-starter:4.1.0`을 직접 선언) — 버전 충돌로 오판하기 쉬운 증상이다 | `application.yml`에 `spring.ai.model.{embedding,image,moderation,audio.speech,audio.transcription}: none`으로 안 쓰는 모델 유형을 끄고, `spring.ai.openai.api-key: ${OPENAI_API_KEY:not-configured}`로 자리표시자 기본값을 뒀다(SDK가 빈 문자열은 거부한다). 커밋 `d311b87` | LLM 스타터는 **모델 유형 하나만 쓰더라도 나머지 자동설정 빈이 함께 뜬다.** 추가 직후 키 없는 환경에서 컨텍스트를 한 번 띄워 확인하고, 안 쓰는 유형은 `spring.ai.model.*: none`으로 끈다. 기동 실패 메시지에 프레임워크 버전이 안 나오면 버전 조합부터 의심하지 말고 **어느 빈이 실패했는지**를 먼저 읽는다 |
| 2026-08-03 | PR #132(설계 문서 전용) 리뷰를 서브에이전트로 돌리면서, 프롬프트에 중점 사항 5가지(스포일러 게이트·구현 가능성·문서 정합성·1차 권장 5건 반영 여부·ADR 적절성)를 지정하고 "**파일을 직접 읽어 확인한 것만** 적어 달라"를 덧붙임 | 리뷰어가 조준된 영역에서는 차단 4건을 정확히 찾았으나(가장 큰 스포일러 구멍 포함), 조준하지 않은 영역인 `checklist.md`의 **허위 완료 기록**("이슈 #131 체크박스·라벨 정리"를 `[x]`로 적었으나 실제로는 미실행)을 놓쳤다. 이후 사람 쪽 리뷰어가 `gh api repos/.../issues/131/timeline`으로 확인해 차단으로 지적했다. "파일만 읽어라"가 **GitHub·Notion 등 문서 밖 상태 검증을 배제**하는 효과를 냈다. 더해서, 리뷰어 지적을 고치는 과정에서 새 결함 2건(수집 크론이 브리핑 근거 구간을 못 덮음, Part C 요약 이원화에 코인 미정의)이 생겼는데 **수정본은 아무 검증도 거치지 않고 푸시**돼 같은 3차 리뷰에서 차단으로 나왔다. 결과적으로 리뷰 라운드가 3회로 늘어 좁은 리뷰 1회보다 비용이 더 들었다 | 3차 리뷰 지적을 전부 반영하고, 이슈 #131을 **실제로** 갱신(완료 조건 6개 체크·`결정필요` 라벨 제거)한 뒤 기록을 정정했다 | 리뷰 서브에이전트에 중점 사항을 주더라도 **"이 목록에 없는 것도 본다"를 함께 명시**한다 — 저자가 프롬프트를 쓰면 판단은 독립적이어도 **범위는 저자의 사각지대를 그대로 물려받는다**. "파일만 읽어라"처럼 검증 수단을 좁히지 않는다. 완료 기록·이슈 상태·외부 문서 반영 주장은 `gh api`로 실제 확인 대상에 넣는다. 그리고 **리뷰 지적을 고친 뒤 최소 1회 재검증한다** — 이 PR에서 "직전 지적을 고치다 새 결함" 패턴이 3라운드 연속 재현됐다(1차 권장→2차 차단, 2차 차단→3차 차단 2건) |
| 2026-08-03 | `docs/agent-mistakes.md` 2026-07-27 행("이 하네스에는 `docker`가 PATH에 없다")을 현재 환경 사실로 인용해, 서브에이전트 보고에 "Testcontainers 검증 불가"라고 적음 | 실제로는 `docker info`가 서버 29.4.2로 응답하고 `@SpringBootTest`가 MySQL 컨테이너로 정상 기동한다. 검증할 수 있는 것을 안 했다고 보고해 오케스트레이터가 잘못된 전제로 다음 작업을 배정할 뻔했다 | 메인 세션과 tester가 각각 `docker info`·`@SpringBootTest` 실행으로 반증 | 이 로그의 환경 관련 행(JAVA_HOME 경로·docker 유무·JDK 버전)은 **그 시점 장비 상태**다. 현재 환경 사실로 인용하기 전에 해당 명령을 1회 실행해 확인한다 — 2026-07-31 "이슈 본문의 기술적 전제를 코드로 확인하지 않고 문서에 옮김" 행과 같은 부류다 |
| 2026-08-03 | 이슈 #155(집계 캔들 조회 하한 좁히기) 구현에서, 단일 필드(`tradingDate`)만 뽑는 리포지토리 조회를 `findDistinctTradingDateByInstrumentIdAndTradingDateBetween...(...)` 같은 파생 쿼리 메서드 이름만으로 선언(“Distinct+속성명을 subject에 넣으면 그 속성만 프로젝션된다”고 전제, MongoDB류 스토어 문서 기억과 혼동) | `@DataJpaTest` 4건·통합 테스트 1건이 `ConversionFailedException: ... Caused by: ConverterNotFoundException: No converter found capable of converting from type [StockCandle] to type [LocalDate]`로 실패. Spring Data JPA는 파생 쿼리 이름의 subject 속성으로 단일 필드를 프로젝션하지 않고 항상 엔티티 전체(`List<StockCandle>`)를 반환하려다, 선언한 반환 타입(`List<LocalDate>`)으로의 변환에 실패한 것 — 컴파일은 통과하므로 테스트를 돌리기 전까지 드러나지 않는다 | 파생 쿼리 이름을 버리고 `@Query("SELECT DISTINCT c.tradingDate FROM StockCandle c WHERE ...")` + `@Param`으로 직접 JPQL을 작성(`StockCandleRepository.findDistinctTradingDateByInstrumentIdAndTradingDateBetweenOrderByTradingDateDesc`) | Spring Data JPA에서 **엔티티 전체가 아닌 단일 필드만 반환**하려면 파생 쿼리 메서드 이름(`findDistinct<필드>By...`)에 의존하지 말고 항상 `@Query`(DTO/인터페이스 프로젝션도 가능)를 쓴다. 이런 메서드는 컴파일이 통과해도 실제 슬라이스 테스트를 돌려보기 전엔 반환 타입 불일치를 알 수 없다 |
| 2026-08-03 | Docker가 없는 환경에서 전체 `build`가 실패한 뒤 `gradlew build -x test`를 대체 게이트로 실행 | 테스트를 제외했지만 `jacocoTestCoverageVerification`이 `compileJava`·`processResources` output에 대한 implicit dependency validation 오류로 실패해 전체 빌드 대체 근거가 되지 못했다 | `build -x test` 결과를 전체 게이트 통과로 사용하지 않고, 대상 비컨테이너 테스트와 `compileJava`·`assemble` 결과를 Docker 제한과 분리해 보고했다 | Docker 없는 환경에서는 대상 비컨테이너 테스트와 compile/assemble 검증 및 미실행 제한을 각각 보고한다. 정상 전체 `build`는 Docker가 있는 환경에서 실행하며 `build -x test`를 전체 빌드의 대체 근거로 쓰지 않는다 |
| 2026-08-03 | 서로 다른 장기 브랜치가 각각 다음 Flyway 번호를 `V13`으로 선점한 채 순차 병합됐는데, 두 번째 PR에서 최신 `dev` 기준 migration 번호 중복을 다시 확인하지 않음 | 최신 `dev`에 `V13__create_ai_feedback_tables.sql`과 `V13__create_favorites.sql`이 함께 존재해 Flyway가 `Found more than one migration with version 13`으로 기동을 거부하고 모든 MySQL 테스트가 컨텍스트 생성 전에 실패 | 먼저 병합된 AI 피드백 V13은 유지하고 후발 즐겨찾기 migration을 다음 미사용 번호 V14로 교정 | migration 포함 브랜치는 PR 생성 전 최신 `dev`를 반영한 뒤 `db/migration`의 번호 유일성을 다시 확인하고, 실제 MySQL 컨텍스트 테스트나 전체 build로 Flyway validation을 실행한다 |
| 2026-08-04 | 이슈 #167에서 뉴스 수집용 `RestClient` 빈(`naverSearchRestClient`)을 `@Bean`으로 하나 더 등록하고, 주입부에는 `KisHistoricalCandleClientImpl`을 본떠 **필드에 `@Qualifier` + Lombok `@RequiredArgsConstructor`**를 썼다 (그 클래스 주석이 "이름 매칭을 명시한다"고 적고 있어 동작하는 패턴으로 믿었다) | `compileJava`·`spotbugsMain`이 전부 통과한다. 그러나 컨텍스트를 띄우면 `NoUniqueBeanDefinitionException: expected single matching bean but found 2: kisRestClient,naverSearchRestClient`로 **애플리케이션 전체가 기동하지 않는다** — 새로 만든 빈뿐 아니라 **기존 `KisHistoricalCandleClientImpl`이 함께 깨진다.** `javap -v`로 확인하니 Lombok이 생성한 생성자에 `RuntimeVisibleParameterAnnotations`가 **없다**(레포에 `lombok.config`가 없어 `@Qualifier`는 기본 copyableAnnotations가 아니다). 즉 KIS의 `@Qualifier`는 장식이었고 `RestClient` 빈이 하나뿐이라 타입 매칭으로 우연히 버티고 있었다. `@Profile("prod")` 빈은 어떤 기존 테스트도 인스턴스화하지 않아 임시 프로브(`ApplicationContextRunner` + `spring.profiles.active=prod`)를 돌리기 전까지 드러나지 않았다 | 빈을 추가하지 않는 쪽으로 방향을 바꿨다 — `RestClient.Builder`를 받아 클래스 안에서 완성하는 `ResendEmailSender`·`BithumbRestCandleProvider`·OAuth 공급자 형태(레포 6곳 중 5곳)로 교체하고 `NaverSearchRestClientConfig`는 삭제했다. 덤으로 `MockRestServiceServer`용 패키지 전용 생성자 seam이 생겼다 | **필드 `@Qualifier` + Lombok 생성자는 이름 매칭이 되지 않는다.** 2026-07-30 `@Value` 행과 같은 부류다 — Lombok은 스프링 주입 애노테이션을 필드에서 파라미터로 옮기지 않는다. 같은 타입 빈을 2개 이상 만들 때는 생성자를 손으로 쓰고 `@Qualifier`를 **파라미터**에 붙이거나, 애초에 빈을 늘리지 말고 `Builder`를 받아 클래스 안에서 완성한다. 그리고 **`@Profile("prod")` 빈은 기본 테스트 경로가 인스턴스화하지 않으므로** 새로 만들면 `ApplicationContextRunner`에 `spring.profiles.active=prod`를 줘 조립을 1회 확인한다 — 컴파일·SpotBugs 통과를 조립 근거로 삼지 말 것 |
| 2026-08-04 | 이슈 #193(즐겨찾기 인메모리 전환) 단위 테스트에서 `when(favorite.getInstrument()).thenReturn(instrument)`처럼, 바깥 `when()` 인자 표현식 안에서 mock 객체의 다른 getter를 또 스텁하려 함(중첩 `when()`) | Mockito가 `UnfinishedStubbingException`을 던진다 — 바깥 `when()` 호출이 완료되기 전에 안쪽 `when()`이 끼어들어 스텁 스택이 꼬인다. 대상이 엔티티든 아니든 발생하는 Mockito 자체의 제약이라 클래스 문제로 오판하기 쉽다 | 안쪽에서 필요한 mock 반환값을 로컬 변수로 먼저 계산·스텁 완료한 뒤 바깥 `when()`에 넘긴다. 이 참에 `Instrument`처럼 정적 팩토리(`Instrument.create(...)`)가 있는 엔티티는 mock 대신 실제 인스턴스를 쓰는 기존 관례(`CandleQueryServiceTest` 등)로 통일했다 | Mockito stubbing에서 `when()` 인자 표현식 안에 다른 `when()`/`mock().getX()` 체인을 직접 넣지 않는다 — 항상 로컬 변수로 먼저 분리한다. 정적 팩토리가 있는 엔티티는 mock보다 실제 인스턴스 생성을 우선한다 |
| 2026-08-04 | 이슈 #188에서 테스트 중 배치 크론이 실제로 등록되는 것을 막으려고 `build.gradle`의 `spring.config.additional-location`에 크론을 `"-"`(`Scheduled.CRON_DISABLED`)로 덮는 yml을 추가. **그 시스템 프로퍼티는 `ConfigDataEnvironmentPostProcessor`(즉 `@SpringBootTest`)만 해석하므로 `ApplicationContextRunner` 슬라이스 테스트에는 영향이 없다**는 기존 주석(`bithumb-feed-simulator-disabled-for-tests.yml`·`build.gradle`)과 `tasks.md`를 근거로, 설정 드리프트 테스트는 안전하다고 전제했다 | 의도한 `@SpringBootTest` 쪽은 정상인데 **`ConfigDataApplicationContextInitializer`를 쓰는 yml 전용 드리프트 테스트가 함께 깨졌다** — 이번에 손댄 `NewsCollectionPropertiesYamlTest`뿐 아니라 **건드리지도 않은 기존 `FeedbackBatchPropertiesTest`까지** `expected: "0 45 8 * * MON-FRI" but was: "-"`로 실패. 그 초기화자는 이름 그대로 `ConfigData` 처리를 그대로 돌리므로 시스템 프로퍼티의 `additional-location`을 `@SpringBootTest`와 **똑같이** 해석한다. 기존 주석의 "영향이 없다"는 **초기화자 없이 도는 순수 슬라이스**(`BithumbFeedSimulatorConditionalTest`)에만 해당하는 말이었다 | 드리프트 테스트 runner에 `.withSystemProperties("spring.config.additional-location=")`를 붙여 그 실행 동안만 테스트 전용 덮어쓰기를 걷어 냈다(`run()` 종료 시 자동 복원). 단정을 없애지 않고 "application.yml 원본을 보는 자리"를 유지한 것이다 | `spring.config.additional-location`으로 테스트 전용 값을 얹을 때는 **`ConfigDataApplicationContextInitializer`를 쓰는 슬라이스 테스트도 그 값을 본다**고 전제한다. "`@SpringBootTest`만 해석한다"는 기존 주석을 그대로 인용하지 말 것 — 그 문장이 참인 대상은 초기화자 **없는** `ApplicationContextRunner`다. 덮어쓴 값을 원본으로 대조해야 하는 테스트가 있으면 runner에서 그 프로퍼티를 빈 문자열로 되돌린다 |
| 2026-08-04 | 이슈 #208 1번 항목 작업 중 서브에이전트 세션이 `git checkout pr206` → `git checkout dev`를 실행 (다른 브랜치 내용을 확인할 목적으로 보인다) | 메인 세션의 HEAD가 작업 브랜치 `feat/208-post-sell-feedback`에서 `dev`로 옮겨졌다. **신설 소스 파일은 untracked라 살아남고 수정된 tracked 파일도 그대로 남아** 워킹 트리가 정상처럼 보이는데, **작업 브랜치에만 커밋돼 있던 `tasks.md`·`plan.md`는 dev 버전으로 되돌아갔다.** 메인 세션이 `tasks.md`의 1번 체크박스를 체크하려다 "File has been modified"로 실패하고, 다시 grep하니 **이전 이슈(#188)의 작업 항목 8개가 전부 `[x]`로 보였다** — planner가 방금 쓴 #208 범위 재작성이 사라진 것처럼 읽힌다. `git log --oneline`도 커밋 2개가 없는 dev 이력을 보여준다 | `git reflog`로 `checkout: moving from feat/208-post-sell-feedback to pr206` → `to dev`를 확인하고 **커밋은 유실되지 않았음**(브랜치가 그대로 `c7d06fd`를 가리킴)을 확인한 뒤 `git checkout feat/208-post-sell-feedback`으로 복귀. `tasks.md` 첫 줄과 `plan.md` 진행 상태 행을 대조해 복원을 검증했다 | **서브에이전트 프롬프트에 "브랜치를 옮기지 마라"를 명시한다** — 서브에이전트는 메인 세션과 같은 워킹 트리를 공유하므로 `git checkout`이 오케스트레이터의 작업 브랜치를 바꾼다. 다른 브랜치·커밋의 내용을 봐야 하면 `git switch`가 아니라 `git show <ref>:<path>`를 쓴다. 그리고 **파일 내용이 갑자기 이전 이슈 상태로 보이면 파일이 아니라 `git rev-parse --abbrev-ref HEAD`를 먼저 확인한다** — 되돌아간 파일과 남아 있는 untracked 파일이 섞여 "일부만 사라진 것"처럼 보인다. 워크트리 격리가 필요한 작업은 `docs/parallel-agents.md`를 따른다 |
| 2026-08-04 | 이슈 #208 2번 항목에서 노출 게이트의 시각 상한 sentinel로 `LocalTime.MAX`를 써서 `reveal_time <= :cutoff` 파인더에 넘김 — "과거 서비스 날짜는 그날 카드 전부가 이미 열렸으므로 상한을 최대로 둔다"는 의도였다 | `compileJava`도 단위 테스트도 통과한다. 그러나 실제 MySQL에서는 **정반대로 동작한다** — Connector/J가 `23:59:59.999999999`를 보내고 MySQL이 소수 초를 **올림해 `00:00:00`으로 접는다**(`reveal_time` 컬럼은 소수 초 없는 `TIME`). 조건이 `reveal_time <= '00:00:00'`이 되어 **자정 카드만** 남는다. 결과적으로 **어제 이전에 판 체결은 `priceMoves`가 항상 빈 배열이고 `buyToNewsMinutes`가 항상 `null`인데 예외도 로그도 없다.** 게이트 되돌림(게이트 ⑭)을 막으려 넣은 분기가 더 심하게 감추는 방향으로 작동했다. 진단 쿼리 실측: `select cast(? as char)`가 `LocalTime.MAX`에 `"00:00:00"`, `LocalTime.of(23,59,59)`에 `"23:59:59"`를 돌려주고 `time '11:26:00' <= LocalTime.MAX`가 `0`이다 | 과거 분기의 sentinel을 `LocalTime.of(23, 59, 59)`로 바꿨다(나노초를 버린다). 쿼리를 `<`로 바꾸거나 상한을 없애는 우회는 게이트 판정 자체를 바꾸므로 쓰지 않았다. 회귀 방지로 이 트랩을 실제 쿼리로 못박는 `@DataJpaTest`(`PriceMoveEventRepositoryTest`)를 남기고, 단위 테스트 상한 단정은 특정 sentinel을 고정하지 않도록 `isAfterOrEqualTo(23:59:59)`로 썼다 | **`LocalTime.MAX`·`LocalDateTime.MAX` 같은 나노초 sentinel을 DB 비교 파라미터로 넘기지 않는다** — 소수 초 없는 컬럼에서 반올림이 값을 **범위 밖으로** 옮긴다. "가장 큰 시각"이 필요하면 컬럼 정밀도에 맞춰 나노초를 버린다. 2026-08-01 행("Windows 100ns 해상도 vs MySQL `DATETIME(6)`")과 같은 부류이며, 그 행은 **동등 비교가 어긋나는** 경우였고 이 행은 **범위 조건이 조용히 뒤집히는** 경우다 — 후자는 결과가 0건이 되므로 단위 테스트로는 절대 드러나지 않는다. 시각 상한·하한을 넣는 파인더는 `@DataJpaTest`로 실제 쿼리를 1회 확인한다 |
| 2026-08-04 | 이슈 #208 4번 항목에서 "원장 불변"(8개 이슈 공통 완료 조건)을 검증하려고, 조회 전후로 원장 테이블들의 **행 수 스냅샷**을 raw JDBC로 떠서 비교하는 통합 테스트를 작성 | 테스트가 통과했다. 그런데 픽스처가 실제로 무엇을 잡는지 확인하려고 **저장 컴포넌트가 `accounts.cash_balance`를 바꾸도록 일부러 변형했더니 여전히 초록이었다.** 원인 둘이 겹쳤다 — (1) 스냅샷 조회가 같은 트랜잭션 안의 raw JDBC라 **Hibernate가 아직 flush하지 않은 UPDATE를 보지 못한다**, (2) 애초에 **행 수만 비교하면 UPDATE는 원리적으로 안 잡힌다**(INSERT·DELETE만 잡힌다). 원장을 건드리는 구현이 통과하는 테스트를 "원장 불변 검증 완료"로 보고할 뻔했다 | 스냅샷 직전에 `entityManager.flush()`를 넣고, 행 수 비교에 **값 비교**(체결 행 전 컬럼·계좌 현금 잔액·lot 잔여수량)를 더했다. 그 뒤 같은 변형이 red가 되는 것을 확인했다 | **"불변" 검증은 행 수로 하지 않는다** — UPDATE는 행 수를 바꾸지 않으므로 값까지 비교해야 한다. 그리고 **트랜잭션 안에서 raw JDBC로 스냅샷을 뜨기 전에 `flush()`한다** — 보류된 쓰기가 안 보여 조용히 초록이 된다. 이 조건은 spec 012의 **8개 이슈 공통**이라 뒤 이슈(7·8번)가 같은 테스트를 다시 쓰게 되므로 형태를 여기서 고정한다. 2026-08-03 PR #132 행("픽스처가 무엇을 잡는지 확인하지 않으면 조준 밖이 빈다")과 같은 부류다 — **통과를 근거로 삼기 전에 틀린 구현을 넣어 red를 한 번 본다** |
| 2026-08-04 | 이슈 #208 테스트에서 `stock_replay_sessions` 픽스처의 서비스 날짜를 **작성 당일 날짜(2026-08-04·08-05)로 하드코딩**하고, 항목별 검증을 `--tests` 필터로만 돌려 통과를 확인 | 필터 실행(1,086건)은 전부 초록인데 **전체 `./gradlew build`에서 `SellAllocationQueryServiceTest` 5건이 `setUp` 단계에서 통째로** `DataIntegrityViolationException: Duplicate entry '2026-08-04' for key 'uk_stock_replay_sessions_service_date'`로 깨졌다. 원인은 기존 `CandleQueryServiceIntegrationTest`가 **클래스 레벨 `@Transactional` 없는 `@SpringBootTest`라 그 날짜 세션을 공유 컨테이너에 커밋**하는 것이고, `@DataJpaTest`의 롤백은 남이 커밋한 행을 지우지 못한다. **`@Transactional`이 걸린 통합 테스트도 안전하지 않다** — 삽입 자체가 UNIQUE에 걸리므로 실행 순서만 바뀌면 터진다(이번에 새로 만든 통합 테스트 3개가 같은 폭탄을 안고 순서 운으로 통과하고 있었다) | 다섯 클래스의 **서비스 날짜만** 저장소에서 아무도 쓰지 않는 전용 연도(2031)로 옮겼다. `source_trading_date`는 UNIQUE 대상이 아니라 그대로 둬 "서비스 날짜 ≠ 원본 거래일" 성질을 유지했고, 남의 행을 지우거나 커밋된 행을 재사용하는 방향은 쓰지 않았다(그 행은 `PREPARING`·`source_trading_date=null`이라 단정이 성립하지 않는다) | **UNIQUE 컬럼에 들어가는 픽스처 값을 오늘 날짜나 흔한 리터럴로 두지 않는다** — 클래스 전용 값을 쓰고 저장소 전체 grep으로 유일성을 확인한다(날짜뿐 아니라 종목 심볼·회원 이메일도 같다). 그리고 **`--tests` 필터 통과를 회귀 근거로 삼지 않는다** — 공유 컨테이너에서 다른 클래스가 커밋한 행과 부딪히는 실패는 **전체 실행에서만, 그것도 실행 순서에 따라** 드러난다. 새 통합 테스트를 추가한 항목은 마무리 `build` 전에 한 번은 전체를 돌린다 |
| 2026-08-07 | `@Transactional` 통합 테스트에 Redis 캐시가 켜진 조회 경로를 태우면서 캐시 정리를 두지 않았다 (이슈 #245 조회 캐시 배선) | `MarketBriefingQueryGateIntegrationTest` 16건 중 **8건 실패**. `EMPTY`/`UNAVAILABLE`을 기대한 단정이 `READY`를 보고, 저장한 적 없는 다른 메서드의 픽스처(`"하이닉스 기사"`·`"삼성 기사"`)가 목록에 나왔다. **DB는 `@Transactional`로 롤백되지만 Testcontainers Redis는 실행 전체가 공유하는 static 싱글턴이라 롤백되지 않는다** — 앞 메서드가 채운 캐시가 롤백된 DB 상태와 어긋난 채 뒤 메서드에 그대로 보인다. 캐시 키가 `(거래일)`·`(거래일, 상한)`이라 클래스 안 모든 메서드가 같은 키를 쓴다 | 두 게이트 테스트에 `@BeforeEach`로 `feedback:query-cache:*` 삭제 훅을 추가했다(`FeedbackQueryCacheTestKeys.clear`). **캐시를 끄는 방향(`enabled=false`)은 쓰지 않았다** — 그러면 게이트 단정이 운영과 같은 배선을 한 번도 지나가지 않아 "계약 불변"의 증거가 못 된다 | **확증 방법: 코드를 한 줄도 바꾸지 않고 `FEEDBACK_QUERYCACHE_ENABLED=false`(Spring 완화 바인딩) 환경변수만 주고 다시 돌린다 — 전부 통과하면 원인은 캐시 누수다.** 정리는 `@AfterEach`가 아니라 **`@BeforeEach`**에 둔다(앞 테스트가 정리에 실패해도 항상 빈 캐시에서 시작한다). 그리고 **통과가 곧 안전이 아니다** — `InstrumentNewsQueryGateIntegrationTest`는 같은 키 충돌을 안고도 JUnit5 기본 메서드 순서 덕에 통과하고 있었다. Redis·파일 등 **트랜잭션 밖 공유 상태를 쓰는 경로를 통합 테스트에 새로 태우면 격리 훅을 함께 넣는다** |
| 2026-08-07 | 27개 통합 테스트의 중첩 `FixedClockTestConfig`를 공용 `TestClockConfig`로 기계 변환하면서, "수동 보완이 남았는가"를 **`TestClock` 필드가 있거나 `clock.set(` 호출이 있으면 완료**(OR)로 판정했다 (이슈 #134) | 필드만 생기고 세팅 호출이 없는 파일 1건(`CryptoWatchLockConcurrencyIntegrationTest`)이 판정을 빠져나가 자리표시자 시각(2000-01-01)으로 돌았다. **실패는 그 클래스 2건에서 끝나지 않았다** — 이 테스트는 종목을 커밋하고 `@AfterEach`가 `NOW` 날짜 기준으로 지우는데, 행이 2000년으로 쓰여 정리가 빗나갔고 남은 행이 무관한 `InstrumentRepositoryTest`(28건 단정)·`StockReplaySessionRepositoryTest` 5건을 함께 깨뜨렸다(전체 7건). 그 5개 클래스만 단독 실행하면 통과한다 | 빠진 `clock.set(NOW);` 한 줄을 넣고, 판정을 **AND**(필드가 있고 **그리고** 세팅 호출이 있음)로 다시 짜 나머지 26곳을 재확인했다 | 공유 픽스처(시계·컨텍스트 등)로 바꿀 때 **"세팅했는가" 판정은 OR가 아니라 AND로 짠다** — 선언만 남고 초기화가 빠진 파일이 정확히 그 틈으로 빠져나간다. 그리고 **전체 빌드 실패 목록에 무관해 보이는 리포지토리 카운트 테스트가 섞여 있으면 그것부터 고치지 말고 데이터 누수원부터 찾는다**(2026-07-30·2026-08-04 행과 같은 부류) |
| 2026-08-07 | 고정 `Clock` 통합 테스트에서 픽스처의 `created_at`과 `generated_at`을 둘 다 "지금"으로 두었다 (이슈 #245 항목 5, 코인 배치 무효화 검증) | "배치가 갱신하면 재조회가 새 값을 본다"를 검증하려던 테스트 2건이 `Expecting Optional to contain a value but it was empty`로 실패. 코인 재생성 판정이 **`created_at > generated_at`**(§C-9)인데 `Clock`이 고정이라 직전 요약의 `generated_at`과 새 기사의 `created_at`이 **둘 다 같은 `NOW`** 가 되어 부등호가 성립하지 않았다 — 배치가 갱신 없이 `Optional.empty()`로 빠져나갔다. **더 나쁜 것은 반대 방향이다**: 단정을 느슨하게 썼다면 이 테스트는 무효화가 아니라 "건너뛰기"를 검사하면서 초록으로 통과했을 것이다 | 픽스처가 두 시각을 직접 정하게 바꿨다 — 직전 생성 `08:30`을 기준으로 기사의 `created_at`을 `08:00`(이전, 건너뛰기 시나리오)과 `09:00`(이후, 갱신 시나리오)에 나눠 배치했다. 운영 코드는 고치지 않았다 | **고정 `Clock` 테스트에서 `A > B` 형태의 판정을 지나는 픽스처는 두 시각을 모두 명시한다** — 어느 한쪽이라도 `now`를 그대로 쓰면 경계가 등호에 걸려 의도한 분기의 반대편이 실행된다(#198의 고정 `Clock` 계열과 같은 부류다). 그리고 **"배치가 돌았다"를 가정하지 말고 반환값으로 단정한다** — `assertThat(refresh(...)).isPresent()`를 앞에 두면 실패가 "갱신했는데 값이 그대로"인지 "갱신 자체가 안 됨"인지 곧바로 갈린다 |
| 2026-08-09 | 전체 `./gradlew build`의 `:test`가 **단정 실패 없이 워커 사망으로 끝났는데 그것을 코드 실패로 읽을 뻔했다** (Issue #280). 직전에 `--tests '*PriceMove*'` 필터 실행이 있었으나 인과는 확인하지 못했다 | `> Task :test FAILED` — **단정 실패가 아니라 워커 프로세스 사망**이다. `MessageIOException: Could not write '/127.0.0.1:64100'` / `Connection reset by peer` 후 `Process 'Gradle Test Executor 1' finished with non-zero exit value -1 (NTSTATUS 0xFFFFFFFF)`. `build/test-results`에 `<failure>`가 **한 건도 없어** 어떤 테스트가 깨졌는지 특정할 수 없다. 6분 걸리던 빌드가 1분에 끝나 실패를 코드 탓으로 오해하기 쉽다 | `./gradlew --stop` 후 `./gradlew build --no-daemon --max-workers=1` 단독 재실행 — 코드를 한 줄도 바꾸지 않고 `BUILD SUCCESSFUL`. 2026-07-25 행(동시 실행 시 `EOFException`)과 증상 문자열은 다르지만 복구 절차가 같다. **원인은 특정하지 못했다** — 데몬 상태인지 필터 실행의 잔여 프로세스인지 확증하지 않았으므로 단정하지 않는다 | **`:test FAILED`를 봤을 때 실패 테스트 이름부터 찾는다.** `<failure>`가 0건이면 그것은 테스트 실패가 아니라 워커 사망이므로 코드를 고치러 가지 말고 `--stop` → `--no-daemon --max-workers=1` 재실행으로 먼저 갈라낸다. 여기서 코드를 의심하면 멀쩡한 변경을 되돌리게 된다 |
| 2026-08-09 | 워커 크래시 직후 `git add -A`로 커밋했다 (Issue #280 리뷰 2라운드) | JVM 치명 오류 덤프 `hs_err_pid26528.log`(1015행, 90KB)가 저장소 루트에 생겨 있었고 그대로 커밋에 실려 푸시됐다 — 커밋 1060 insertions 중 대부분이 이 파일이다. **988행부터 `Environment Variables` 절에 `PATH` 전체와 `USERNAME`이, 19행 `Command Line`에 워크스페이스 절대경로가 들어 있다.** 이 저장소는 squash가 아니라 merge라(`docs/git-conventions.md:78`) `git rm` 커밋을 얹으면 내용이 `dev` 히스토리에 영구히 남는다 | 푸시된 브랜치를 rebase로 다시 만들어 파일이 실린 커밋 자체에서 제거하고 강제 푸시했다(리뷰 중이라 브랜치 소유자가 나뿐이었다). `.gitignore`에 `hs_err_pid*.log`·`replay_pid*.log`를 넣었다 | **빌드가 비정상 종료한 뒤에는 `git add -A` 전에 `git status`로 새 파일을 눈으로 확인한다** — 크래시는 로그·힙덤프를 작업 디렉터리에 떨어뜨리고 그것들은 대개 로컬 환경을 담고 있다. 커밋 통계에서 **insertions가 변경 규모에 비해 이상하게 크면** 파일 목록부터 본다 |
| 2026-08-10 | 이슈 트리거 하네스에서 **구현 에이전트의 `./gradlew build`와 러너의 검증 빌드가 같은 워크스페이스에서 겹쳤다** (이슈 #276 처리 실행). 프롬프트 4번이 에이전트에게 빌드를 시키고, 바로 다음 스텝에서 러너가 같은 `build/`에 또 빌드를 돌리는 구조였다 | `> Task :test FAILED` — 단정 실패는 **0건**이고 `java.nio.file.NoSuchFileException: build/test-results/test/binary/in-progress-results-generic.bin`만 났다. 컴파일 태스크는 전부 `UP-TO-DATE`(앞선 빌드가 같은 워크스페이스에서 돌았다는 뜻), job 정리 단계에 `Terminate orphan process: pid (6991) (java)`. 에이전트는 100턴·$7.25를 쓰고 브랜치도 PR도 만들지 못했고, PR이 없으니 이력 코멘트 스텝(전부 `steps.pr.outputs.number != ''` 게이트)도 하나도 안 돌아 **이슈에 아무 알림이 가지 않았다**. [실행 로그](https://github.com/finplay-team/finplay/actions/runs/31353092525/job/93347455971) | 두 검증 빌드 스텝(`build`·`build_autofix`)에서 `./gradlew --stop` → Gradle 프로세스가 실제로 사라질 때까지 대기 → `./gradlew build --no-daemon` 순서로 실행하도록 변경 (PR #310) | **하네스에서 에이전트와 러너가 같은 워크스페이스를 쓰면 빌드는 반드시 단독 실행으로 격리한다.** `--stop`은 바쁜 데몬에 종료 *요청*만 보내고 즉시 반환하므로 프로세스 종료 확인까지 해야 한다. 2026-07-25·2026-08-09 행과 같은 실패 모드가 CI에서 재현된 것이다 — 증상 문자열이 달라도 `:test FAILED` + `<failure>` 0건이면 코드가 아니라 프로세스 충돌부터 의심한다 |
| 2026-08-11 | EC2 배포 검증 중 사용자에게 헬스체크 명령으로 `curl -fsS http://localhost:8080/actuator/health`를 **호스트에서** 실행하라고 안내했다. `compose.deploy.yaml`의 `app`이 `ports`가 아니라 `expose`만 쓴다는 것(호스트에 포트를 열지 않는 것이 이슈 #108의 의도적 설계다)을 확인하지 않았다 | `curl: (7) Failed to connect to localhost port 8080` — 그런데 같은 시점 `docker ps`는 `Up (healthy)`였다. 컨테이너 healthcheck가 통과하는데 curl은 실패하는 모순된 신호가 나와, 앱이 실제로 뜬 것인지 판단이 한 번 막혔다. 직전까지 ElastiCache TLS 문제로 앱이 죽어 있었기 때문에 **"아직도 안 뜬 것"으로 오해하기 쉬운 상황**이었다 | `docker exec finplay-deploy-app-1 curl -fsS http://localhost:8080/actuator/health`로 컨테이너 안에서 호출해 `{"status":"UP"}` 확인 | **컨테이너 스택의 헬스체크 명령을 만들 때 `compose` 파일의 `ports`/`expose`를 먼저 본다.** 호스트에 안 열린 포트를 호스트에서 부르면 앱 상태와 무관하게 실패한다. `docker ps`의 `(healthy)`와 호스트 curl 실패가 동시에 나오면 앱을 의심하기 전에 포트 노출부터 확인한다 — 이 저장소는 nginx만 외부 진입점으로 두는 설계라 `app`은 항상 이 상태다 |
| 2026-08-11 | 새로 만든 파일 3개(`compose.bluegreen.yaml`, `deploy/nginx-blue.conf`, `deploy/nginx-green.conf`)와 기존 파일 2개(`.gitignore`, `.env.example`)의 미커밋 수정을 로컬 검증(`docker compose ... up`)까지 마친 뒤에도 바로 커밋하지 않고 다음 대화로 넘어갔다. 같은 워크스페이스를 다른 프로세스(다른 터미널·IDE·별도 Claude 세션으로 추정)가 공유하고 있다는 것을 몰랐다 | 그 사이 외부에서 `git checkout fix/277-comment-tombstone-delete`(reflog로 확인)와 함께 untracked 정리(추정)가 일어나, 새 파일 3개가 통째로 사라지고 기존 파일 2개의 미커밋 수정분도 날아갔다. 브랜치를 원래대로 되돌려도 파일은 돌아오지 않았다 — checkout은 커밋된 내용만 복원하지, 애초에 커밋 안 된 작업은 복원 대상이 아니기 때문이다. `git status`가 깨끗해 보여서(untracked만 있고 확인용 test 디렉터리와 헷갈림) 처음엔 원인 파악이 늦어질 뻔했다 | 파일 내용을 대화 컨텍스트에서 그대로 복원해 다시 작성하고, **이번에는 검증 직후 바로 커밋 + push**했다. 또한 그 시점에 대상 PR(#329)이 이미 머지돼 있어, 새 커밋은 머지된 옛 브랜치가 아니라 최신 `dev`에서 새로 브랜치(`feat/bluegreen-deploy-files`)를 파서 cherry-pick으로 옮겼다 | **로컬 검증(docker up 등)까지 끝난 새 파일·수정 파일은 다음 작업으로 넘어가기 전에 즉시 커밋한다 — "이따 한꺼번에 커밋" 패턴은 같은 저장소 경로를 쓰는 다른 프로세스의 브랜치 전환에 그대로 노출된다.** 특히 여러 파일에 걸친 untracked 신규 파일은 checkout/clean 한 번에 통째로 사라질 수 있어 위험도가 크다. 공유 워크스페이스인지 확신할 수 없다면 항상 커밋된 것만 안전하다고 가정한다. 작업 중 브랜치가 예상과 다르면(reflog에 내가 실행하지 않은 `checkout`이 있으면) 먼저 무엇이 사라졌는지 diff로 확인하고 사용자에게 알린다 |
| 2026-08-10 | 이슈 #319(코인 가상 가격 세션 tick 진행) tester가 신설 `PracticePriceTickConcurrencyIntegrationTest`(`education.priceruntime.service` 패키지)를 `@Transactional` 없는 `@SpringBootTest`로 작성하면서(동시 스레드가 실제 커밋을 봐야 lock 경합을 재현할 수 있어 의도적으로 뺐다) `@AfterEach` 정리를 두지 않았다 | `--tests` 필터 실행(해당 클래스만)은 통과했지만, 메인 세션의 전체 `./gradlew build`에서 **건드리지 않은** `InstrumentRepositoryTest` 3건이 `Expected size: 28 but was: 30`(크립토 12→14)로 실패했다. `education.priceruntime`이 `market.repository`보다 알파벳순으로 먼저 실행돼, 정리 없이 커밋된 테스트 전용 crypto Instrument 2건이 그 뒤의 정확한 개수 단정을 깨뜨렸다 — 2026-07-30 행과 같은 실패 모드지만, 그 행의 해법(`@Transactional` 추가)은 여기 그대로 못 쓴다: 동시성 테스트는 워커 스레드가 메인 스레드의 미커밋 데이터를 볼 수 없어야 하므로(별도 커넥션) `@Transactional`을 걸면 lock 경합 자체가 사라진다 | 같은 패키지의 기존 `PracticeHoldingReflectionConcurrencyIntegrationTest` 관례를 그대로 따라 `@AfterEach`에 raw `JdbcTemplate` 삭제(자식→부모 FK 순서: `practice_price_sessions`→`instruments`→`users`)를 추가. `--tests` 필터 재실행과 전체 `build` 재실행 모두 통과 확인 | **`@Transactional` 없는(의도적) 신규 concurrency 통합 테스트를 추가할 때는 같은 패키지의 기존 concurrency 테스트에 `@AfterEach` raw JDBC 정리가 있는지 먼저 확인하고 그대로 따른다.** `--tests` 필터로 해당 클래스만 통과했다고 안전하다고 보지 않는다 — 카운트 단정이 깨지는 건 **다른 클래스가 이후 실행될 때만**(패키지 알파벳 순서) 드러나므로 전체 `build`로 확인해야 한다(2026-07-30·2026-08-04 행과 같은 부류) |
| 2026-08-12 | 이슈 #347(OCO 엔진 골격)에서 `ExitPlan` 엔티티의 **private 생성자 끝에서** 019 tagged union 불변식 검증(`validateRateSnapshot`)을 호출해 `IllegalArgumentException`을 던지게 했다 — "엔티티는 팩토리 안에서 항상 유효한 상태로 만든다"(conventions.md)를 생성자에서 강제하는 게 자연스럽다고 봤다 | `compileJava`는 통과하고, `spotbugsMain`만 `CT_CONSTRUCTOR_THROW`("Exception thrown ... will leave the constructor. The object under construction remains partially initialized and may be vulnerable to Finalizer attacks")로 실패한다. 같은 세션에서 만든 record 4개(`ExitPriceInputDto` 등)는 compact 생성자에서 똑같이 던지는데 **하나도 걸리지 않았다** — record는 final이라 finalizer 공격 대상이 아니기 때문이다. 그래서 "생성자에서 던져도 record는 되니 엔티티도 된다"고 오판할 여지가 있다 | 검증 호출을 생성자에서 빼내 두 정적 팩토리(`createGeneral`·`createEducational`)가 `new` **전에** 호출하도록 옮겼다. 같은 패키지 `Trade.of`의 `validateStockReplaySession`이 이미 그 형태다 | **엔티티(비-final 클래스)의 생성자에서는 예외를 던지지 않는다 — 검증은 정적 팩토리가 `new` 전에 한다.** record의 compact 생성자는 던져도 SpotBugs가 잡지 않으므로 record 선례를 엔티티 근거로 삼지 말 것. 2026-07-29 `EI_EXPOSE_REP` 행과 같은 부류다 — `spotbugsMain`은 `build`·`check`에서만 도니, 엔티티·record를 새로 만든 항목은 `compileJava` 뒤에 `spotbugsMain`만 단독으로 1회 돌린다(이 머신에서 1분, Docker 불필요) |
| 2026-08-11 | 이슈 #278(문서 전용 2행 수정) 완료 전 `./gradlew build`를 이 하네스(Windows, Bash 툴 = Git Bash)에서 그대로 실행하고, 실패하자 "PowerShell에서는 될까" 하며 전체 빌드를 또 돌렸다 (컴파일·포맷 검사로 충분한 문서 전용 변경에 3800여 개 통합 테스트 전체를 두 번 태운 판단 자체가 과했다) | Bash 툴: `3811 tests completed, 983 failed`, 전부 `DefaultCacheAwareContextLoaderDelegate`에서 `Failed to load ApplicationContext` → 근본 원인은 `IllegalStateException: Could not find a valid Docker environment`(`TestcontainersConfiguration.<clinit>`). 같은 시각 `docker info`·`docker ps`는 CLI로는 정상 응답(Desktop 29.4.2, Up)해서 "Docker가 없다"는 2026-07-27 행과는 증상이 다르다. PowerShell 툴: 이번엔 Testcontainers가 Docker에 실제로 붙어(여러 `HikariPool-N:housekeeper` 스레드로 확인) 테스트가 진행됐지만, 전체 스위트 도중 `OutOfMemoryError`가 연쇄 발생하며 50분 넘게 멈췄다. 로그를 먼저 확인하지 않고 `--stop`으로 데몬부터 죽여 실제 원인(OOM) 파악이 늦어졌다 | `compileJava`·`compileTestJava`·`spotlessCheck`만 별도로 돌려 컴파일·포맷은 정상임을 확인하고, 문서 전용 변경은 이 결과로 완료 처리 | **문서 전용(비컴파일 대상) 변경에는 애초에 전체 `./gradlew build`를 시도하지 않는다** — `compileJava`/`compileTestJava`/`spotlessCheck`로 검증 범위를 좁힌다. 이 하네스(Bash 툴)는 Testcontainers가 Docker 소켓을 못 찾아 전량 실패하고, PowerShell 툴은 소켓 접근은 되지만 전체 통합 스위트를 다 돌리면 메모리 부족으로 죽는다 — **둘 다 이 머신에서 전체 `build`를 완주시키는 용도로 쓰지 않는다.** 오래 걸리는 백그라운드 명령이 멈춘 것처럼 보이면 강제 종료하기 전에 먼저 지금까지의 출력을 읽어 원인을 확인한다 |
