# Spec: 체결별 투자일기 작성 (매수 · 매도)

> 단계: **2차 MVP (팀 회의 표현: 1차 고도화)**. 2026-07-28 Notion API 표 확인 결과 1차 MVP가 아님을 확인해 하향 조정했다.
>
> 상태: **2차 착수** (2026-08-04, 이슈 [#183](https://github.com/finplay-team/finplay/issues/183) — JOUR-003 매도 회고 작성). `plan.md`·`tasks.md`가 이 폴더에 있다.
>
> | 착수 | 요구사항 | 이슈 | 상태 |
> |---|---|---|---|
> | 1차 | JOUR-001 매수 회고 작성 | [#159](https://github.com/finplay-team/finplay/issues/159) (PR #181) | 구현 완료 |
> | 2차 | JOUR-003 매도 회고 작성 | [#183](https://github.com/finplay-team/finplay/issues/183) | **이번 범위** |
>
> 이번 착수 범위는 **JOUR-003(매도 회고 작성) 1건뿐**이다. JOUR-002·004·005·006은 아래 §범위 제외에 남긴다.
>
> PRD 근거: `docs/prd.md` **JOUR-001**·**JOUR-003**. 선행: `004-order-buy`(매수 체결), `005-order-sell`(매도 체결·FIFO 배분) — 모두 완료됨.
>
> **요구사항 ID 표기 정리(2026-08-04)** — 이 문서의 초안은 같은 요구사항을 `PORT-004`로 적고 있었으나, PRD와 이슈 #159는 `JOUR-001`로 부른다. **PRD가 정본이므로 `JOUR-001`로 통일한다.** `PORT-004`는 더 이상 쓰지 않는다.

## 개요

인증 사용자가 본인 소유의 체결 1건에 투자일기 1건을 작성한다. 매수 체결에는 매수 회고를(JOUR-001, 구현 완료), 매도 체결에는 매도 회고를(JOUR-003, 이번 범위) 작성한다. 투자일기는 체결 이후의 기록이며 주문·체결·계좌·잔액·보유·FIFO lot·실현손익 원장을 변경하지 않는다. 이번 착수 범위는 **매도 회고 작성 엔드포인트 1개**이며, 수정·목록·상세·AI 복기는 포함하지 않는다.

## 사용자 시나리오

- 사용자는 매수 체결 후 해당 체결에 투자일기 본문을 한 번 작성한다.
- 사용자는 매도 체결 후 해당 체결에 매도 회고 본문을 한 번 작성한다.
- 사용자는 매수 체결에는 매도 회고를, 매도 체결에는 매수 회고를 작성할 수 없다.
- 사용자는 다른 사용자의 체결에는 투자일기를 작성할 수 없다.
- 사용자는 같은 체결에 같은 종류의 투자일기를 다시 작성할 수 없다.
- 사용자는 같은 종목을 매수·매도했더라도 매수 회고와 매도 회고를 각각 별개로 작성한다.
- 사용자는 투자일기를 작성하지 않아도 매매·보유·커뮤니티 기능을 계속 사용할 수 있다.

## 요구사항

### JOUR-001 매수 투자일기 작성 (1차 착수, 구현 완료)

- [x] `POST /api/trades/{buyTradeId}/journal`로 투자일기를 작성한다.
- [x] `buyTradeId`가 존재하는 매수 체결이며 인증 사용자 소유인지 검증한다.
- [x] 매수 체결 1건당 투자일기 1건만 존재한다 (`UNIQUE(buy_trade_id)`).
- [x] 이미 투자일기가 존재하면 409 `DUPLICATE_RESOURCE`로 거부한다.
- [x] 본문은 공백이 아닌 텍스트여야 하며, 목표가·손절가·예상보유기간 같은 별도 필드는 이번 계약에 포함하지 않는다.
- [x] 성공 시 투자일기 ID, 매수 체결 ID, 본문, 작성시각을 반환한다.

### JOUR-003 매도 투자일기 작성 (이번 착수 범위, 이슈 #183)

- [ ] `POST /api/trades/{sellTradeId}/sell-journal`로 매도 회고를 작성한다.
- [ ] `sellTradeId`가 존재하는 매도 체결이며 인증 사용자 소유인지 검증한다.
- [ ] 매도 체결 1건당 매도 회고 1건만 존재한다 (`UNIQUE(sell_trade_id)`).
- [ ] 이미 매도 회고가 존재하면 409 `DUPLICATE_RESOURCE`로 거부한다.
- [ ] 본문은 공백이 아닌 텍스트여야 하며, 목표가·손절가·예상보유기간 같은 별도 필드는 이번 계약에 포함하지 않는다 (JOUR-001과 동일).
- [ ] 성공 시 투자일기 ID, 매도 체결 ID, 본문, 작성시각을 반환한다.

## 비즈니스 규칙

- 본인의 체결에만 작성할 수 있다. 없는 체결은 404, 타인 체결은 403으로 거부한다.
- **체결 구분과 회고 종류가 맞아야 한다.** 매수 회고는 매수가 아닌 체결에, 매도 회고는 매도가 아닌 체결에 대해 400으로 거부한다.
- **검증 순서는 `존재(404) → 소유(403) → 체결 구분(400) → 중복(409)`으로 고정한다.** 타인 체결이면 그 체결의 매수·매도 속성을 오류로 흘리기 전에 403이 먼저다. 매수·매도 회고가 같은 순서를 쓴다.
- 투자일기 저장과 소유권·중복 검증은 한 트랜잭션에서 처리한다.
- 투자일기 작성 성공·실패는 주문·체결·계좌·잔액·보유·FIFO lot·실현손익을 변경하지 않는다.
- 동시 중복 작성은 애플리케이션 검증과 유니크 제약(`UNIQUE(buy_trade_id)`·`UNIQUE(sell_trade_id)`)으로 한 건만 성공시킨다.
- 투자일기는 원장(`trades`)을 **읽기만** 한다. 투자일기 쪽에서 체결·계좌·보유를 갱신하는 경로를 만들지 않는다.
- 매수 회고와 매도 회고는 서로의 존재를 검증 조건으로 삼지 않는다. 매수 회고 없이 매도 회고만 쓸 수 있고, 그 반대도 가능하다.

## 범위 제외

이번 착수(JOUR-003)에서 하지 않는 것이다. 괄호 안은 다루는 PRD 요구사항이다.

- **매수 회고 수정** (JOUR-002) — `docs/prd.md`가 **Decision Gate 미해결**로 표시한 항목이다. `005-order-sell/spec.md`는 "첫 매도 배분이 발생한 매수 lot은 투자일기 수정이 잠긴다"고 규정하지만 **잠금 판정 시점·조건·오류 코드(`JOURNAL_LOCKED` 예정)가 팀 확정 전**이라, 수정을 지금 구현하면 잠금 없는 계약이 먼저 굳는다.
- **매도 회고 수정** (JOUR-004) — 이 이슈 이후 별도 이슈다. PRD는 매도 회고가 후속 매도 배분에 의한 잠금 대상이 **아니라고** 적었지만 "그 외 잠금 조건이 필요한지는 착수 시 확정한다"고 남겼다. 이번 범위는 작성뿐이므로 `updated_at` 컬럼도 만들지 않는다.
- **투자일기 상세 조회** (JOUR-005) — 역시 **Decision Gate 미해결**이다. 매수 회고와 매도 회고를 **같은 `journalId` 리소스로 조회할지, 식별자 체계를 분리할지**가 확정 전이다. 이 결정이 곧 리소스 URL과 응답 계약을 좌우하므로 확정 후 착수한다. 이번 착수는 그 결정을 앞당기지 않는다 — `plan.md` §데이터 모델의 "테이블을 합치지 않는 이유"를 참고한다.
- **투자일기 목록 조회** (JOUR-006) — 커서 페이지네이션·`market` 축.
- 투자일기 삭제 (PRD에 요구사항 없음).
- 목표가·손절가·근거·예상보유기간 같은 구조화 필드.
- 첫 매도 전 수정 잠금과 `JOURNAL_LOCKED` 오류 코드.
- 매도 회고 응답에 실현손익·배분 lot 등 매도 결과 정보를 포함하는 것 (`GET /api/trades`가 이미 제공한다).
- 투자일기 기반 AI 피드백·가격 알림·주간 회고 (spec 012 범위).

## 완료 조건

### 1차 착수 (JOUR-001, 이슈 #159) — 완료

- [x] 본인 매수 체결에 투자일기 작성 통합 테스트 통과.
- [x] 같은 매수 체결의 순차·동시 중복 작성이 한 건만 성공하고 나머지는 409인 테스트 통과.
- [x] 없는 체결·매도가 아닌 체결·타인 체결·공백 본문 거부 테스트 통과.
- [x] 성공·실패 전후 주문·체결·계좌·잔액·보유·손익 원장이 변하지 않는 통합 테스트 통과.
- [x] **신규 Flyway 마이그레이션으로 투자일기 테이블을 생성한다** (`V15__create_buy_trade_journals.sql`).
- [x] **`docs/api-routes.md`·`docs/api-contracts.md`에 신규 엔드포인트를 같은 커밋에서 반영한다** (CLAUDE.md 규칙 7).
- [x] **이 문서의 "2차 착수 전 초안·지금은 착수하지 않는다" 상태 서술을 실제 착수 상태로 갱신한다** (2026-08-04 반영 완료 — 위 머리말).
- [x] `./gradlew build` 통과.

### 2차 착수 (JOUR-003, 이슈 #183) — 이번 범위

- [ ] 본인 매도 체결에 매도 회고 작성 통합 테스트 통과.
- [ ] 같은 매도 체결의 순차·동시 중복 작성이 한 건만 성공하고 나머지는 409인 테스트 통과.
- [ ] 없는 체결·매수인 체결·타인 체결·공백 본문 거부 테스트 통과.
- [ ] 성공·실패 전후 주문·체결·계좌·잔액·보유·손익 원장이 변하지 않는 통합 테스트 통과.
- [ ] **신규 Flyway 마이그레이션으로 매도 투자일기 테이블을 생성한다** (ADR-0004 — 착수 시점 `dev` 기준 다음 버전 번호를 확인하고, 머지된 마이그레이션은 수정하지 않는다. `buy_trade_journals`가 V15, 조사 시점 `dev` 최신은 V16이다).
- [ ] 같은 매도 체결에 매수 회고를, 같은 매수 체결에 매도 회고를 쓸 수 없음(각각 400)을 확인한다.
- [ ] 매수 회고 기존 계약(`POST /api/trades/{buyTradeId}/journal`)과 기존 테스트가 그대로 통과한다.
- [ ] **`docs/api-routes.md`·`docs/api-contracts.md`에 신규 엔드포인트를 같은 커밋에서 반영한다** (CLAUDE.md 규칙 7).
- [ ] `./gradlew build` 통과.
