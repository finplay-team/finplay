# Run Log: 021-general-risk-management-oco

## AI 로그 (에이전트 참조용)
| 시각 | 에이전트 | 실행 명령 | 근거 |
|---|---|---|---|
| 14:39 | implementer | `.\gradlew.bat spotlessApply compileJava` | plan.md 데이터 모델·오류 코드, ADR-0004, ADR-0002 |
| 14:59 | implementer | `.\gradlew.bat spotlessApply compileJava spotbugsMain` | plan.md 일반 경로 3~9단계·잠금 순서, 019 계산 정책, 015 예약 원장 |
| (#348) | implementer | `& gradlew.bat compileJava` (JAVA_HOME=ms-17.0.20) | plan.md "API 설계"·"일반 경로 검증 순서"·"멱등성"·"잠금 순서" 표, docs/conventions.md 레이어 규칙, ADR-0002 |
| (#348) | implementer | `& gradlew.bat test --tests "com.finplay.api.common.ErrorCodeTest"` (JAVA_HOME=ms-17.0.20) | 오케스트레이터 지적: 새 오류 코드 2건 추가로 `ErrorCodeTest`의 소진적 개수·매핑·메시지 단정이 깨짐 |
| (#349) | implementer | `& gradlew.bat compileJava spotbugsMain` (JAVA_HOME=ms-17.0.20) | plan.md "트리거·취소·잠금 순서"(holding → plan)·"데이터 모델"(triggered_order_id), `LimitOrderTriggerListener`/`LimitOrderFillService`(015) 재사용 패턴, ADR-0002 |
| (#349) | implementer | `& gradlew.bat compileJava compileTestJava spotbugsMain test --tests "com.finplay.api.order.service.ExitPlanFillServiceTest"` (JAVA_HOME=ms-17.0.20) | 리뷰 차단 수정 근거: 기존 시장가 매도(`OrderExecutionService`)·지정가 체결(`LimitOrderFillService`)의 잠금 순서 `account → holding` 관례 |
| (목록·응답 계약 전환) | implementer | `.\gradlew.bat compileJava` (JAVA_HOME=ms-17.0.20) | plan.md "API 설계"·"응답 계약", docs/conventions.md DTO·레이어 규칙 |
| (#351 통합·경합 검증) | implementer | `& gradlew.bat test --tests "com.finplay.api.order.*"` × 반복, `& gradlew.bat build` (JAVA_HOME=ms-17.0.20) | plan.md "테스트 계획" 통합 시나리오, `015-limit-order`의 `LimitOrderConcurrencyIntegrationTest` 취소-대-체결 패턴, docs/agent-mistakes.md 2026-08-10 Redis 락 정리 사례 |
| (#461 샌드박스 생성 차단) | 메인 세션 | `./gradlew --no-daemon --max-workers=1 build` | 이슈 #461 코멘트 "1안으로 구현해줘", 047 spec TUTORIAL-CASH-ISOL-010 |

## 모니터링 (사람용 요약)
- 14:39 — exit_plans/conditions/idempotency_keys V35 migration + 엔티티·Repository·오류 코드 2건 추가, 컴파일 통과.
- 14:59 — ExitPricePolicy(019 공식)+ExitPlanCreationService(holding 잠금·PENDING 1건·예약·저장) 추가, 컴파일·SpotBugs 통과.
- 회귀 발견·수정: `ExitPlanRepositoryTest`의 `educationalPlan` 헬퍼가 강화된 팩토리 불변식(교육 경로 buyTrade non-null)과 맞지 않아 2건 실패 → 실제 buyTrade를 생성해 전달하도록 수정(`4f3d880`). 전체 `./gradlew build` 통과.
- 리뷰(reviewer, 리뷰 모드): 차단 0건. [권장] `ExitPlanCreationService.lockHolding`이 `PortfolioSellService.getHoldingForUpdate`를 재사용하는데, 그 메서드 JavaDoc은 "이미 예약 확인된 값" 전제의 비동기 리스너 문맥용이라 동기 생성 경로와 호출 문맥이 어긋난다 — **다음 이슈(#348, 일반 경로 생성·취소)에서 전용 접근자 분리 또는 JavaDoc 갱신을 검토할 것.**
- (#348) 일반 경로 생성·취소 구현: `POST /api/exit-plans`(intentionId 생략, 있으면 400)·`DELETE /api/exit-plans/{id}` 추가. 기존 `ExitPlanCreationService`/`ExitPlan` 생성자는 손대지 않고(#347 테스트 회귀 방지), 멱등키 저장은 새 `ExitPlanIdempotentCreationService`(별도 빈, 같은 트랜잭션)로 분리. 취소는 `holding → plan` 순서로 각각 재조회+잠금해 트리거 엔진과 잠금 순서를 맞췄다. 오류 코드 `EXIT_PLAN_NOT_FOUND`·`EXIT_PLAN_NOT_PENDING` 신설. `compileJava` 통과, 테스트는 작성하지 않음.
- 회귀 발견·수정(오케스트레이터 보고): 새 오류 코드 2건 추가로 `ErrorCodeTest`의 소진적 단정(`hasSize(39)`→41, HTTP 상태 `Map.ofEntries`, `exitPlanErrorsKeepPublicDefaultMessages`)이 깨져 `EXIT_PLAN_NOT_FOUND`·`EXIT_PLAN_NOT_PENDING` 항목을 추가해 수정. `ErrorCodeTest` 단독 재실행 통과.
- PR #368 사람 리뷰(namdongyeob) 차단 2건 수정:
  - **차단 1(항상 500)**: `HoldingService.findHoldingForOwner`가 `findById`로 조회해 반환한 `Holding`은 `instrument`가 LAZY라 `open-in-view: false`에서 세션 종료 후 `ExitPlanService.validateMarketIsCrypto`의 `.getInstrument().getMarket()` 접근이 `LazyInitializationException`(500)으로 죽었다. `HoldingRepository.findByIdFetchingInstrument`(JOIN FETCH)를 추가해 `findHoldingForOwner`가 이를 쓰도록 교체(`account`는 이미 필터에서 초기화되므로 그대로 둠). 이 메서드를 직접 스텁하던 `HoldingServiceTest` 3건도 `findByIdFetchingInstrument`로 갱신. 재현 회귀 테스트 `ExitPlanGeneralPathOsivBoundaryIntegrationTest`(신규, `@Transactional` 없음, `PostSellFeedbackBoundaryIntegrationTest` 선례)를 추가 — 기존 `ExitPlanGeneralPathIntegrationTest`는 클래스 `@Transactional`이 테스트 트랜잭션을 요청 전체에 걸쳐 열어 둬 이 버그를 못 잡았음을 확인.
  - **차단 2(정밀도 검증 누락 → 500)**: `ExitPlanCreateRequest`의 `stopLoss`/`takeProfit`(`DECIMAL(18,8)` 기준 `@Digits(integer=10, fraction=8)`)·`stopLossRate`(`DECIMAL(7,4)` 기준 `@Digits(integer=3, fraction=4)`)·`takeProfitRate`(`DECIMAL(8,4)` 기준 `@Digits(integer=4, fraction=4)`)·`quantity`에 `@Positive` 추가. PERCENT 원본 rate가 컬럼 자리수를 넘으면 400으로 걸러진다. `ExitPlanControllerTest`에 오버사이즈 rate 2건(stopLossRate·takeProfitRate) 400 검증 케이스 추가.
  - `compileJava`·`compileTestJava` 통과 확인(JAVA_HOME=ms-17.0.20). 전체 테스트·build는 오케스트레이터가 재실행.
- (#349) 가격 트리거·GTC 체결 구현: `ExitPlanTriggerListener`(일반 `@EventListener`, `LimitOrderTriggerListener`와 동일 관례 — 건별 catch, FeedConnectionStatus 체크 없음)가 `CryptoPriceUpdatedEvent`에서 `ExitPlanRepository.findPendingExitPlansToFill`(신규, `(takeProfitPrice <= price) OR (stopLossPrice >= price)`)로 후보를 조회하고 `ExitPlanFillService.fillIfPending(planId, price)`에 건별 위임한다. 잠금 순서는 `ExitPlanCancelService`와 동일하게 `holding → plan`(unlocked ownership 조회로 holding FK 확보 → holding 잠금 → `findByIdForUpdate`로 plan 잠금 → PENDING 재확인, terminal이면 no-op skip — 정확히 한 번·중복/역순 이벤트 규칙의 핵심 메커니즘). `OrderExecutionService`는 `Idempotency-Key`·요청 DTO 등 API 요청 문맥에 묶여 있어 재사용하지 않기로 판단(plan.md가 "먼저 코드를 읽고 판단" 위임) — 대신 서버가 직접 `Order.create`(MARKET, 즉시 FILLED)+`Trade.of`를 생성하고, `PortfolioSellService.applySellTrade`/`finalizeSellRealizedPnl`·`LimitOrderFeeCalculator`(코인 수수료 계산)는 그대로 재사용. 트리거 전용 주문 식별자는 `EXIT_PLAN_TRIGGER-{planId}` 결정적 문자열(사용자 요청 문맥이 없어 신규 도입, exactly-once 잠금이 unique 제약과 충돌하지 않음을 보장). `ExitPlan.fillTakeProfit`/`fillStopLoss`, `ExitPlanCondition.trigger`/`cancelByOco` 신규 상태 전이 메서드 추가(기존 `cancel()`과 동일하게 PENDING 전제 검증 후 전이). 주식 세션 만료·`CANCELLED_EXPIRED` 분기는 범위 밖(spec 확정). 컨트롤러 변경 없어 `docs/api-routes.md`·`docs/api-contracts.md` 갱신 대상 아님. `docs/prd.md` §3 "투자 실습 — OCO exit plan" 행 갱신(가격 트리거 완료 반영). `compileJava`·`spotbugsMain` 통과(JAVA_HOME=ms-17.0.20). 정정: 이후 tester가 `ExitPlanFillServiceTest`·`ExitPlanTriggerListenerTest` 등 테스트 3개 파일을 작성했다(이 줄의 이전 "테스트는 작성하지 않음" 기록은 implementer 시점 기준이었을 뿐 최종 상태가 아니었다).
- (#349 리뷰 차단 수정, 새 세션) `ExitPlanFillService`가 `Account`를 잠그지 않고 `holding.getAccount()`(lazy 참조, 락 없음)로 `finalizeSellRealizedPnl`의 `account.addCash`/`addRealizedPnl`을 mutate하던 문제를 고쳤다 — 기존 시장가 매도(`OrderExecutionService`)·지정가 체결(`LimitOrderFillService.fillIfPending`)은 둘 다 cash 갱신 전에 `accountService.getAccountByIdForUpdate(...)`로 계좌를 먼저 잠그는데 이 서비스만 생략돼 있었다(lost-update 위험, `Account`에 `@Version` 없음). plan.md의 `holding → plan` 잠금 순서는 예약수량 원장 규정이지 현금 갱신 순서를 규정한 게 아니므로, 전체 순서를 `account → holding → plan`으로 바꿨다 — `AccountService` 주입 추가, `ownershipCheck.getHolding().getAccount().getId()`(FK 값만 읽어 lazy 초기화 없음)로 계좌를 먼저 잠그고 그 계좌 객체를 `holding` 조회·`executeMarketSell`에 그대로 전달. `ExitPlanFillServiceTest`도 `AccountService` mock 추가 + 잠금 순서 검증(`account → holding → plan`)으로 갱신, 해당 테스트 클래스 7건 재실행 통과. `compileJava`·`compileTestJava`·`spotbugsMain` 통과(JAVA_HOME=ms-17.0.20). 전체 테스트·build는 오케스트레이터가 재실행.
- (#351 통합·경합 검증) plan.md 테스트 계획의 통합 시나리오를 Testcontainers로 확정: 기존 `ExitPlanGeneralPathIntegrationTest`에 PERCENT+목록 조회, 재생성 허용, 멱등 재시도 3건 추가, `ExitPlanTriggerFillIntegrationTest`에 손절 트리거 대칭 케이스 추가, 신규 `ExitPlanCancelFillConcurrencyIntegrationTest`(취소↔트리거 경합, `015`의 취소-대-체결 패턴 재사용)로 "정확히 한 번" 규칙을 검증. 교육 경로 회귀는 범위 밖(별도 이슈).
- **실제 버그 발견·수정(최소 범위)**: 신규 경합 테스트가 8회 중 3~6회 간헐 실패 — `ExitPlanFillService`/`ExitPlanCancelService`가 "잠그지 않은 소유권 조회"(`ownershipCheck.getHolding().getAccount()` 등 non-id 접근) 이후 같은 트랜잭션에서 `findByIdForUpdate`로 같은 엔티티를 재조회할 때, DB는 최신 행을 제대로 잠그지만 Hibernate 1급 캐시가 이미 세션에 있는 stale 인스턴스를 필드 갱신 없이 반환해 lost-update가 발생했다(raw JDBC로 재현 확인). `entityManager.detach(ownershipCheck)`·`detach(preloadedHolding)`로 이 두 엔티티만 선택적으로 분리한 뒤(직전 `flush()` 필수 — 안 하면 같은 트랜잭션 안의 미반영 변경을 버려 새 버그를 냄) 재조회하도록 두 서비스를 수정. `entityManager.clear()`(세션 전체 초기화)는 `@Transactional` 통합 테스트의 무관한 엔티티까지 분리해 500을 내 폐기했다. 상세는 `docs/agent-mistakes.md` 2026-08-17 행. `ExitPlanCancelServiceTest`·`ExitPlanFillServiceTest`(단위, mock `EntityManager` 추가)도 생성자 시그니처 변경에 맞춰 갱신.
- 신규 통합 테스트 8회 연속 통과, 전체 `com.finplay.api.order.*`(513건) 통과, `./gradlew build` 전체 통과(spotbugs·spotless 포함). `docs/api-routes.md`·`docs/api-contracts.md`·`docs/prd.md` §3 갱신 대상 아님(신규 엔드포인트·기능 범위 변경 없음, 기존 구현의 버그 수정 + 테스트 추가).
- (#461) 이슈 #461·047 spec TUTORIAL-CASH-ISOL-010(코멘트 "1안으로 구현해줘")에 따라 RISK-OCO-014 신설: `ExitPlanService.create`가 `validateMarketIsCrypto` 다음 단계로 `validateNotTutorialSample`을 추가해, 대상 holding의 종목이 샌드박스(`isTutorialSample()=true`)면 신규 오류 코드 409 `EXIT_PLAN_TUTORIAL_INSTRUMENT_NOT_ALLOWED`로 거부한다(엔진 `ExitPlanCreationService`가 아니라 호출부에 둬, 향후 교육 경로 재접합이 이 판정에 막히지 않게 함). `ExitPlanServiceTest`·`ExitPlanGeneralPathIntegrationTest`에 각 1건 추가. 회귀 발견·수정: 기존 `TutorialSandboxSellCashIsolationIntegrationTest`의 OCO 체결 현금격리 테스트가 `ExitPlanService.create`로 샌드박스 holding에 plan을 만들고 있어 새 차단에 걸림 — 이 테스트의 관심사는 "이미 존재하는 plan의 체결 현금 격리"이므로 생성을 `ExitPlanCreationService.create`(공용 엔진) 직접 호출로 바꿔 차단을 우회하도록 수정(legacy plan 시나리오로 재해석, 1안의 알려진 한계와 정합). `ErrorCodeTest`(소진적 개수 43·매핑·메시지 단정)도 갱신. `./gradlew --no-daemon --max-workers=1 build` 전체 통과(4541건, 0 실패/에러). `021` spec.md RISK-OCO-014·비즈니스 규칙, `047` spec.md TUTORIAL-CASH-ISOL-010(결정 완료로 갱신)·범위 제외, `docs/api-contracts.md` §021, `docs/prd.md` §3 TUTORIAL-CASH-ISOL 행(일부 완료→완료) 동기화.
- (목록·응답 계약 전환) `GET /api/exit-plans?status=` production 추가: `ExitPlanController.getMyExitPlans`(`@RequestParam(required = false) ExitPlanStatus status`), `ExitPlanService.list`(null이면 `PENDING` 기본값, 기존 `ExitPlanRepository.findByUserIdAndStatusOrderByIdDesc` 그대로 재사용), 신규 `ExitPlanListResponse`(생성·취소가 이미 쓰는 `ExitPlanResponse` 재사용, 신규 DTO 없음). 허용값 밖 status는 기존 `MethodArgumentTypeMismatchException` 전역 핸들러가 400으로 처리(신규 오류 코드 없음). `docs/api-routes.md`(계획 표 → 실제 라우트 표로 이동)·`docs/api-contracts.md`(016 "계획" 절 제거, 021 production 절에 목록 조회 절 신설) 동기화. `docs/prd.md` §3 "투자 실습 — OCO exit plan" 행·"일반 리스크관리 OCO 설계 확정" 행 갱신(목록 조회 완료 반영, 남은 것은 교육 경로 재접합뿐). `.\gradlew.bat compileJava`·`spotlessApply` 통과(JAVA_HOME=ms-17.0.20). 테스트는 작성하지 않음(tester 담당).
