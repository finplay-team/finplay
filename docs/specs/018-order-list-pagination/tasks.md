# Tasks: 주문 목록 market 필수 · 커서 페이지네이션

- [x] `OrderCursor` 신설(`TradeCursor` 그대로 이식 — `record(LocalDateTime requestedAt, Long id)`, `parse`/`encode`, `{ISO_LOCAL_DATE_TIME}_{id}` 포맷, 파싱 실패 400 `VALIDATION_ERROR`) + `OrderCursorTest` 신설(`TradeCursorTest` 이식: 정상 파싱, 구분자 없음, 날짜 파싱 실패, id 파싱 실패, 커서 없음=null)
- [x] `OrderRepositoryCustom`/`OrderRepositoryImpl` 신설(`TradeRepositoryCustom`/`TradeRepositoryImpl` 이식 — QueryDSL `QOrder`, `order.account.id.eq` + 커서 이전 조건 + `requestedAt desc, id desc` + `instrument` fetchJoin), `OrderRepository`가 `OrderRepositoryCustom`을 extends + `@DataJpaTest OrderRepositoryTest` 갱신(`TradeRepositoryTest` 대응 — 정렬·필터·커서 경계 케이스)
- [x] `OrderListResponse` 신설(`content`/`nextCursor`/`hasNext`, 항목은 기존 `OrderListItemResponse` 재사용) + `OrderService.getMyOrders(userId, market, cursor, limit)`로 시그니처 변경(`AccountService.getAccountFor`로 소유권+시장 스코프 선검증, `limit+1` fetch로 `hasNext` 판정) + `OrderServiceTest` 갱신(`TradeServiceTest` 대응)
- [ ] `OrderController.getMyOrders`에 `market`(필수)·`cursor`(선택)·`limit`(선택, 기본 20, `validateLimit`로 1~100 검증) 파라미터 추가·응답 타입을 `OrderListResponse`로 변경(`TradeController.getMyTrades` 대응) + `docs/api-routes.md`·`docs/api-contracts.md`의 `GET /api/orders` 행을 새 시그니처·응답 형태로 동기화(같은 커밋, CLAUDE.md 규칙 7) + `@WebMvcTest OrderControllerTest` 갱신(`market` 400·`limit` 범위 400·정상 200 필드 계약·기존 8필드 회귀)
- [ ] `OrderListIntegrationTest` 갱신(기존 파일 — Testcontainers): `market` 필터링, 첫 페이지→`nextCursor`로 다음 페이지 이어받기(중복·누락 없음), 마지막 페이지 `hasNext=false`·`nextCursor=null`, 타 사용자·존재하지 않는 계좌의 `market` 조회 거부
