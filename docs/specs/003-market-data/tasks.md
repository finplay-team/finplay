# Tasks: 종목과 시세

- [x] instruments 마이그레이션 + 16+12종 시드 + 엔티티·Repository + 종목 목록 API `GET /api/instruments?market=` (+ @DataJpaTest·@WebMvcTest) — 이슈 #14
- [x] 종목 단건 조회 API `GET /api/instruments/{instrumentId}` (+ @WebMvcTest) — 이슈 #15
- [x] stock_candles 마이그레이션(UNIQUE(instrument_id, trading_date, candle_time), validation_status 컬럼 없음) + 엔티티·Repository — 이슈 #16 (가격 조회 최소 스키마. KrxReplayPriceProvider가 읽어 가격 계약을 만족시키는 데 필요 — 실물 데이터 대신 테스트 픽스처로 시딩)
- [x] stock_replay_sessions 마이그레이션(UNIQUE(service_date), preparation_status는 PREPARING·READY·FAILED 3개만, resolved_at·source_trading_date·failure_reason은 nullable — prepared_at이 아닌 resolved_at 이름 사용) + 엔티티·Repository — 이슈 #16 (시장상태 OPEN/CLOSED·sourceTradingDate 계산에 필요 — 세션 값은 테스트 픽스처로 시딩, 실제 전환은 아래 Scheduler 항목에서)
- [ ] market_data_imports 마이그레이션(status는 SUCCESS·PARTIAL_SUCCESS·FAILED·SKIPPED_DUPLICATE 4개) + 엔티티·Repository — 이슈 #17 범위 아님 (KisHistoricalCandleCollector 수집 파이프라인 전용 테이블. 아래 KisHistoricalCandleCollector 항목과 함께 진행하는 별도 후속 작업 — 아직 GitHub 이슈 번호 미배정)
- [ ] StockReplaySessionScheduler (MarketDataImport·StockCandle 수집 결과 확인 → StockReplaySession 생성, source_trading_date 고정, PREPARING→READY/FAILED만 전환, StockCandle 직접 저장 안 함, 상태별 nullable 규칙을 애플리케이션 검증으로 강제 — DB CHECK 제약 없음) (+ 단위 테스트: PREPARING+resolved_at 존재/PREPARING+failure_reason 존재/READY+source_trading_date 없음/READY+resolved_at 없음/READY+failure_reason 존재는 거부, FAILED+resolved_at 없음/FAILED+failure_reason 없음은 거부, FAILED+source_trading_date NULL·존재는 둘 다 허용. Clock으로 실행시점 제어 — production 실행시각은 Decision Gate) — 이슈 #17 범위 아님 (실제 수집 결과 기반 세션 전환 — market_data_imports·KisHistoricalCandleCollector 종속, 별도 후속 작업 — 아직 GitHub 이슈 번호 미배정. #16에서는 StockReplaySession을 테스트 픽스처로 직접 시딩해 StockReplayService·가격 조회만 검증했다)
- [ ] **리네이밍 결정**: 이슈 #17 범위에서는 기존 `KrxReplayPriceProvider`/`KRX_REPLAY` 이름을 그대로 쓴다 — 이슈 #17 본문이 "재생 구현체(KrxReplayPriceProvider)에 구현합니다"라고 명시했다. `KisHistoricalReplayPriceProvider`/`KIS_HISTORICAL`로 전체 리네이밍할지는 아래 KisHistoricalCandleCollector 작업 시 별도 결정한다. KisHistoricalCandleCollector (KIS Open API 과거 1분봉 조회: 샘플 응답 데이터로 MVP 16종 확인·필수필드·형식·중복·시간범위·OHLC 대소관계·거래일 일치 등 명백한 오류만 검증 → StockCandle 저장 + MarketDataImport 이력. 전체 응답 오류는 미저장·FAILED, 특정 종목만 구조 오류면 그 종목만 미저장·PARTIAL_SUCCESS, `UNIQUE(instrument_id, trading_date, candle_time)` 기반 기본 재실행 멱등, StockReplaySession 직접 수정 안 함) (+ 단위 테스트) — production 응답 포맷 파싱과 분봉 개수 임계치는 KIS 상품 세부사항 확인 대기, 샘플 데이터로 골격만 구현 — 이슈 #17 범위 아님 (MKT-005 파이프라인 기본 동작, 별도 후속 작업 — 아직 GitHub 이슈 번호 미배정)
- [ ] **(이슈 #83 — 장기운영 방어 로직, MVP 범위 아님)** 동일 거래일 재수집 정책 (수집 전 market_data_imports에서 해당 source_trading_date의 기존 SUCCESS·PARTIAL_SUCCESS와 수집 결과 비교. 동일 데이터 재수집은 StockCandle·세션 불변 + SKIPPED_DUPLICATE 이력만 추가, 상충 데이터는 거부 + FAILED 기록, FAILED 이력만 있으면 재시도 허용) (+ 단위 테스트: 최초 SUCCESS/PARTIAL_SUCCESS/FAILED, 동일 데이터→SKIPPED_DUPLICATE·무변화, 상충 데이터→FAILED·무변화, FAILED만 있을 때 재시도 허용) — KisHistoricalCandleCollector 수집 파이프라인 종속
- [ ] **(이슈 #83 — 장기운영 방어 로직, MVP 범위 아님)** StockCandleCleanupJob (20영업일 초과 삭제, 재생 중 거래일 보존) (+ 단위 테스트) — 실물 수집으로 누적된 분봉 정리 배치
- [x] StockReplayService (StockReplaySession 읽기 전용 + Clock으로 OPEN·CLOSED 계산, 첫 분봉 시가/이후 마감 종가, 공휴일 목록, 재생세션 DB 쓰지 않음) (+ 단위 테스트) — 이슈 #16 (시장상태·현재가 계산 로직 — 가격 응답의 핵심)
- [x] StockPriceProvider 인터페이스 + KrxReplayPriceProvider(StockReplayService 위임, 공개 기본) + StockFeedConfig (STOCK_FEED_PROVIDER·SERVICE_EXPOSURE·KIS_PUBLIC_DISPLAY_APPROVED로 빈 1개 선택, PUBLIC+KIS_REALTIME+미승인은 기동 실패) (+ 단위 테스트: 허용 조합 3종 Provider 선택, 금지 조합 컨텍스트 로드 실패, KIS 키 없이 KRX_REPLAY 기동 성공) — 이슈 #16 (Provider 계약·공급자 설정은 테스트 기준의 "공급자 설정"에 해당. KisRealtimePriceProvider가 아직 없으므로 PRIVATE+KIS_REALTIME 조합은 스텁/미승인 조합 검증까지만 — 실제 KIS 배선은 아래 항목에서 별도 진행). **네이밍 참고**: 데이터 소스를 KRX→KIS로 전환하기로 결정하면서 이후 문서·이슈는 `KisHistoricalReplayPriceProvider`/`KIS_HISTORICAL` 명칭을 쓴다. 이미 병합된 이 코드(`KrxReplayPriceProvider`/`KRX_REPLAY`)의 리네이밍 여부는 이슈 #17 범위에서는 이름을 유지하는 것으로 확정했다 — 전체 리네이밍 결정은 아래 KisHistoricalCandleCollector 항목(이슈 #17 범위 아님)에서 별도로 진행한다.
- [ ] **(이슈 #82 — KIS 실시간 틱 집계, MVP 범위 아님)** KisRealtimePriceProvider + FakeKisRealtimePriceProvider (KIS 국내주식 WebSocket 체결 틱 수신·재연결·연결상태, 끊김 시 가격 무효·재연결 후 복귀. 키는 KIS_REALTIME일 때만 바인딩) (+ 단위 테스트는 Fake로) — 실제 KIS 연결은 외부 스모크로 구분 보고
- [ ] **(이슈 #82 — KIS 실시간 틱 집계, MVP 범위 아님)** KisTickAggregator (체결 틱 → 1분 OHLCV 서버 집계, 결과 모델이 캔들 API·KisHistoricalReplayPriceProvider와 동일) (+ 단위 테스트: open/high/low/close/volume 산출, 분 경계 전환) — KisRealtimePriceProvider 종속
- [x] PriceStore (Redis 코인 최신 시세·과거 틱 무시, stale 10초) + BithumbFeedClient 인터페이스·Fake 구현 (+ 단위 테스트) — 이슈 #16 (코인 가격 장애·stale 시나리오 테스트에 필요, Fake 구현만 사용)
- [x] PriceQueryService 유효성 판정 (주식은 주입된 StockPriceProvider만 사용 — 구현체를 알지 못함) (+ 단위 테스트: 두 Provider로 같은 시나리오를 돌려 동일 계약 확인) — 이슈 #16 (핵심 산출물 — 가격·시장상태 응답 계약)
- [ ] 실제 BithumbFeedClient (WebSocket 수신·재연결·연결상태 기록) — 이슈 #16 범위 아님 (실제 빗썸 연동, 외부 스모크로 별도 진행)
- [x] 가격 API `GET /api/instruments/{instrumentId}/price` (Controller/Service/Repository 책임 분리, 공통 오류 응답·인증·소유권 규칙, 가격 없으면 409 PRICE_UNAVAILABLE) (+ @WebMvcTest: 401, 404, 409, 200 계약 — price·sourceTime·status·sourceTradingDate 필드) — 이슈 #16 (핵심 산출물)
- [x] 캔들 API `GET /api/instruments/{instrumentId}/candles?interval=1m&from=&to=` (StockPriceProvider에 1분봉 조회 메서드 추가 + 재생 구현체 KrxReplayPriceProvider에 구현, CandleInterval 검증 — 1m만 허용·그 외 400 VALIDATION_ERROR, CandleResponse DTO + StockCandle 매핑, StockCandleRepository에 시각 범위 조회 쿼리 추가, from·to는 ISO-8601·from > to면 400·둘 다 생략 시 재생 중인 거래일 전체, 아직 마감하지 않은 분봉은 노출하지 않음 — StockReplayService의 공개 컷오프 로직 재사용, 재생세션 미준비·미공개 분봉이면 200 빈 배열(가격 API의 409 PRICE_UNAVAILABLE과 구분), 코인 instrumentId로 요청하면 400 VALIDATION_ERROR, Controller/Service/Repository 책임 분리) (+ 단위 테스트: CandleInterval·from/to 검증·공개 컷오프 로직, @DataJpaTest: StockCandleRepository 시각 범위 조회, @WebMvcTest: 401·404·400(×3)·200 계약) — 이슈 #17. **주의**: 여기서 넣은 "코인이면 400 VALIDATION_ERROR" 거부는 아래 코인 차트 항목(이슈 #20)에서 제거된다
- [x] SSE 공통 골격 — `@EnableScheduling` + `spring.mvc.async.request-timeout` 설정, SSE 이벤트 DTO 3종(`snapshot`/`price`/`status`, `sourceTime`·`emittedAt`·`sourceTradingDate` 직렬화 — 코인 이벤트는 `sourceTradingDate` 미포함), `SseEmitterRegistry`(market별 emitter 집합 관리, 등록 시 `retry: 3000` 1회 전송, `onCompletion`·`onTimeout`·`onError`에서 해당 emitter 제거, `@Scheduled`로 20초마다 등록된 모든 emitter에 heartbeat 주석(`: heartbeat\n\n`) 전송), `PriceQueryService`에 예외를 던지지 않고 `PriceQuoteDto`(status=AVAILABLE/UNAVAILABLE)를 반환하는 조회 경로 추가(기존 `getPrice`는 이 경로를 감싸 PRICE_UNAVAILABLE이면 예외를 던져 409 계약 유지, 동작 변경 없음) (+ 단위 테스트: `SseEmitterRegistry` 등록·제거·콜백에서 정리되는지, 이벤트 DTO 직렬화(sourceTime/emittedAt/sourceTradingDate 필드 구분·코인은 sourceTradingDate 없음), 기존 `getPrice` 409 계약 회귀 없음) — 이슈 #18 (컨트롤러가 아직 없는 상태에서도 `./gradlew build` 통과, `/api/stocks/stream`·`/api/cryptos/stream` 엔드포인트는 제외 범위 — 아래 항목에서 이슈 #19·#98으로 별도 진행)
- [ ] `/api/stocks/stream` SSE (`StockPriceSseController`, fetch+Bearer 인증, `SseEmitterRegistry.register(STOCK)`로 emitter 획득, 구독 직후 주식 16종 전체를 `snapshot` 1건으로 전송 — 가격 없는 종목도 price/sourceTime=null·status=UNAVAILABLE로 포함, id는 `price` 이벤트에만 부여(`snapshot`/`status`는 id 없음), 매분 새로 공개된 가격을 `price` 이벤트로 push, 장 마감 후에도 마지막 유효가격 유지, marketStatus(OPEN/CLOSED)와 종목별 status(AVAILABLE/UNAVAILABLE) 분리) (+ @WebMvcTest: 401, price에만 id 존재, snapshot 전체종목 포함(가격없음 포함), 장마감 후 마지막 유효가격 유지, 재접속 시 snapshot 재전송, emitter 정리) — 이슈 #19 (이번 범위 아님, 이슈 #18 병합 후 진행)
- [ ] `/api/cryptos/stream` SSE (`CryptoPriceSseController`, fetch+Bearer 인증, `SseEmitterRegistry.register(CRYPTO)`로 emitter 획득, 구독 직후 코인 12종 전체를 `snapshot` 1건으로 전송, 빗썸 수신 틱마다 `price` 이벤트, 코인 stale·연결 끊김 시 종목 status만 UNAVAILABLE로 전환하고 marketStatus는 계속 OPEN 유지, `sourceTradingDate` 필드 미포함) (+ @WebMvcTest: 401, snapshot 전체종목 포함, 코인 stale 시 marketStatus OPEN 유지, emitter 정리) — 이슈 #98 (이번 범위 아님, 이슈 #18 병합 후 진행)
- [ ] 코인 1분봉 차트 연동 (MKT-008) — `CryptoCandleProvider` 인터페이스 + `BithumbRestCandleProvider`(빗썸 공개 캔들 REST `GET /v1/candles/minutes/1`, 심볼→`KRW-{symbol}` 변환, `from`·`to`→`to`+`count` 변환·최대 200개, 빗썸 내림차순→시각 오름차순 반전, **진행 중인 분봉 포함**(주식과 반대 — 코인은 실시간이라 가릴 대상이 없다. StockReplayService의 공개 컷오프 로직을 재사용하지 않는다), `trade_price`→`close`·`candle_acc_trade_volume`→`volume` 매핑, 연결·읽기 타임아웃 설정) + `FakeCryptoCandleProvider`(자동 테스트용) + `CandleQueryService` 시장 분기(CRYPTO는 CryptoCandleProvider 위임, 기존 "코인이면 400 VALIDATION_ERROR" 거부 제거) + `CandleResponse.volume`을 long→BigDecimal로 확대 + `ErrorCode.MARKET_DATA_PROVIDER_ERROR`(502) 추가. 저장·캐시 없음 — 마이그레이션·Redis 키 추가 금지 (+ 단위 테스트: 정렬 반전, 진행 중 분봉 포함(주식과 반대라는 것을 고정), 필드 매핑(거래대금과 수량 혼동 없음), 심볼 변환, from/to 조합별 count 산출·200 캡, 소수 volume 미절단, 타임아웃·비정상 상태코드·파싱 불가 시 502 발생(빈 배열 아님), Redis·MySQL 미기록, 주식 캔들 회귀(volume 확대 후 값·정렬·공개 컷오프·200 빈 배열 유지), 코인 캔들 경로와 현재가 경로의 독립성 / @WebMvcTest: 401, 코인 200, interval 오류 400, 빗썸 실패 502) + docs/api-routes.md·docs/api-contracts.md 같은 커밋 갱신 — 이슈 #20 (실제 빗썸 REST 조회는 외부 스모크로 구분 보고)
- [x] 캔들 API 통합 테스트 (픽스처 분봉 + 고정 Clock으로 장중·마감 후 시나리오 검증) + docs/api-routes.md·docs/api-contracts.md 갱신 — 이슈 #17
- [ ] 파이프라인 통합 테스트 (샘플 KIS 응답 데이터 수집·세션 READY·재생, 서버 재시작 시 원본 거래일 유지, DB에 OPEN·CLOSED 미저장 확인, Fake Feed 정상·끊김·재연결, 개장·첫분봉·장중·마감, 화면 가격과 모의 체결가격이 같은 Provider에서 나오는지 확인) — 이슈 #17 범위 아님 (실물 KIS Open API 수집·재생 파이프라인 통합 테스트 — market_data_imports·StockReplaySessionScheduler·KisHistoricalCandleCollector 종속, 별도 후속 작업 — 아직 GitHub 이슈 번호 미배정. #16의 검증은 위 in-scope 항목들의 단위·@WebMvcTest로 충분하며 별도 통합 테스트 항목을 두지 않았다)
- [ ] **(이슈 #83 — 장기운영 방어 로직, MVP 범위 아님)** 동일 거래일 다른 수집 결과 재수집 거부 시 기존 READY 세션·StockCandle 불변 확인 통합 테스트
