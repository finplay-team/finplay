# Tasks: 전체 랭킹 조회 (RANK-001)

- [ ] `RealizedPnlUpdatedEvent`(신규, `account/event`) + `RankingStore`(신규, `ranking/store` — `addScoreWithRetry`/`topN`/`countStrictlyGreater`, key `ranking:{market}` 조립을 이 클래스에서만 함) + `RankingEventListener`(신규, `ranking/listener` — `@TransactionalEventListener(AFTER_COMMIT)`, 메서드 본문 전체를 try/catch로 감싸 `RankingService.refreshScore` 호출 중 어떤 예외(DB 재조회 실패 포함)가 나도 밖으로 전파하지 않음) + `OrderExecutionService.createSellOrder()`에 `eventPublisher.publishEvent(new RealizedPnlUpdatedEvent(account.getId()))` 추가 + `RankingStoreTest`(Redis mock, 재시도 소진 시 예외 미전파 검증) + `RankingEventListenerTest`(신규, `RankingService` mock이 `DataAccessException`을 던지도록 stub해도 리스너가 예외를 전파하지 않는지 검증) 신설
- [ ] `RankingService`(신규, `ranking/service` — `refreshScore(accountId)`, `getRankings(market, limit)`: limit 클램핑·`AccountRepository.findAllByIdInFetchUser` 배치조회·`countStrictlyGreater` 캐시 기반 공동순위 계산·userId 오름차순 tie-break) + `AccountRepository`에 `findAllByIdInFetchUser` 메서드 추가 + `RankingServiceTest`(mock 기반, 클램핑·공동순위·tie-break 케이스) 신설
- [ ] `RankingListResponse`/`RankingListItemResponse`(신규, `ranking/dto/response`) + `RankingController`(신규, `ranking/controller` — `GET /api/rankings`, `market` 필수) + `docs/api-routes.md`·`docs/api-contracts.md`에 `GET /api/rankings` 계약 추가(같은 커밋) + `RankingControllerTest`(`@WebMvcTest` — market 400, limit 클램핑 200, 필드 계약, 401)
- [ ] `RankingIntegrationTest`(신규, Testcontainers MySQL+Redis, 기존 `TestcontainersConfiguration` 재사용): 매도 체결→커밋→`GET /api/rankings` 반영, 이벤트 순서 역전 시 최종수렴, 커밋 전 실패 시 Redis 미반영, Redis 장애 시에도 매도 체결 정상 성공, 매도 이력 없는 계좌 제외, 동점자 공동순위(공동 1위 2명→다음 3위) 실데이터 검증
