# Tasks: 전체 랭킹 조회 (RANK-001)

- [x] `RealizedPnlUpdatedEvent`(신규, `account/event`) + `RankingStore`(신규, `ranking/store` — `addScoreWithRetry`/`topN`/`countStrictlyGreater`, key `ranking:{market}` 조립을 이 클래스에서만 함) + `RankingEventListener`(신규, `ranking/listener` — `@TransactionalEventListener(AFTER_COMMIT)`, 메서드 본문 전체를 try/catch로 감싸 `RankingService.refreshScore` 호출 중 어떤 예외(DB 재조회 실패 포함)가 나도 밖으로 전파하지 않음) + `OrderExecutionService.createSellOrder()`에 `eventPublisher.publishEvent(new RealizedPnlUpdatedEvent(account.getId()))` 추가 + `RankingStoreTest`(Redis mock, 재시도 소진 시 예외 미전파 검증) + `RankingEventListenerTest`(신규, `RankingService` mock이 `DataAccessException`을 던지도록 stub해도 리스너가 예외를 전파하지 않는지 검증) 신설
- [x] `RankingService`(신규, `ranking/service` — `refreshScore(accountId)`, `getRankings(market, limit)`: limit 클램핑·`AccountRepository.findAllByIdInFetchUser` 배치조회·`countStrictlyGreater` 캐시 기반 공동순위 계산·userId 오름차순 tie-break) + `AccountRepository`에 `findAllByIdInFetchUser` 메서드 추가 + `RankingServiceTest`(mock 기반, 클램핑·공동순위·tie-break 케이스) 신설
- [x] `RankingListResponse`/`RankingListItemResponse`(신규, `ranking/dto/response`) + `RankingController`(신규, `ranking/controller` — `GET /api/rankings`, `market` 필수) + `docs/api-routes.md`·`docs/api-contracts.md`에 `GET /api/rankings` 계약 추가(같은 커밋) + `RankingControllerTest`(`@WebMvcTest` — market 400, limit 클램핑 200, 필드 계약, 401)
- [x] `RankingIntegrationTest`(신규, Testcontainers MySQL+Redis, 기존 `TestcontainersConfiguration` 재사용): 매도 체결→커밋→`GET /api/rankings` 반영, 이벤트 순서 역전 시 최종수렴, 커밋 전 실패 시 Redis 미반영, Redis 장애 시에도 매도 체결 정상 성공, 매도 이력 없는 계좌 제외, 동점자 공동순위(공동 1위 2명→다음 3위) 실데이터 검증

## RANK-002 내 랭킹 조회 (이슈 #233)

- [x] `RankingStore`에 `score(Market, Long accountId)`(신규 — `ZSCORE` 단건 조회, ZSET에 member 없으면 `null` 반환) 추가 + `RankingService.getMyRanking(userId, market)`(신규 — `@Transactional(readOnly=true)`, `AccountService.getAccountFor`로 계좌 조회 후 `RankingStore.score`로 매도 이력 유무 판정, 있으면 기존 `countStrictlyGreater` 재사용해 보정 순위 계산) 신설 + `RankingStoreTest` 확장(`score` 존재/부재 케이스) + `RankingServiceTest` 확장(매도이력 없음→`rank` null·닉네임/실현손익 정상값, 있음→보정 순위 매핑)
- [x] `MyRankingResponse`(신규, `ranking/dto/response` — `record(String market, Integer rank, String nickname, long realizedPnl)`) + `RankingController`에 `GET /api/rankings/me`(신규, `@AuthenticationPrincipal`로 본인 userId만 사용, `market` 필수) 추가 + `docs/api-routes.md`·`docs/api-contracts.md`에 `GET /api/rankings/me` 계약 추가(같은 커밋) + `docs/prd.md` §3 구현 현황의 "랭킹 — 내 랭킹 조회(RANK-002)" 행 갱신(같은 커밋, 근거는 이 PR 번호) + `RankingControllerTest` 확장(`market` 누락 400, 인증 없음 401, 매도이력없음 `rank: null` 200, 정상 200 필드 계약)

## 랭킹 재구성 절차 (이슈 #279)

> 항목 1개 = 커밋 1개. 설계 근거는 `plan.md` "랭킹 재구성 설계 (이슈 #279)". 스키마 변경 없음(Flyway 마이그레이션 없음).

- [x] **재구성이 읽고 쓸 창구 만들기** — `TradeRepository`에 매도 이력 조회 3종 추가(`findDistinctAccountIdsBySideAndMarket` `@Query` + `existsByAccountIdAndSide`·`existsBySideAndAccountMarket` 파생 쿼리, `realized_pnl != 0` 기준 금지) + `TradeService`에 위임 메서드 3종 추가(`getSoldAccountIds`/`hasSellHistory`/`hasAnySellHistory` — `ranking` 도메인이 `TradeRepository`를 직접 주입하지 않게 하는 ADR-0002 경유점) + `AccountService.getAccountsByIds`(신규, `JpaRepository.findAllById` 위임 — `AccountRepository` 신규 메서드 없음) + `RankingStore.replaceAll(market, entries)`(신규 — 임시 키 `ranking:{market}:rebuild` `DEL` → 청크 `ZADD` → `RENAME` 순서, entries 0건이면 `RENAME` 대신 본 키 `DEL`, 전체 try/catch로 예외 미전파) + `TradeRepositoryTest` 확장(`@DataJpaTest` — 중복 제거·market 한정·매수만 있는 계좌 제외, exists 경계) + `RankingStoreTest` 확장(`InOrder`로 DEL→ZADD→RENAME 순서, 0건 경계, 청크 분할 시 RENAME 1회, Redis 예외 미전파)
- [x] **재구성 실행 경로 만들기** — `RankingRebuildService`(신규, `ranking/service` — `rebuild(Market)`: `getSoldAccountIds` → `getAccountsByIds` → `RankingEntryDto(id, realizedPnl)` 매핑 → `replaceAll`, `rebuildAll()`: `Market.values()` 루프에서 시장별 try/catch로 한 시장 실패가 다른 시장을 막지 않게 함, 클래스에 `@Transactional` 붙이지 않음) + 기동 훅 `@EventListener(ApplicationReadyEvent.class)` + 주기 배치 `@Scheduled(cron = "${ranking.rebuild.cron}", zone = "Asia/Seoul")` + `application.yml`에 `ranking.rebuild.cron: "0 20 4 * * *"`(매일 04:20 KST, 기존 크론과 겹치지 않는 근거를 주석으로) 추가 + `RankingRebuildServiceTest`(신규, mock — 위임 순서·score 출처 `ArgumentCaptor` 검증, **매도 이력 있고 `realized_pnl = 0`인 계좌 포함 회귀 테스트**, 대상 0건도 `replaceAll` 호출, 시장별 예외 격리, 두 트리거가 같은 경로)
- [x] **유실 상태 노출(`status`) 추가** — `RankingStatus`(신규 enum, `ranking/domain` — `READY`/`REBUILDING`) + `RankingListResponse`·`MyRankingResponse`에 `status` 필드 추가(기존 필드 유지, 하위 호환) + `RankingService`에 판정 로직 추가(목록: `topN` window가 비었을 때만 `tradeService.hasAnySellHistory(market)` 조회 — 정상 경로 추가 왕복 0, 유령 필터링으로 content만 빈 경우는 `READY` 유지 / 내 랭킹: `score == null && tradeService.hasSellHistory(accountId)`로 부분 유실까지 감지) + `RankingService`에 `TradeService` 의존 추가(순환 참조 아님을 주석으로) + `docs/api-contracts.md` 랭킹 절 갱신(같은 커밋, CLAUDE.md 규칙 7 — 두 응답에 `status` 추가, `rank: null` + `READY`(매도 이력 없음)와 `rank: null` + `REBUILDING`(집계 준비 중)의 의미 차이 명시) + `docs/api-routes.md`는 Method·URL·요약 불변이라 변경 없음을 확인만 + `RankingServiceTest` 확장(목록·내 랭킹 각 3케이스, 정상 경로에서 `TradeService` 미호출 검증) + `RankingControllerTest` 확장(`@WebMvcTest` — `status` JSON 필드 계약, 유실 상태에서도 200, 기존 필드 유지)
- [ ] **실데이터 검증과 상위 문서 정리** — `RankingRebuildIntegrationTest`(신규, Testcontainers MySQL+Redis, 기존 `TestcontainersConfiguration` 재사용·`@ServiceConnection` 금지): ZSET `DEL` 후 재구성 시 유실 전과 동일한 순위·금액, `realizedPnl`이 `accounts.realized_pnl`과 일치, `realized_pnl = 0`인 매도 이력 계좌 포함, 매도 이력 없는 계좌 제외, 대상 0건 시장의 `RENAME` 경계(실 Redis에서만 재현되는 `ERR no such key`), 재구성 전 두 엔드포인트가 200 + `REBUILDING`, 재구성 전후 원장(`orders`·`trades`·`accounts`·보유·잔액) 스냅샷 불변 + `docs/prd.md` 본문 2곳 갱신(RANK-001 절의 "재구성 트리거·절차의 세부 구현은 착수 시 확정한다"·"여전히 Decision Gate다" → 확정 내용으로, §6 "Redis 키 책임"의 랭킹 항목(1048행 부근) → 트리거·대상 판정 기준·임시 키 교체·`status` 노출 덧붙임). **§3 구현 현황 표는 갱신 대상 아님** — RANK-001·RANK-002 행 판정이 "완료"에서 바뀌지 않는다(근거는 `spec.md` "요구사항 ID를 새로 부여하지 않는 이유") + `./gradlew build` 통과 확인
