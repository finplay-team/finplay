# Run Log: 047-tutorial-sandbox-cash-isolation

## AI 로그 (에이전트 참조용)
| 시각 | 에이전트 | 실행 명령 | 근거 |
|---|---|---|---|
| 18:52 | implementer | `./gradlew test --tests TutorialAccountRepositoryTest` | plan.md 데이터 모델·마이그레이션 설계, ADR-0004 |
| 19:00 | implementer | `./gradlew compileJava` | plan.md TutorialAccountService API 표, 설계 판단 — 계좌 생성 시점 |
| 19:37 | implementer | `./gradlew compileJava` | plan.md "호출부 변경 지점" 1·3·4번, spec TUTORIAL-CASH-ISOL-002·005 |
| 20:00 | implementer | `./gradlew compileJava` / `./gradlew compileTestJava` | plan.md "호출부 변경 지점" 2번(`finalizeSellRealizedPnl`)·1번(`createSellOrder`), spec TUTORIAL-CASH-ISOL-003 |
| 20:16 | implementer | `./gradlew compileJava` | plan.md "호출부 변경 지점" 5번(재시작 리셋), spec TUTORIAL-CASH-ISOL-006 |
| 20:41 | implementer | `./gradlew compileJava` | plan.md "요약 응답 — totalValue 공식 원복", spec TUTORIAL-CASH-ISOL-007 |
| 21:12 | tester | `./gradlew test --tests TutorialCashIsolationFullCycleIntegrationTest` | plan.md 178~190줄 "이슈 #450 재현 시나리오의 역-검증", tasks.md 8번 |
| 21:12 | tester | `./gradlew test --tests "com.finplay.api.order.*" --tests "com.finplay.api.portfolio.*" --tests "com.finplay.api.education.*" --tests "com.finplay.api.account.*"` | tasks.md 8번 완료조건 3(관련 도메인 회귀 확인) |
| 21:19 | tester | `./gradlew test --tests CryptoLimitPracticeFlowRestartIntegrationTest --tests LegacyPracticeCompletionAttemptCompatibilityIntegrationTest --tests PracticeAttemptOrderQueryIntegrationTest --tests PracticeHoldingObservationIntegrationTest --tests PracticeLimitOrderTickIntegrationTest` | 위 회귀 실행에서 나온 5건 실패의 원인 분리(테스트 정리 누락 3건 vs 구현 버그 2건) 재확인 |
| 21:20 | tester | `./gradlew test --tests "com.finplay.api.order.*" --tests "com.finplay.api.portfolio.*" --tests "com.finplay.api.education.*" --tests "com.finplay.api.account.*"` | 테스트 정리 누락 3건 수정 후 전체 도메인 재확인(1107건 중 2건만 잔존 — 구현 버그, src/main 미수정) |
| 21:48 | implementer | `./gradlew test --tests "com.finplay.api.order.*" --tests "com.finplay.api.portfolio.*" --tests "com.finplay.api.education.*" --tests "com.finplay.api.account.*"` | tester가 남긴 구현 버그(위 행), 사용자 승인 후 `PracticeLimitOrderCreationService.createSessionBuyOrder`에 isTutorialSample 분기 추가 |
| 21:53 | implementer | (문서 갱신, 테스트 실행 없음) | CLAUDE.md 규칙 10, tasks.md 9번 |

## 모니터링 (사람용 요약)
- 18:52 — TutorialAccount 엔티티·Repository·V46 마이그레이션·TUTORIAL_INSUFFICIENT_CASH 추가, DataJpaTest 6건 통과.
- 19:00 — TutorialAccountService(get-or-create·reset) 추가, ensureAttempt에 연결. compileJava 통과, 기존 PracticeAttemptServiceTest는 생성자 인자 불일치로 compileTestJava 깨짐(tester 몫).
- 19:37 — 매수 경로 전환(OrderExecutionService.createBuyOrder, PracticeLimitOrderCreationService.createSessionBuyOrder, LimitOrderFillService.fillBuy)을 샌드박스면 튜토리얼 계좌로 리다이렉트하도록 수정, TUTORIAL_INSUFFICIENT_CASH 분기 추가. compileJava 통과, 생성자 인자 추가로 OrderExecutionServiceTest·PracticeLimitOrderCreationServiceTest·LimitOrderFillServiceTest의 compileTestJava가 깨짐(tester 몫).
- 19:37 — 오케스트레이터 지시로 LimitOrderCreationService(일반 코인 지정가 생성, POST /api/orders/limit)도 범위에 포함. 조사 결과 "샌드박스 종목 생성 자체를 거부"는 오판임을 확인(spec.md TUTORIAL-CASH-ISOL-002가 이 경로를 명시적으로 포함하고, PracticeAttemptOrderAttributionService.lockForOrder는 isTutorialSample()이 아니면 항상 empty를 반환하므로 이 경로의 attempt 귀속 매수는 전부 샌드박스 종목 — 이미 통과 중인 기존 테스트 2건도 이를 검증함) — 대신 다른 3개 파일과 동일한 "튜토리얼 계좌로 리다이렉트" 방식으로 구현하고 근거를 보고에 남김. LimitOrderCreationServiceTest에 TUTORIAL_INSUFFICIENT_CASH·튜토리얼 계좌 예약 검증 테스트 2건 추가, 기존 tutorialSample 테스트 갱신. compileJava·LimitOrderCreationServiceTest 포함 compileTestJava 통과(기존 3개 파일의 compileTestJava 실패는 그대로 tester 몫). LimitOrderModifyService·LimitOrderCancelService에 동일 카테고리 미반영 갭 발견 — 후속 이슈로 분리(spawn_task).
- 19:37 — 오케스트레이터 지시로 LimitOrderModifyService.modifyOrder·LimitOrderCancelService.cancelOrder에도 동일 리다이렉트 패턴(isTutorialSample()이면 TutorialAccountService로 조회한 튜토리얼 계좌에서 releaseReservedCash/reserveCash) 적용. 두 서비스 모두 Clock 의존성이 없어 새로 추가(LocalDateTime.now(clock)로 튜토리얼 계좌 조회 시각 확보). 기존 LimitOrderModifyServiceTest·LimitOrderCancelServiceTest는 생성자 인자(TutorialAccountService, Clock) 추가로 컴파일이 깨져 최소 mock 추가로 직접 복구(신규 로직 검증 테스트는 미작성, tester 몫). compileJava 통과, compileTestJava는 이전과 동일하게 3개 파일(OrderExecutionServiceTest·PracticeLimitOrderCreationServiceTest·LimitOrderFillServiceTest)만 실패.
- 19:37 | implementer | `./gradlew test --tests com.finplay.api.order.service.OrderExecutionServiceTest` | tester가 찾은 회귀 리포트(호출 순서 계약 위반) — tester가 이미 OrderExecutionServiceTest 생성자를 TutorialAccountService 인자로 갱신해 컴파일은 통과하던 상태였고, createBuyOrder에서 `userQueryService.getUser(userId)` 호출을 현금 검증(튜토리얼/실제 분기 전체) 이후·`createOrder` 호출 직전으로 이동해 "실패 시 무흔적" 순서를 복원. `./gradlew test --tests OrderExecutionServiceTest` 33건 전부 통과(failures=0, errors=0) — 회귀로 지목된 createOrderThrowsInsufficientCashWhenCashBalanceBelowAmountPlusFee·createOrderThrowsInsufficientCashWhenAvailableCashBelowAmountPlusFeeEvenIfCashBalanceSuffices 포함.
- 20:00 — 매도 경로 전환. `PortfolioSellService.finalizeSellRealizedPnl`에 TutorialAccountService를 주입하고 분기 추가(샌드박스면 실제 Account.addCash를 건너뛰고 튜토리얼 계좌 addCash+addRealizedPnl, trade.realizedPnl은 종목 종류와 무관하게 항상 채움) — 시그니처에 `LocalDateTime now` 파라미터 추가. 이 메서드를 호출하는 3곳(LimitOrderFillService.fillSell, PracticeRunRestartOrderService.createCompensatingSell, ExitPlanFillService.executeMarketSell) 모두 이미 `now`(또는 `command.restartedAt()`)를 갖고 있어 호출부 인자 추가만으로 반영됨을 코드 확인으로 검증 — 세 곳 다 로직 변경 없음. `OrderExecutionService.createSellOrder`는 finalizeSellRealizedPnl을 쓰지 않는 별도 인라인 구현이라(기존 설계 노트) 동일 분기를 인라인으로 추가. compileJava 통과, compileTestJava는 시그니처 변경으로 LimitOrderFillServiceTest·PracticeRunRestartOrderServiceTest·PortfolioSellServiceTest(생성자 인자도 추가) 3개 파일이 깨짐(tester 몫) — 기존 3개 파일(OrderExecutionServiceTest·PracticeLimitOrderCreationServiceTest·LimitOrderFillServiceTest)의 실패는 그대로.
- 20:16 — `PracticeRunRestartOrderService.cleanupCurrentRun`에 TutorialAccountService 주입, 모든 성공 경로(주문 미선택·순체결수량 0·보상매도 완료) 끝에서 `resetForUpdate` 호출. `createCompensatingSell`(직전 항목에서 튜토리얼 계좌 현금·realizedPnl을 증가시키도록 완료됨) 뒤에 리셋을 걸어, 그 보상매도 증가분까지 포함해 항상 절대값(1000만원/0/0)으로 덮어쓰도록 순서를 확정 — plan.md "트랜잭션 끝에" 문구와 절대값 리셋(reset()) 설계상 이 순서가 유일하게 맞음(먼저 리셋하면 뒤이은 보상매도가 잔고를 다시 불려 무의미해짐). compileJava 통과.
- 20:41 — `addSandboxCashAdjustment(...)` 호출부 5곳(OrderExecutionService.createBuyOrder·createSellOrder, PortfolioSellService.finalizeSellRealizedPnl, LimitOrderFillService.fillBuy, PracticeHoldingReflectionService.payTutorialCompletionReward) 전부 제거, 자리에 폐지 근거 주석만 남김. `AccountService.getAccountSummary`의 `totalValue` 공식을 `cashBalance + holdingsValue - account.getSandboxCashAdjustment()`에서 `cashBalance + holdingsValue`로 원복(033 이전 공식) — `PortfolioService`는 이 값을 그대로 합산하므로 별도 수정 불필요(코드 확인으로 검증). `Account.sandboxCashAdjustment` 필드·`addSandboxCashAdjustment()` 메서드 자체는 spec "범위 제외"(물리적 DROP 아님)에 따라 존치, 더 이상 호출되지 않음. compileJava 통과. 테스트 파일 11개(호출부 5개 서비스의 단위 테스트 + AccountSummaryIntegrationTest·AccountServiceTest·SandboxCashAdjustmentBackfillMigrationTest 등)가 여전히 옛 sandboxCashAdjustment 계약을 검증하고 있어 tester 단계에서 갱신 필요.
- 21:04~21:22 (tester, tasks.md 8번) — `docs/specs/047-tutorial-sandbox-cash-isolation/{spec,plan,tasks,run-log}.md`·`docs/context-router.md`·`docs/adr/0003-testing-strategy.md`를 읽고, 기존에 이미 커밋된 047 통합 테스트(`TutorialSandboxSellCashIsolationIntegrationTest`·`TutorialAccountBackfillMigrationTest`·`PracticeAttemptRestartIntegrationTest` 등)가 plan.md 178~190줄의 시나리오 대부분을 이미 커버하고 있음을 확인했다 — 마이그레이션 멱등성은 `TutorialAccountBackfillMigrationTest`(@DataJpaTest, tasks.md 1번)가 이미 검증 중이라 중복 작성하지 않았다. 남은 것은 "이슈 #450 재현 시나리오의 역-검증" 하나였고, 기존 테스트들과 달리 Order/Trade/Holding을 직접 만들지 않고 `PracticeAttemptService`(진입·종목선택)·`OrderService`(시장가 매수·매도)·`PracticeHoldingObservationService`(관찰)·`PracticeAttemptRestartService`(재시작) 네 실제 서비스를 이슈 #450 재현 순서 그대로 3회 반복 호출하는 `TutorialCashIsolationFullCycleIntegrationTest`(education/marketpractice/service 패키지, `PracticeAttemptRestartIntegrationTest`와 동일 위치)를 새로 작성했다 — 매 사이클 매수 2단위·매도 1단위(부분)로 두어 재시작마다 `PracticeRunRestartOrderService.createCompensatingSell`(보상매도) 경로까지 함께 거치도록 설계했다. `./gradlew test --tests TutorialCashIsolationFullCycleIntegrationTest`는 첫 실행에서 바로 통과(1건, 0 실패).
- 21:12~21:19 — tasks.md 8번 완료조건 3(관련 도메인 회귀 확인)에 따라 `order`·`portfolio`·`education`·`account` 패키지 전체(1108건)를 함께 돌렸더니 5건이 실패했다. 개별 재실행으로 원인을 분리한 결과 3건은 **테스트 파일 자체의 결함**이었다 — (1) `CryptoLimitPracticeFlowRestartIntegrationTest.cleanUpCommittedFixtures`·(2) `PracticeAttemptOrderQueryIntegrationTest.cleanUp`이 047 이전에 작성된 정리 루프라 `tutorial_accounts` 삭제가 빠져 있었고, 047 이후 이 두 테스트가 거치는 흐름(coin-practice 세션 매수·attempt 진입/재시작)이 항상 튜토리얼 계좌를 get-or-create하면서 남긴 행 때문에 `DELETE FROM users`가 `fk_tutorial_accounts_user` 위반으로 깨졌다(두 테스트 모두 본문 assertion은 전부 통과, `@AfterEach`/`@AfterAll` 정리 단계에서만 실패 — 정리 루프에 `DELETE FROM tutorial_accounts WHERE user_id = ?`를 추가해 해결). (3) `LegacyPracticeCompletionAttemptCompatibilityIntegrationTest`는 `ensureAttempt`(진입) 응답과 `getProgress`가 감싸는 attempt 응답을 완전 동일 비교(`isEqualTo`)하고 있었는데, 047 tasks.md 7번이 `PracticeAttemptResponse`에 추가한 `tutorialCashBalance`·`tutorialAvailableCash`·`tutorialRealizedPnl` 세 필드는 설계상(`PracticeAttemptResponse.from(attempt, snapshot)` 2-인자 오버로드 주석, TUTORIAL-CASH-ISOL-011 범위는 진입·재시작 응답 한정) 진입 응답에만 실값이 실리고 진행 조회 응답에는 항상 0이 채워진다 — 필드 추가 이전에는 우연히 항상 같았을 뿐인 낡은 완전 동일성 비교였다. `usingRecursiveComparison().ignoringFields(...)`로 그 세 필드만 제외하도록 고쳤다. 세 건 모두 테스트 코드(src/test)만 수정했고 production 코드는 건드리지 않았다.
- 21:19~21:20 — 나머지 2건(`PracticeHoldingObservationIntegrationTest.observationUsesPracticeSessionCurrentPriceWhenBuyTradeIsSessionScoped`, `PracticeLimitOrderTickIntegrationTest.tickAdvancesFillOnlySameSessionOrdersAndCancelUnfilledOrdersExactlyAtLastTick`)는 테스트 버그가 아니라 **구현 버그**로 확인돼 src/main은 수정하지 않고 그대로 남겼다 — `PracticeLimitOrderCreationService.createSessionBuyOrder`(85~91줄)는 "이 엔드포인트는 항상 튜토리얼 전용"이라는 전제로 `instrument.isTutorialSample()` 분기 없이 무조건 튜토리얼 계좌에 현금을 예약하는데, 그 주문을 체결·취소하는 `LimitOrderFillService.fillBuy`·`LimitOrderCancelService.cancelOrder`는 정확히 `instrument.isTutorialSample()`로만 튜토리얼/실제 계좌를 분기한다. 두 테스트 모두 이 세션 매수 경로에 `isTutorialSample()=false`인 실제 종목(BTC·ETH)을 쓰는 "030 코인 연습" 시나리오라 생성 시점 예약은 튜토리얼 계좌에 걸리고, 체결·취소 시점은 실제 `Account`(예약 0원)를 대상으로 확정·해제를 시도해 `IllegalStateException`(예약된 금액보다 큰 금액을 확정/해제할 수 없습니다)이 난다. `./gradlew test --tests "com.finplay.api.order.*" --tests "com.finplay.api.portfolio.*" --tests "com.finplay.api.education.*" --tests "com.finplay.api.account.*"` 재실행으로 1107건 중 이 2건만 남음을 재확인했다(나머지는 전부 통과, 신규 테스트 포함 회귀 없음).
- 21:48 — 위에서 발견된 구현 버그를 이 브랜치에서 마저 수정했다(사용자 승인). `PracticeLimitOrderCreationService.createSessionBuyOrder`에 `instrument.isTutorialSample()` 분기를 추가해 `LimitOrderCreationService.createBuyOrder`·`LimitOrderFillService.fillBuy`와 동일한 패턴으로 맞췄다 — 샌드박스 종목은 기존대로 튜토리얼 계좌에서 검증·예약(`TUTORIAL_INSUFFICIENT_CASH`)하고, 실제 종목(030 코인 연습의 BTC·ETH 등)은 실제 `Account`에서 검증·예약(`INSUFFICIENT_CASH`)한다. `./gradlew compileJava` 통과. `PracticeHoldingObservationIntegrationTest`·`PracticeLimitOrderTickIntegrationTest` 재실행으로 두 회귀가 모두 해소됐음을 확인했다. 기존 단위 테스트 `PracticeLimitOrderCreationServiceTest`의 두 테스트(`...ReservesCashInTutorialAccountOnlyAndCreatesPendingBuyOrderWithSessionId`, `...ThrowsTutorialInsufficientCashRegardlessOfRealAccountBalance`)는 instrument에 `tutorialSample=true`를 명시하지 않고도 우연히 통과하던 낡은 픽스처였음이 드러나 `ReflectionTestUtils.setField(instrument, "tutorialSample", true)`를 추가했고, 실제 종목 경로를 검증하는 신규 테스트 2건(`...ForRealInstrumentReservesCashInRealAccountOnly`, `...ForRealInstrumentThrowsInsufficientCashWhenRealAccountBalanceInsufficient`)을 추가했다(총 8→10건). `order`·`portfolio`·`education`·`account` 전체 재실행(1109건)에서 실패 0건.
- 21:53 — 마지막 항목(9번, `docs/prd.md` §3 구현 현황 갱신)을 완료했다. `TUTORIAL-ORDER-001~004` 행 뒤에 `TUTORIAL-CASH-ISOL-001~011` 행을 신설했다. 판정은 **일부 완료**다 — 001~009·011은 이 spec에서 구현·검증됐지만, `TUTORIAL-CASH-ISOL-010`(확인 필요)이 spec.md에서 "후속 확인 이슈로 넘긴다"고 명시한 채 실제로는 그 후속 이슈가 아직 생성되지 않았다(`intentionId` 없는 일반 리스크관리 OCO가 샌드박스 holding에도 생성될 수 있는 gap — 현금은 격리되지만 튜토리얼 진행 판정에 미치는 영향은 미확인). 이걸 근거 없이 "완료"로 적으면 CLAUDE.md 규칙 10(판정과 근거를 함께 적는다)을 어기므로 그대로 반영했다. 이번 브랜치에서 함께 고친 `createSessionBuyOrder` 회귀도 같은 행에 근거로 남겼다.

## 후속 — `sandbox_cash_adjustment` 컬럼 물리적 삭제 (이슈 #459)

spec 047 본문의 "제외 범위"(`Account.sandboxCashAdjustment` 컬럼은 파괴적 변경 2단계 배포 원칙에 따라 존치)에
따른 후속 정리를 이슈 #459로 분리해 추적한다. ADR-0021 §결정 7을 그대로 따르기 위해 PR 2개로 나눈다(PR #460
리뷰 코멘트에서 팀 자동 리뷰가 권장한 추적 메모).

- 02:16 — **PR-A(#460, `fix: Account에서 sandboxCashAdjustment 매핑 제거`) 완료·머지 대기.** `Account.sandboxCashAdjustment` 필드·`addSandboxCashAdjustment()` 메서드와 잔여 주석 4곳을 제거하고, 필드 제거로 깨지는 테스트 9개를 정리했다(컬럼이 아직 존재하는 마이그레이션 replay 테스트 2개는 엔티티 getter 대신 raw JDBC로 전환해 커버리지 유지). 마이그레이션은 포함하지 않는다 — 컬럼 자체는 이 배포 이후에도 DB에 남지만 앱은 더 이상 참조하지 않는다. **PR-B(컬럼 `DROP`, 새 Flyway 마이그레이션 + replay 테스트 정리)는 이 PR의 배포가 실제로 나간 것을 확인한 뒤에만 진행한다** — ADR-0021 §결정 6의 자동 롤백 창(직전 색 앱이 없는 컬럼을 조회하게 되는 상황)을 피하기 위함이다.
