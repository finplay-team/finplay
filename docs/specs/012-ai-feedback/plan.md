# Plan: 012 AI 피드백 — 구현 이슈 분할

> **이 문서가 분할 계획의 단일 출처다.** GitHub 이슈는 **착수 시점에 1개씩** 만들고, 계획을 바꿀 때는 이 문서만 고친다.
>
> **값(임계값·크론·범위·상태값·클래스명)은 `spec.md` §확정값이 정본이다.** 이 문서에도, 생성할 이슈 본문에도 값을 다시 적지 않는다 — 참조만 한다. #136에서 차단 96건을 고친 원인이 "같은 값이 다섯 곳에 있어 한 곳만 고쳤다"였다. 이슈 8개는 그 여섯 번째 자리가 되기 쉽다.

## 관련 문서

| 문서 | 담는 것 |
|---|---|
| `./spec.md` | **값·규칙의 정본.** §확정값(C-1~C-9)이 임계값·크론·범위·상태값·클래스명을 갖는다 |
| `./run-log.md` | 리뷰 9라운드 기록. 왜 그렇게 정했는지가 궁금하면 여기 |
| `docs/api-contracts.md` | 엔드포인트 4종의 요청·응답·오류 계약 |
| [ADR-0011](../../adr/0011-llm-provider-integration.md) | LLM 연동 방침 — 1번 이슈의 근거 |
| [ADR-0003](../../adr/0003-testing-strategy.md) | 완료 조건의 테스트 레벨 구분 근거 |
| [ADR-0004](../../adr/0004-flyway-migrations.md) | V13 단일 마이그레이션 — 2번 이슈의 근거 |
| [ADR-0002](../../adr/0002-architecture.md) | 도메인 패키지·레이어 규칙 |

**`_template/plan.md`의 API 설계·입력 명세·데이터 모델·테스트 계획 절이 여기 없는 이유가 있다.** 이 spec은 그 넷을 이미 다른 곳에 확정해 뒀다 — API 설계는 `api-contracts.md`, 데이터 모델은 `spec.md` §데이터 모델과 §C-8, 테스트 계획은 §완료 조건(레벨 표기 포함)이다. **여기 다시 적으면 그 자체가 이 문서가 막으려는 중복이 된다.** 이 문서는 남은 하나, **작업을 어떻게 쪼갤 것인가**만 담는다.

## 왜 한 번에 8개를 만들지 않는가

spec은 구현 중에도 바뀐다. 이슈 8개가 먼저 존재하면 spec을 한 번 고칠 때마다 이슈 8개를 훑어야 하고, 반드시 한둘을 놓친다. 놓친 이슈를 보고 구현하면 낡은 규칙이 코드가 된다.

**계획은 이 문서 하나에 두고, 이슈는 착수할 때 만든다.** 아직 안 만든 이슈는 고칠 필요가 없다.

## 분할 근거

병합된 `feat:` PR **41건** 전수 실측이다(2026-08-03 기준).

| 지표 | 값 |
|---|---|
| additions 중앙값 | **1,090줄** |
| IQR (Q1~Q3) | 606 ~ 1,563줄 |
| 최대 | 3,740줄 |
| 500줄 초과 | 41건 중 **34건 (82%)** |

`team-conventions.md`의 "PR 500줄 이하 권장"은 실태와 어긋난다. **분할 기준은 `003-market-data` 선례를 따른다** — 이슈 9개. spec 012는 003과 형태가 가장 닮았다(수집 파이프라인 + 배치 + 다수 조회 API + 실시간 경로).

spec 012 총량 추정 약 10,000줄 ÷ 1,090 ≈ 9.2다. **8개로 잡으면 평균 1,250줄**로 중앙값보다 조금 크지만 IQR 안이며, 게이트 대칭 검증(5번)과 DTO 공유(6·7번) 때문에 더 쪼개면 완료 조건의 소유자가 없어진다.

## 이슈 8개 (번호 = 착수 순서)

| # | 제목 | FEED | 선행 | 추정 |
|---|---|---|---|---|
| 1 | `[1차 고도화][AI 피드백] LLM 서술 생성·후검증·템플릿 폴백 구현` | 003 일부 | — | 800~1,100 |
| 2 | `[1차 고도화][AI 피드백] V13 마이그레이션·엔티티·리포지토리 구축` | 데이터 모델 | — | 1,000~1,300 |
| 3 | `[1차 고도화][AI 피드백] 뉴스·공시 수집 파이프라인 구현` | 001 | 2 | 1,200~1,600 |
| 4 | `[1차 고도화][AI 피드백] 주식 변동 탐지·개장 전 배치·카드 조회 API 구현` | 002·003·004·006 | 1·2·3 | 1,500~2,000 |
| 5 | `[1차 고도화][AI 피드백] 종목 뉴스 요약·개장 전 브리핑 조회 API 구현` | 008·009 | 3·4 | 1,100~1,500 |
| 6 | `[1차 고도화][AI 피드백] 매도 직후 피드백 조회 API 구현` | 007 | 1·2·4 | 1,200~1,700 |
| 7 | `[1차 고도화][AI 피드백] 반사실 시뮬레이션·집단 비교 구현` | 010·011 | 6 | 900~1,300 |
| 8 | `[1차 고도화][AI 피드백] 코인 실시간 변동 감시 구현` | 005 | 1·2·3 | 900~1,300 |

라벨은 8개 공통으로 `enhancement`, `scope:고도화`, `도메인:AI피드백`(신설). 1~3번에는 `type:기반`을 추가한다.

### 순서를 이렇게 잡은 이유

**1번이 LLM인 것이 핵심이다.** `spec.md` §외부 API 호출 상세가 Spring AI **2.0.0 이상**을 요구하는데(1.x는 Boot 3.x 전용), 이 조합은 **아직 검증되지 않았다.** 여기서 컨텍스트가 기동하지 않으면 나머지 7개의 전제가 통째로 바뀐다. 가장 싼 이슈에서 가장 비싼 위험을 먼저 터뜨린다.

**4번을 5·6번보다 앞에 둔다.** FEED-004가 개장 전 배치에서 FEED-002·003·008·009를 실행하므로, 5번은 4번이 만든 `FeedbackBatchService` 위에 얹힌다. 순서를 바꾸면 배치 골격을 두 번 만든다.

**5번이 Part C와 D를 함께 담는다.** 두 API가 §C-2의 09:00 하한을 공유하고, 완료 조건에 **"08:41에 두 API를 모두 호출"**하는 게이트 대칭 검증이 있다. 엔드포인트 단위로 자르면 그 조건의 소유자가 없어진다.

**7번을 6번에서 분리하되 붙여 둔다.** 둘 다 `PostSellFeedbackResponse`에 필드를 더하는 작업이라 동시에 하면 리베이스 충돌만 는다.

**7번이 시작할 때 확인할 것** (2026-08-05, #208 리뷰에서 나왔다). 6번이 `peerComparison.status`를 상수 `NOT_YET`으로 둬서 **재생성 게이트가 구조적으로 닫혀 있고**, 그 게이트를 여는 유일한 테스트가 `@MockitoBean PostSellFeedbackReader`에 의존한다. 7번이 확정 집계 행 기준 판정을 붙이면 게이트 조건은 **한 줄도 고치지 않고 열리지만**, 그 mock을 **실제 `price_move_peer_stats` 픽스처로 갈아야** 종단이 검증된다. 경계표와 코드 주석은 이슈 #208 본문 §6·7번 경계에 있다.

**8번이 마지막인 이유는 선행 작업이 딸려 있어서다.** 코인 감시는 `market`에 **가격 스냅샷 보관을 신설**해야 성립한다(`spec.md` §코인 가격 스냅샷). 없이 기존 조회로 대체하면 예외 없이 3.3시간 표본으로 계산돼 임계치 판정이 조용히 틀린다.

**병렬 여지** — `1 ∥ 2` → `3` → `4` → `{5, 6, 8}` → `7`. 다만 5·6·8이 `FeedbackBatchService`·`PostSellFeedbackResponse` 등 같은 파일을 건드리므로 **1인 진행이면 순차가 낫다**(`docs/parallel-agents.md`).

## 완료 조건 배정

`spec.md` §완료 조건은 **총 91건**이다. 그중 1건(원장 불변)은 8개 이슈 공통이므로 **개별 배정은 90건**이고, 아래 표의 합이 90이어야 한다.

> **절 단위로만 나누면 새어 나간다.** 초안이 "노출 게이트 → 4·5번", "원장 불변 → 1·2·3번"으로만 적었다가 **매도 회고 게이트 3건과 원장 불변 1건이 어느 이슈에도 안 걸렸다**(PR #148 리뷰 1라운드). 한 절이 여러 이슈로 쪼개지면 **조건을 하나씩 지목한다.**
>
> **건수는 추정하지 말고 세라.** 2라운드에서 이 표 자체가 합이 안 맞았다(93 vs 91) — 같은 조건을 두 이슈에 중복 계상하고, 두 절의 개수를 눈대중으로 적은 탓이다. 아래는 `spec.md`의 bullet을 실제로 센 값이다.

| 이슈 | 배정 | 건수 |
|---|---|---|
| 1 | 문구 (단위) 9 | **9** |
| 2 | 수집 절의 `@DataJpaTest` 2 + "V13 `validate` 통과" 1 | **3** |
| 3 | 수집 절 나머지 4 + "외부 키 없이 build" 1 + "`.env.example`" 1 | **6** |
| 4 | 탐지 (단위) 7 + 노출 게이트 ①~⑥ 6 + 배치 ①~⑤ 5 + API 계약 2 | **20** |
| 5 | 노출 게이트 ⑦~⑫ 6 + 상태값 4 + 배치 ⑨~⑬ 5 + API 계약 1 | **16** |
| 6 | 매도 회고 12 + 노출 게이트 ⑬~⑮ 3 + 상태값 1 + API 계약 2 | **18** |
| 7 | 매도 회고 4 + 상태값 2 + 배치 ⑥~⑧ 3 | **9** |
| 8 | 코인 탐지 (단위) 7 + 배치 ⑭~⑮ 2 | **9** |
| | **합계** | **90** |

`90 + 원장 불변 1 = 91`.

### 절이 쪼개지는 자리 — 조건 단위 지목

**노출 게이트 15건** (번호는 `spec.md` 등장 순서)

| | 이슈 | | 이슈 | | 이슈 |
|---|---|---|---|---|---|
| ① `reveal_time` 지난 카드만 | 4 | ⑥ 재재생에도 게이트 작동 | 4 | ⑪ Part D에 장중 기사 없음 | 5 |
| ② 전날 저녁 기사 09:00 노출 | 4 | ⑦ Part C·D 09:00 하한 동일 | 5 | ⑫ `D` 접수 공시는 `FULL`만 | 5 |
| ③ 갭 카드 `revealTime` 09:00 | 4 | ⑧ 목록 상한·정렬 | 5 | ⑬ 매도 후 흐름·반사실 `NOT_YET` | **6** |
| ④ 장중 카드 `+1분` | 4 | ⑨ Part C `items` 범위 | 5 | ⑭ 전날 매도 건 `READY` 유지 | **6** |
| ⑤ 근거 기사 시각까지 밀림 | 4 | ⑩ Part C 요약이 장중 미언급 | 5 | ⑮ `priceMoves`에도 카드 게이트 | **6** |

⑬~⑮가 1라운드에서 빠져 있었다. **매도 회고의 게이트라 카드에도 Part C·D에도 속하지 않는다.**

**배치 15건**

| | 이슈 | | 이슈 |
|---|---|---|---|
| ① 개장 전 배치가 분봉을 받아온다 | 4 | ⑨ 코인 요약·브리핑이 배치에서만 생성 | 5 |
| ② 분리된 크론·`READY` 확인 | 4 | ⑩ 새 기사 없으면 LLM 미호출 | 5 |
| ③ 생성 순서 §C-6 | 4 | ⑪ 코인 조회가 `generated_at` 최신 1행 | 5 |
| ④ LLM 하나 실패해도 계속 | 4 | ⑫ 코인 UPSERT 하루 1행 | 5 |
| ⑤ 두 번 실행해도 중복 없음 | 4 | ⑬ 코인 Part C는 `ROLLING_24H`만 | 5 |
| ⑥ 장 마감 배치가 집계 저장 | 7 | ⑭ `cron` `@Scheduled`에 `zone` | 8 |
| ⑦ 재재생 시 서비스 날짜별로 | 7 | ⑮ `pool.size` ≥ `@Scheduled` 수 | 8 |
| ⑧ 배치 전 조회 시 재생성 안 함 | 7 | | |

**상태값 7건** — Part C·D 판정 4건(①~④)은 5번, `narrativeStatus` 1건(⑤)은 6번, `peerComparison` 2건(⑥⑦)은 7번.

**매도 회고 16건** — 반사실·집단 비교 4건(반사실 수수료 재계산 · 반사실이 서술에 미포함 · 모집단 재구성 · 회원 식별자 없음)은 **7번**, 나머지 12건은 **6번**.

**수집 6건** — `@DataJpaTest` 2건은 **2번**(스키마·제약), 나머지 4건은 **3번**.

**API 계약 5건** — 401 인증과 카드 0건·세션 미준비는 **4번**, `market` 파라미터 400은 **5번**, 체결 403/400과 응답 직렬화는 **6번**.

**FEED-006의 코인 분기는 8번 소유다.** 컨트롤러는 4번이 만들지만 4번의 탐지는 주식 전용이라, "코인은 최근 24시간 카드를 반환한다"와 `originTradeDate=null`은 **8번이 그 컨트롤러에 분기를 더하며** 닫는다. 응답 쪽 단정은 이미 **코인 탐지 ⑦**("응답의 `originTradeDate`는 코인이면 `null`이다", 8번 소유)이 갖고 있으므로 **새 조건을 만들지 않고 표의 건수도 바뀌지 않는다.** 2026-08-04 planner가 짚은 자리다 — 적어 두지 않으면 4번이 주식만 만들고 8번이 조회 경로를 잊는다.

**원장 불변·기동 4건** — "외부 키 없이 build"는 **3번 한 곳에만** 배정한다(1라운드에서 1·3번에 중복 계상했다). `.env.example`은 3번, V13 `validate`는 2번이다.

**문구 9건 — 마지막 조건만 검증 범위가 좁다.** "재생성 후에도 걸리면 `summary=null`·`narrative_source=NONE`·상태값 `UNAVAILABLE`이고 `items`는 채워진다"의 **뒤쪽 절반(`UNAVAILABLE` 매핑·`items` 채움)은 1번이 검증할 수 없다** — 1번에는 엔티티도 응답 DTO도 없고 둘 다 5번에서 생긴다. **1번은 후검증 결과 타입에서 "서술 없음 + `NONE`"까지만 단정한다.** 응답 계층은 이미 **상태값 절 ④**("행이 있고 `summary`가 `NULL`이면 `UNAVAILABLE`이고 `items`가 채워진다", 5번 소유)가 별도 조건으로 담고 있으므로 **중복 계상이 아니고 표의 건수도 바뀌지 않는다** — 문구 9건은 그대로 1번이다. 이슈 #147 본문에도 같은 범위 주석을 달았다.

### 원장 불변은 전 이슈 공통이다

**"카드 생성·조회 전후로 주문·체결·계좌·잔액·보유·손익 원장이 변하지 않는다"는 한 이슈에 배정하지 않는다.** 이 spec의 어떤 코드도 원장을 건드리면 안 되므로 **8개 이슈 전부의 완료 조건에 넣는다.** 배치가 생기는 4번, 회원별 쓰기가 생기는 6번, 실시간 경로가 생기는 8번이 특히 위험하다.

## 이슈 본문 작성 규칙

`.github/ISSUE_TEMPLATE/feature.yml`의 5개 항목을 채운다.

| 항목 | 담을 것 |
|---|---|
| 기능 설명 | 무엇을 왜. **비즈니스 언어로.** 구현 방법은 spec에 있으니 적지 않는다 |
| 완료 조건 | 위 배정표의 해당 절을 옮긴다. 규칙은 `spec §X 참조`로 |
| 제외 범위 | 다음 이슈로 넘길 것을 번호와 함께 |
| 선행 / 충돌 주의 | `blocked by #NNN` + 같은 파일을 건드리는 이슈 |
| 관련 스펙 | `docs/specs/012-ai-feedback` |

**금지 사항 셋.**

- **값을 적지 마라.** 크론·임계값·범위·상태값은 전부 `spec.md` §확정값 참조다.
- **이슈 제목에 spec 번호(012)를 넣지 마라.** 브랜치명이 3자리를 spec 번호로 오독하는 문제가 있었다(`docs/git-conventions.md`).
- **완료 조건을 새로 만들지 마라.** spec에 없는 조건이 필요하면 spec을 먼저 고친다.

**예외 하나.** 이 문서와 이슈 본문이 **왜 그렇게 쪼갰는지 설명할 때는 클래스명·시각을 인용해도 된다** — "5번은 4번이 만든 `FeedbackBatchService` 위에 얹힌다" 같은 문장이다. 값을 **정의**하는 것과 근거를 **서술**하는 것은 다르다. 다만 그 값이 판단 기준이 되면(임계값·크론·범위) 반드시 §참조로 적는다.

## 착수할 때마다 하는 일

```
1. 이 문서에서 다음 번호 확인
2. 이슈 1건 생성 → 아래 진행 상태 표에 번호 기록
3. 브랜치 feat/<이슈번호>-<영문요약>
4. 구현 → ./gradlew build → PR (Closes #NNN)
5. spec을 고쳤으면 이 문서의 배정표도 함께 확인
```

## 진행 상태

| # | GitHub 이슈 | 상태 |
|---|---|---|
| 1 | #147 | **생성됨** |
| 2 | #160 | **생성됨** |
| 3 | #167 | **생성됨** |
| 4 | #180 | **생성됨** |
| 5 | #188 | **생성됨** |
| 6 | #208 | **생성됨** |
| 7 | #212 | **생성됨** |
| 8 | — | 미생성 |

## 착수 전 확인이 필요한 것

- **Spring AI 2.0.0 × Boot 4.1.0 호환** — **검증 완료(2026-08-03, 커밋 `d311b87`).** 스타터 POM이 `spring-boot-starter:4.1.0`을 직접 선언해 좌표 수준 충돌이 없다. 다만 **키 없이는 컨텍스트가 기동하지 않는 문제가 실재했다** — OpenAI 스타터가 chat 외에 embedding·image·moderation·audio 빈까지 자동 등록하는데 audio speech 빈이 기동 시점에 키를 요구한다. `spring.ai.model.*`로 안 쓰는 유형을 끄고 `spring.ai.openai.api-key`에 자리표시자 기본값을 둬 해결했다(`docs/agent-mistakes.md` 2026-08-03 행). 후속 7개 이슈의 전제가 바뀌지 않았다.
- **외부 API 키 4종** — `NAVER_SEARCH_CLIENT_ID`·`NAVER_SEARCH_CLIENT_SECRET`·`DART_API_KEY`·`OPENAI_API_KEY`. **없어도 구현·테스트는 진행된다**(Fake 구현 + 템플릿 폴백). 3번 착수 전까지 발급하면 된다.
- **OpenDART `corpCode.xml` 매핑** — 주식 종목의 `corp_code`를 미리 리소스로 만들어 둔다. 3번에서 필요하다.
- **코인 검색어 품질** — 보정 규칙(`" 코인"` 접미, 유사 종목명 제외)을 넣었지만 실측은 3번 착수 후에야 가능하다. §튜닝에 확인 항목이 있다.

## 후속으로 낼 이슈

- **뉴스 `publisher`의 한글 언론사명 매핑** — 네이버 뉴스 검색 응답에 언론사 이름 필드가 없어(`title`·`originallink`·`link`·`description`·`pubDate`가 전부) 3번 이슈는 `originallink` 호스트를 그대로 저장한다(§C-8). 화면에 `한국경제`가 아니라 `hankyung.com`이 뜬다. 도메인→한글 이름 매핑은 국내 언론사가 수백 개라 태생적으로 불완전하고 유지 대상이라 **이 spec의 범위 밖으로 두고 별도 이슈로 연다.** 2026-08-03 결정.

- **`team-conventions.md`의 "PR 500줄 이하 권장" 현실화** — 병합된 `feat:` PR 41건 중 34건(82%)이 이 기준을 넘겼고 중앙값이 권장치의 2.2배다. 사문화된 규칙이 분할 판단을 흐린다. `003`·`012` 같은 유형(수집 파이프라인 + 배치 + 다수 조회 API)은 예외라는 각주를 다는 편이 낫다. **이 spec의 범위 밖이므로 별도 문서 이슈로 연다.**

- **조회 경로 LLM 호출량 계측** (2026-08-05, 6번 이슈 #208에서 나왔다). `LlmCallStats`는 **배치 스코프 전용이고 조회 경로 호출을 세지 않는다** — #198이 그렇게 만들었고 의도된 동작이다. 그런데 6번이 조회 경로에 LLM을 처음 넣었다(매도 회고는 최초 조회 시 생성, 재생성 게이트 통과 후 1회 더). **체결 1건당 최대 2회로 상한이 있어 폭주하지는 않지만 현재 관측 수단이 0이다.** #198 실측으로 건당 중앙값 2,550ms · p95 3,119ms이므로 사용자가 그 시간을 기다리는 경로이기도 하다.
  - **이 spec의 완료 조건에 없는 항목이라 8개 분할에 넣지 않는다.** 선례는 **#198(개장 전 배치 계측)** 이며 그것도 분할 밖의 별도 이슈이고 위 §진행 상태 표에 없다. 같은 형태로 연다.
  - 착수하면 **spec에 조건을 먼저 추가**해야 한다(§이슈 본문 작성 규칙의 "완료 조건을 새로 만들지 마라"). 또는 spec 밖 관측성 이슈로 세운다 — #198이 후자였다.
  - 주의: `System.nanoTime()`을 쓴다. 이 저장소는 시각을 `Clock`으로 주입받고 테스트가 고정 `Clock`으로 바꾸므로 `Clock`으로 재면 **통합 테스트에서 항상 0이 나오면서 통과한다**(#198이 회귀 테스트까지 남겼다).

- **`docs/prd.md`의 FEED 요구사항 번호 정합** (2026-08-05, #208 planner·reviewer가 짚었다). PRD가 매도 직후 피드백을 **FEED-009**로 적고 있었다(#208에서 그 두 줄만 FEED-007로 정정했다). 남은 어긋남은 **뉴스 요약·브리핑을 FEED-008로 묶은 행**이며 spec은 008=종목 뉴스 요약, 009=개장 전 브리핑으로 나눈다.
  - **번호를 옮기는 쪽은 비용이 크다** — spec 012의 FEED-001~011은 이미 머지된 이슈 6개(#147·#160·#167·#180·#188·#208)의 본문과 커밋 메시지, `plan.md` 이슈 표의 FEED 열, `api-contracts.md`의 근거 열(`012 FEED-007` 형태)에 인용돼 있다. **PRD에 대응표를 두는 방향도 후보다.**
  - PRD §3이 "무엇이 실제로 동작하는지의 정본"(CLAUDE.md 규칙 10)이라 ID로 spec 요구사항을 역추적하는 경로가 끊기는 것이 실제 손해다. **별도 문서 이슈로 연다.**
