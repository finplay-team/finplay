# Run Log: 026-market-order-practice-tutorial

## AI 로그 (에이전트 참조용)
| 시각 | 에이전트 | 실행 명령 | 근거 |
|---|---|---|---|
| - | implementer | `.\gradlew.bat compileJava` | plan.md "3단계 참조 가격선 계산"·"Evidence A"·"Evidence B" 절, 019 spec.md 계산 규칙 |
| - | reviewer(PR #298 리뷰, 독립 재검토) | `git diff origin/dev...HEAD -- src/main/java`, `git diff origin/dev...HEAD -- docs/...`, PowerShell `[decimal]` 산술로 `ReferencePriceCalculatorTest` 반올림 기대값(11933.62345660/13305.20370801 등) 재계산 검증, `gh pr view 298`·`gh issue view 297`·`gh pr checks 298` 대조 | docs/conventions.md, ADR-0002, ADR-0003, docs/specs/026-market-order-practice-tutorial/plan.md·spec.md, 019-exit-price-policy/spec.md |
| - | 오케스트레이터(PR #298 리뷰 참고사항 반영) | `EvidenceJudgmentService.judgeBoundaryEvidence`의 동률 tie-break를 임의 결정에서 명시 확정으로 전환(Javadoc·plan.md "Evidence A" 절에 근거 추가) | PR #298 리뷰 참고 2번 |
| - | implementer(이슈 #300, tasks.md 3번) | `.\gradlew.bat compileJava` | plan.md "확정 HTTP·JSON 계약"·"DTO 필드와 nullable"·"트랜잭션과 경합" 절, ADR-0002, docs/conventions.md |
| - | reviewer(이슈 #300 리뷰) | `git diff origin/dev...HEAD`, `MarketPracticeChainResolutionService`·`ReferencePriceCalculator`·`EvidenceJudgmentService`·`ErrorCode`·`docs/prd.md` §3 대조 | docs/conventions.md, ADR-0002, ADR-0003, plan.md "2단계 chain 해석"·"확정 HTTP·JSON 계약" 절 |
| - | implementer(이슈 #303, tasks.md 4번) | `JAVA_HOME=~/.jdks/ms-17.0.20 ./gradlew.bat compileJava` | plan.md "확정 HTTP·JSON 계약"·"트랜잭션과 경합" 절, ADR-0002·0004, docs/conventions.md |

## 모니터링 (사람용 요약)
- 참조 가격선 계산(`ReferencePriceCalculator`)·evidence A/B 판정(`EvidenceJudgmentService`) 순수 서비스 추가, 컴파일 통과. `PracticeIntention`에 `exitPriceType`/rate 필드가 아직 없어(019 미착수) PERCENT 분기는 독립 메서드로만 구현.
- PR #298 독립 재리뷰(reviewer 세션, 이전 판정을 신뢰하지 않고 재검증) — 차단 0건, 권장 0건, 참고 2건. PowerShell decimal로 PERCENT 반올림 테스트 기대값을 직접 재계산해 정확함을 확인했고, tie-break 죽은 분기 주장도 대수적으로 재확인(entryPrice가 항상 두 경계 사이에 있다는 019 불변조건 하에 성립). 신규 API 없음 → api-routes/api-contracts/prd 갱신 불필요, ADR 위반 없음, 순수 서비스라 단위 테스트만으로 충분(ADR-0003). 머지 가능.
- **참고 1(DTO 네이밍) 처리**: `BoundaryEvidenceResult`/`ObservationEvidenceJudgment`/`ReferencePriceLines`가 `docs/conventions.md`의 `~Dto` 접미사 관례를 따르지 않는다는 지적. 기존 코드베이스에 `OAuthAuthorizationResult`(`~Result` 접미사) 선례가 있고 리뷰어가 명시적으로 "지적하지 않는다"고 판정해, **변경하지 않기로 결정**한다(개인 취향 수준, 일관된 다른 관례로도 커버됨).
- **참고 2(tie-break 임의 결정) 처리**: 동률이면 `STOP_LOSS` 우선이라는 규칙을 spec.md가 아니라 코드에만 있던 임의 결정에서, `plan.md` "Evidence A" 절과 Javadoc에 근거(019 불변조건 하 도달 불가능, 불변조건 깨질 경우를 대비한 명시적 확정)를 남겨 **문서화된 결정으로 전환**했다. 로직 자체는 변경하지 않았다(이미 "STOP_LOSS 우선"이었고 그 선택 자체를 바꿀 근거는 없었다 — 임의성만 해소).
- **이슈 #300(tasks.md 3번)**: `POST /api/education/practice/holding-observations` Controller·Service·DTO를 `education.marketpractice` 하위(controller/service/dto)에 구현. holding 조회는 `HoldingService.findHoldingForOwner` 신설(owner 불일치 시 빈 값→404), chain 재해석은 기존 `MarketPracticeChainResolutionService.resolve` 결과의 `holdingId`가 요청과 일치하는지 확인(불일치·chain 없음 모두 409 `PRACTICE_EVIDENCE_MISSING`). `ErrorCode.PRACTICE_EVIDENCE_MISSING`을 신설(016 계획 문서에 이미 정의돼 있던 코드를 이번에 최초로 실제 enum에 추가 — "새 코드 미추가" 원칙은 신규 개념이 아니라 이미 설계된 코드를 formalize한 것으로 해석). V27 migration은 이슈 #294가 이미 만들어 재사용. 컴파일 통과, 테스트는 tester 담당으로 남김. `docs/api-routes.md`·`docs/api-contracts.md`에 신규 라우트·계약 추가(실제 표로 반영, 026 신규 절). `docs/prd.md` §3은 갱신 대상 아님(오케스트레이터 지시대로 판단 보류 — holding-reflections·GET 진행조회까지 갖춰야 사용자가 실제로 쓸 수 있는 완결 기능이라 지금은 "일부 완료" 여부를 이 항목이 결정하지 않음).
- 이슈 #300 리뷰(reviewer, 코드 리뷰 모드) — 차단 1건: `PracticeHoldingObservationService`가 `MarketPracticeChainResolutionService.resolve(userId, tutorialKey)`로 tutorialKey 전체에서 우선순위 1건만 골라 요청 `holdingId`와 대조하는 방식이라, 같은 market에 완료된 chain이 2개 이상이면(예: 두 종목 각각 즐겨찾기→의도→매수 완료) 후순위 holding을 요청할 때 그 holding 자신의 chain은 유효해도 409 `PRACTICE_EVIDENCE_MISSING`으로 오탐한다(plan.md "해당 instrument"의 chain 재해석 계약과 불일치, `MarketPracticeChainResolutionService` 클래스 Javadoc도 이 시그니처가 다음 작업 항목에서 바뀔 수 있음을 예고해 뒀었다). 권장 1건: `docs/prd.md` §3 미갱신 결정(오케스트레이터 판단 보류, 이 로그 상단 이슈 #300 항목 참고)이 CLAUDE.md 규칙10 "새 엔드포인트를 제공하는 PR" 문구와 배치되므로 재검토 권고. `ErrorCode.PRACTICE_EVIDENCE_MISSING` 신설(016 설계에는 있었으나 코드에는 없던 것을 처음 코드화, 타당함), `ErrorCodeTest` 화이트리스트 갱신, `PracticeHoldingObservationIntegrationTest`의 `@Transactional` 추가(AccountSummaryIntegrationTest 관례와 일치, InstrumentRepositoryTest 재파괴 없음)는 모두 검증 확인, 문제 없음.
- **이슈 #303(tasks.md 4번)**: `POST /api/education/practice/holding-reflections` Controller·Service·DTO를 `education.marketpractice` 하위에 구현. 착수 전 `db/migration/` 최신 확인 결과 V27이 최신이라 `V28__create_practice_completions_table.sql`로 `practice_completions`(FK `reflection_id → practice_market_reflections.id`, `UNIQUE(user_id, tutorial_key)`) 신설. `PracticeCompletion` 엔티티·`PracticeCompletionRepository` 추가, `PracticeProgress`에 `complete(LocalDateTime)` 전이 메서드 신설(기존에 없었음). 서비스 처리 순서: holding 소유권 404 → `practice_progresses` FOR UPDATE 잠금(행 없으면 409 `PRACTICE_EVIDENCE_MISSING`) → 이미 `COMPLETED`면 409 `PRACTICE_ALREADY_COMPLETED` → `resolveForInstrument`로 chain 재해석(불일치·실패 409 `PRACTICE_EVIDENCE_MISSING`) → 기존 관찰 중 `evidenceType` non-null 1건 이상 필요(없으면 같은 409) → 복기 1행 + completion 1행 저장 + progress `COMPLETED` 전이, 전부 한 `@Transactional`. 새 오류 코드 없음(기존 코드만 사용, spec 완료조건과 일치). `docs/api-routes.md`·`docs/api-contracts.md`에 신규 라우트·계약 절 추가. `docs/prd.md` §3은 오케스트레이터 최종 판단 사항이라 이번 항목에서 갱신하지 않음(지시사항). 컴파일 통과(테스트는 tester 담당으로 남김).
