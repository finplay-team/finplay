# Run Log: 030-coin-practice-price-runtime

## AI 로그 (에이전트 참조용)
| 시각 | 에이전트 | 실행 명령 | 근거 |
|---|---|---|---|
| - | implementer | `education.priceruntime` 패키지에 엔티티·Repository·v1 생성기·서비스·컨트롤러·V29 migration 작성 | plan.md 도메인·패키지 경계, 데이터 모델, 생성기·가격 anchor 절, Issue #318 |
| - | implementer | `.\gradlew.bat compileJava`, `spotlessApply` | conventions.md 포맷 규칙 |
| - | implementer | `ErrorCode` 2건 추가, `PracticePriceSession.advance()`, `findByIdAndUserIdForUpdate` 비관 잠금, `PracticePriceTickAdvanceRequest`, 신설 `PracticePriceTickService.advanceTick`, 컨트롤러에 `POST /{sessionId}/ticks` 추가 | 이슈 #319 코멘트(tick 전용 서비스 분리), plan.md 트랜잭션·잠금 절, Order.cancel() 패턴 |
| - | implementer | `./gradlew compileJava` | 컴파일 통과 확인 |
| - | tester | 단위(`PracticePriceTickServiceTest`)·`@DataJpaTest`(잠금 조회)·`@WebMvcTest`(tick 엔드포인트)·Testcontainers 동시성(`PracticePriceTickConcurrencyIntegrationTest`) 작성, `ErrorCodeTest` 화이트리스트 갱신 | ADR-0003, conventions.md 테스트 규칙, `LimitOrderConcurrencyIntegrationTest` 동시성 패턴 |
| - | 메인 세션 | 전체 `./gradlew build`에서 `InstrumentRepositoryTest` 3건 회귀 발견(신설 concurrency 테스트가 커밋한 crypto Instrument 2건 정리 누락) → `PracticePriceTickConcurrencyIntegrationTest`에 `@AfterEach` raw JDBC 정리 추가, 재검증 | `PracticeHoldingReflectionConcurrencyIntegrationTest` 관례, `docs/agent-mistakes.md` 2026-08-10(두 번째) 행 |
| - | implementer | `Order.practicePriceSessionId` 필드+`createPracticeLimitPendingBuy` 팩토리, V30 migration, `OrderRepository`에 세션 스코프 쿼리 2개 추가 + `findPendingLimitOrdersToFill`에 `practicePriceSessionId is null` 역방향 오염 차단 조건 추가 | 이슈 #320 코멘트(1안), plan.md 데이터 모델·역방향 오염 차단, ADR-0004 |
| - | implementer | `order.service`에 `PracticeLimitOrderCreationService`(BUY 고정·세션 FK·기존 검증 재사용, `LimitOrderCreationService`의 검증 3메서드를 package-private static으로 재사용)·`PracticeOrderSettlementService.settleOnTick`(세션 PENDING 주문 id ASC 일괄 잠금→체결 판정→lastTick 잔여 취소·예약 반환, 기존 `LimitOrderFillService`/`LimitOrderCancelService` 위임) 신설 | 이슈 #320 코멘트(1안), plan.md 트랜잭션·잠금·이벤트 절 |
| - | implementer | `education.priceruntime`에 `PracticeLimitOrderController`/`PracticeLimitOrderService`(세션 FOR UPDATE→owner·instrument·ACTIVE 검증→order 공개 서비스 호출)·`PracticePriceTickAdvancedEvent`·`PracticeTickFillListener`(일반 동기 `@EventListener`, 예외 미흡수) 신설. `PracticePriceSession.advance()`를 tick/가격 갱신 전용으로 좁히고 `complete()`를 분리, `PracticePriceTickService.advanceTick`이 advance→이벤트 발행(동기 체결)→lastTick이면 complete 순서로 호출하도록 재구성 | 이슈 #320 코멘트(1안), plan.md 트랜잭션·잠금·이벤트 절("체결 판정→잔여 취소·예약 반환→COMPLETED 전이" 순서), `@TransactionalEventListener` 금지 규칙 |
| - | implementer | `ErrorCode`에 `PRACTICE_LIMIT_ORDER_ALREADY_PENDING`·`PRACTICE_PRICE_SESSION_MISMATCH` 추가, `docs/api-routes.md`·`docs/api-contracts.md`(030 계획→실제 이관, 이 controller 한정)·`docs/prd.md` §3 갱신 | CLAUDE.md 규칙 7·10, plan.md "후속 이슈 순서"(계획-실제 대조 후 이관) |
| - | implementer | 시그니처 변경(`PracticePriceTickService` 생성자, `PracticePriceSession.advance`)으로 깨진 `PracticePriceTickServiceTest` 컴파일 오류 최소 수정(mock `ApplicationEventPublisher` 추가, `advance`+`complete` 2단 호출로 교체) 후 `.\gradlew.bat compileJava compileTestJava`, `spotlessApply` 실행 | `docs/agent-mistakes.md` Write 도구 CRLF 행, conventions.md 포맷 규칙 — 테스트 보강·재검증은 tester 담당 |
| - | reviewer | `git diff dev...HEAD`(리뷰 모드) 점검, `docs/conventions.md`·ADR-0002/0003/0004·`030/plan.md`·`030/spec.md`·이슈 #320 코멘트 원문(1안 채택 근거)과 대조 | CLAUDE.md 규칙 7·10, 리뷰 체크 질문(conventions.md) |

## 모니터링 (사람용 요약)
- Issue #318 범위(세션 생성·조회 API, v1 생성기, migration V29)는 완료. 이번 작업으로 next-tick(`POST /{sessionId}/ticks`)까지 구현. 교육 지정가 주문 체결 연결(tick 트랜잭션에 주문 체결·취소 추가)은 여전히 후속 이슈.
- `PracticePriceSessionService`는 수정하지 않고 새 `PracticePriceTickService`로 tick 전진만 분리(이슈 #319 코멘트 2안).
- `./gradlew build` 전체 통과(Spotless·SpotBugs·JaCoCo 40%·전체 테스트 포함). api-routes.md·api-contracts.md·prd.md §3·`030/tasks.md` 동기화 완료.
- (이슈 #320) 교육 전용 지정가 BUY API(`POST /api/education/practice/limit-orders`)·주문 `practicePriceSessionId` FK·세션 전용 이벤트 체결·마지막 tick 취소/예약 반환을 이슈 코멘트 1안(order가 세션 스코프 유스케이스 제공, education이 리스너 소유)대로 구현. **판단**: plan.md·api-contracts.md의 계획 응답이 "기존 `OrderResponse`"였으나 생성 시점은 항상 PENDING(Trade 없음)이라 실제로는 기존 `/api/orders/limit`과 동일하게 `LimitOrderResponse`를 반환하도록 구현하고 계약 문서도 그렇게 갱신했다(코드를 계획에 억지로 맞추지 않음, plan.md "후속 이슈 순서" 원칙). holding 관찰 세션 역추적(tasks.md 4번째)과 사용자·세션·일반 주문 격리 통합 테스트(tasks.md 5번째, #313)는 이번 범위에 포함하지 않았다. compileJava·compileTestJava만 확인했고 테스트 작성·전체 `./gradlew build`는 tester 담당.
- (PR 리뷰, 이슈 #320) 차단 0건. 도메인 경계(order는 education 미의존)·잠금 순서·tick 99 순서·`findPendingLimitOrdersToFill` 역방향 오염 차단·동기 `@EventListener`(예외 미흡수) 모두 plan.md대로 구현 확인. 이슈 #320 코멘트가 사전에 명시한 "order 락 재획득" 우려는 같은 트랜잭션 내 재확인이라 plan.md 요건을 만족(코멘트 원문 기준 의도된 설계). 권장 2건(plan.md API 표 미갱신 — OrderResponse로 남아 있어 api-contracts.md의 LimitOrderResponse와 불일치; 교육 주문이 기존 GET /api/orders/pending·PATCH/DELETE /api/orders/{orderId}에 노출·조작 가능한데 계약 문서에 미기재 — 이슈 자체 분석이 예견한 QA 리스크). SHA-256 요청해시 헬퍼 3중 중복은 참고로 남김.
