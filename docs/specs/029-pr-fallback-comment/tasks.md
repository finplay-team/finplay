# Tasks: PR 생성 전 정지 지점 이슈 알림

- [x] `.github/workflows/agent.yml` — 구현 에이전트 호출 스텝(`uses: anthropics/claude-code-action@v1`, 현재 id 없음)에 `id: implement`를 부여하고, 그 뒤에 "구현 에이전트 호출 실패 이력 코멘트" 스텝(`id: implement_failure_comment`, `if: always() && steps.implement.outcome != 'success'`)을 추가한다. plan.md의 스텝 1번 YAML을 그대로 반영한다.
- [x] `.github/workflows/agent.yml` — "빌드 검증"(`id: build`) 스텝 뒤, "방금 연 PR 번호 조회" 스텝 앞에 "빌드 검증 실패 이력 코멘트 — PR 생성 전" 스텝(`id: build_failure_issue_comment`, `if: always() && steps.implement.outcome == 'success' && steps.build.outcome != 'success'`)을 추가한다. plan.md의 스텝 2번 YAML을 그대로 반영한다.
- [x] `.github/workflows/agent.yml` — "방금 연 PR 번호 조회"(`id: pr`) 스텝 뒤, "코드 리뷰" 스텝 앞에 "PR 번호 조회 실패 이력 코멘트" 스텝(`id: pr_lookup_failure_comment`, `if: always() && steps.build.outcome == 'success' && steps.pr.outcome != 'success'`)을 추가한다. plan.md의 스텝 3번 YAML을 그대로 반영한다. 기존 `build`·`pr` 스텝 본문과 이후 PR 기반 이력 코멘트 스텝(`review_failure_comment` 등)은 손대지 않는다.
- [x] `docs/adr/0016-review-gate-auto-fix-round.md` — plan.md "ADR-0016 갱신" 절의 두 수정(메타데이터 갱신 이력 한 줄 추가 + "이력 코멘트 스텝은 always()" 목록에 신규 3개 스텝 항목 추가)을 그대로 반영한다. ADR을 새 번호로 대체하지 않는다(CLAUDE.md 규칙 2 — 적용 범위 확장이지 결정 번복이 아님).
- [ ] 로컬 검증 — plan.md "로컬 검증 절차" 1~3번을 수행한다: `python -c "import yaml; yaml.safe_load(...)"` 통과, 신규 `run:` 블록 3개를 LF로 저장해 `bash -n` 통과, 실제 커밋된 `agent.yml`의 세 신규 `if:` 문자열을 plan.md "도달 가능한 outcome 조합 전수 검토" 표와 한 글자씩 대조. `./gradlew build`는 이 변경의 검증 대상이 아니다(`src/` 무변경).
- [ ] 테스트 이슈 1건으로 실제 검증 — **PR 머지 후에만 가능** (spec.md 완료 조건, `issue_comment` 트리거는 항상 `dev` 기준 워크플로우 버전으로 실행됨). 세 정지 지점 중 최소 1곳(빈 `PR_NUMBER` 유발이 가장 재현하기 쉬움)에서 이슈 코멘트가 실제로 남는지 확인하고, 같은 실행에서 정상 구간(PR 오픈 이후) 코멘트가 중복되지 않는지 함께 확인한다.
