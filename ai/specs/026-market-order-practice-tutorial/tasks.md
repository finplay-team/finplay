# Tasks: 시장가/지정가 매매 기반 3단계 투자 실습 완결 경로 (OCO 없이)

- [x] **migration + 2단계 chain 해석 서비스** — `V27__create_practice_market_observations_and_reflections.sql`, `practice_market_observations`/`practice_market_reflections` 엔티티·repository, `favorite → intention → buyTrade → holding` chain 해석 로직(수량 정규화 비교, 최이른 buyTrade 선택, 우선순위 규칙) + 단위 테스트 42건 통과. (이슈 #294)
- [x] **참조 가격선 계산 + evidence A/B 판정** — `019` PRICE/PERCENT 공식 재사용한 참조 손절가·익절가 계산(매 요청 재계산, snapshot 없음), evidence A(경계 접근)·B(2분 3회) 판정 로직 + 단위 테스트 29건 통과(경계값·PERCENT 반올림 포함). PERCENT 계산은 019 production 미착수·intention이 아직 PRICE만 저장해 현재 chain 해석에서는 호출되지 않으나, 019 착수 시 재사용할 수 있게 공식은 완성해 뒀다. (이슈 #297)
- [x] **`POST /api/education/practice/holding-observations`** — Controller·DTO·Service, holding 소유권 404, chain 실패 409 `PRACTICE_EVIDENCE_MISSING`(spec에 설계돼 있었으나 코드에는 없어 이번에 `ErrorCode`에 신설), `PRICE_UNAVAILABLE`. 테스트: `@WebMvcTest` 11건 + Testcontainers 통합 3건 + `PracticeHoldingObservationService` 단위 6건(PR #302 리뷰 차단 반영 — 오케스트레이션 분기별 검증) + chain 해석 회귀 단위 2건, 전부 통과. `ai/api-routes.md`·`docs/api-contracts.md` 갱신 완료. (PR #302, 이슈 #300)
- [x] **`POST /api/education/practice/holding-reflections`** — Controller·DTO·Service, `practice_progresses` 잠금 + A/B 선행 검증 + `practice_completions` 원자 저장(신규 `V28` migration, `practice_completions` 테이블·엔티티·repository 최초 생성). 새 오류 코드 추가 없음(기존 `NOT_FOUND`/`PRACTICE_EVIDENCE_MISSING`/`PRACTICE_ALREADY_COMPLETED` 재사용). 테스트: 서비스 단위·`PracticeProgress.complete` 단위·`@WebMvcTest`·통합·경합 5개 클래스 전부 통과. `ai/api-routes.md`·`docs/api-contracts.md` 갱신 완료. (이슈 #303)
- [x] **`GET /api/education/practice?market=STOCK|CRYPTO`** — 필수 시장별 holding 기반 `InvestmentPracticeResponse`/`PracticeStepResponse`/`PracticeEvidenceResponse` 구현. OCO 전용 URL·완료 key와 분리하고, 완료 전 회귀·완료 후 불변을 단위·`@WebMvcTest`·통합 테스트로 검증. (이슈 #305, PR #307)
- [x] **코인 지정가 전체 흐름 + Spring Context 재생성 통합 검증** — `CryptoLimitPracticeFlowRestartIntegrationTest`를 Fake `PriceStore` + 일반 `LimitOrderService` 직접 호출 방식에서 실제 세션 경로(`PracticePriceSessionService.createSession` → `PracticeLimitOrderService.createOrder` → `PracticePriceTickService.advanceTick`)로 재연결했다. 같은 종목의 다른 사용자 세션 주문·세션에 귀속되지 않은 일반 주문이 tick 이벤트로 체결되지 않음을 같은 시나리오에서 함께 검증한다. 코인 즐겨찾기→의도→지정가 매수 FILLED(세션 tick 1 체결)→관찰(세션 `currentPrice` 역추적, CLOSER_TO_BOUNDARY)→복기→완료 전체 흐름과 실제 Spring Context 재생성에 따른 완료 전 인메모리 evidence 유실·완료 후 DB 완료 불변을 검증한다. 주식 시장가 전체 흐름 통합 검증은 3차 MVP로 이관한다. 대상 테스트 3건 통과. (이슈 #313)
- [x] **`./gradlew build` 통과 확인.** — 재연결된 통합 테스트를 포함해 전체 재실행 완료, `spotlessJavaCheck` 포맷 위반(테스트 파일)만 `spotlessApply`로 수정 후 `BUILD SUCCESSFUL`. 검증 SHA `41be54845957aa0a49a30fce746449a36c3a1e5d`(실제 PriceStore 미변경 단정 추가 포함 최종). (이슈 #313)
