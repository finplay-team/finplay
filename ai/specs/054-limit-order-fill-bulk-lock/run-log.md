# Run Log: 054-limit-order-fill-bulk-lock

## AI 로그 (에이전트 참조용)
| 시각 | 에이전트 | 실행 명령 | 근거 |
|---|---|---|---|
| 23:48 | implementer | `./gradlew test --tests OrderRepositoryTest --tests AccountRepositoryTest --tests HoldingRepositoryTest` | plan.md "데이터 모델·인터페이스 설계" 1~3 |
| 23:48 | implementer | `./gradlew spotlessJavaCheck spotbugsMain spotbugsTest` | ai/agent-mistakes.md §공유 작업 폴더 규칙 5 |
| 23:52 | tester | `./gradlew test --tests "com.finplay.api.domain.order.repository.OrderRepositoryTest" --tests "com.finplay.api.domain.account.repository.AccountRepositoryTest" --tests "com.finplay.api.domain.portfolio.repository.HoldingRepositoryTest" --rerun-tasks` | tasks.md 1번째 항목의 self-report 독립 검증 (오름차순 정렬·존재하지 않는 ID 조용히 누락) |
| 23:56 | implementer | `grep -rn "applyBuyTrade" src/main` 후 `./gradlew compileJava compileTestJava`, `./gradlew test --tests PortfolioBuyServiceTest --tests OrderExecutionServiceTest --tests LimitOrderFillServiceTest --rerun-tasks`, `./gradlew spotlessJavaCheck spotbugsMain spotbugsTest` | plan.md "5. PortfolioBuyService", tasks.md 2번째 항목, ai/agent-mistakes.md §공유 작업 폴더 규칙 5 |
| 23:58 | tester | `./gradlew test --tests "com.finplay.api.domain.portfolio.service.PortfolioBuyServiceTest" --tests "com.finplay.api.domain.order.service.OrderExecutionServiceTest" --tests "com.finplay.api.domain.order.service.LimitOrderFillServiceTest" --rerun-tasks` | tasks.md 2번째 항목의 self-report 독립 검증 (새 오버로드가 재조회·재락 없이 넘겨받은 Holding만 쓰는지, 기존 7-인자 호출부 2곳의 동작·컴파일 영향 여부) |
| 00:06 (8/24) | implementer | `./gradlew compileJava spotlessJavaCheck` | plan.md "4. LimitOrderFillService.fillBatch 재구성", ai/adr/0002-architecture.md(도메인 간 참조는 service 레이어로만) |
| 00:10~00:18 (8/24) | tester | `./gradlew test --tests LimitOrderFillServiceTest --rerun-tasks`(수정 전 회귀 확인) → 테스트 작성 → `./gradlew test --tests LimitOrderFillServiceTest --tests LimitOrderFillBatchAtomicityIntegrationTest --tests LimitOrderFillAccountLockContentionIntegrationTest --tests LimitOrderFillIntegrationTest --tests AccountServiceTest --tests PortfolioBuyServiceTest --tests OrderExecutionServiceTest --rerun-tasks` → `./gradlew compileJava compileTestJava spotlessJavaCheck spotbugsMain spotbugsTest` | tasks.md 3번째 항목("단위 테스트로 호출 순서·맵 갱신 시점 검증" + 기존 원자성 테스트 회귀 확인), ai/adr/0003-testing-strategy.md |

## 모니터링 (사람용 요약)
- 23:48 — 벌크 락 Repository 3종(OrderRepository/AccountRepository/HoldingRepository) 추가, ID 오름차순·존재하지 않는 ID 조용히 누락 @DataJpaTest 6건 통과, 컴파일·spotless·spotbugs 통과.
- 23:52 — tester가 implementer의 self-report를 독립 검증. `--rerun-tasks`로 캐시를 우회해 실제 실행(첫 시도는 UP-TO-DATE로 스킵됨을 확인 후 재실행). OrderRepositoryTest 32건·AccountRepositoryTest 12건·HoldingRepositoryTest 12건 모두 통과(0 failures/errors). 신규 6건이 두 요구사항(입력 순서를 뒤섞어도 ID 오름차순 반환, 존재하지 않는 ID가 예외 없이 결과에서만 빠짐)을 실제로 검증하는지 fixture(설정 순서·ID 생성 순서)까지 대조 확인 — 보강 불필요, 코드 추가 없음.
- 23:56 — `PortfolioBuyService.applyBuyTrade`에 이미 잠근/신규 `Holding`을 받는 8-인자 오버로드 추가, 기존 7-인자 시그니처는 내부에서 이 오버로드로 위임(반환 타입 `void`→`Holding`). 착수 전 `grep -rn "applyBuyTrade" src/main`으로 호출부 2곳(`OrderExecutionService`·`LimitOrderFillService.fillBuy`) 모두 반환값을 문(statement)으로만 쓰는 것을 확인 — 반환 타입 변경이 컴파일을 깨지 않음. 새 오버로드가 `holdingRepository.save`만 호출하고 `findByAccountIdAndInstrumentIdForUpdate`를 호출하지 않음을 검증하는 단위 테스트 2건 추가(기존 행 재사용·신규 생성 각각), `PortfolioBuyServiceTest` 6건 모두 통과. `--rerun-tasks`로 재실행 확인. 컴파일·spotless·spotbugs 통과, 변경 파일은 의도한 2개뿐(`git status` 확인).
- 23:58 — tester가 implementer의 self-report를 독립 검증. `git diff HEAD -- PortfolioBuyService.java`로 코드 자체를 대조: 새 8-인자 오버로드 본문은 기존 7-인자 메서드 뒷부분(`holding.applyBuy`~`return`)을 그대로 옮긴 것이라 리팩터링 전후 동작이 텍스트로 동일함을 확인, 8-인자 오버로드 내부에 `holdingRepository`를 조회하는 호출이 없음(save만 호출)을 직접 확인 — self-report만 믿지 않고 구현 코드 원문으로 검증. `git status`로 `OrderExecutionService.java`·`LimitOrderFillService.java`가 이번 diff에 없음을 확인(호출부 자체는 손대지 않았음) 후 두 파일을 직접 읽어 133번째 줄·150번째 줄(현재 라인 번호 기준)이 여전히 기존 7-인자 시그니처를 문(statement)으로 호출하는 것을 확인 — 반환 타입 변경이 두 호출부 컴파일에 영향 없음을 실측(코드 대조 + 아래 빌드로 이중 확인). `PortfolioBuyServiceTest`·`OrderExecutionServiceTest`·`LimitOrderFillServiceTest`를 `--rerun-tasks`로 재실행 — `BUILD SUCCESSFUL`, 6/32/13건(총 51건) 모두 0 failures/0 errors. `PortfolioBuyServiceTest`의 신규 2건(`applyBuyTradeWithLockedHoldingSavesWithoutSeparateLookupForExistingHolding`·`applyBuyTradeWithLockedHoldingSavesNewlyCreatedHoldingWithoutSeparateLookup`)이 `verify(holdingRepository, never()).findByAccountIdAndInstrumentIdForUpdate(...)` + `verify(holdingRepository).save(lockedHolding/newHolding)`로 "재조회 없이 넘겨받은 holding만 저장"을 실제로 검증하는 것을 확인 — 보강 불필요, 코드 추가 없음.
- 00:06(8/24) — `LimitOrderFillService.fillBatch`를 order→account→holding 벌크 조회(오름차순) 순서로 재구성. 가변 `holdingsByAccountId` 맵을 청크 루프에서 즉시 갱신해 같은 청크·같은 신규 종목 2건 이상이 두 번째 INSERT를 시도하지 않게 했고, 벌크 조회가 빠뜨린 주문 ID는 루프에서 기존과 동일한 메시지의 `IllegalStateException`을 직접 던진다. `fillOnePending`/`fillBuy`/`fillSell`(단건 경로)은 한 글자도 건드리지 않고, 병렬 경로로 `fillOnePendingWithLockedResources`/`fillBuyWithLockedHolding`/`fillSellWithLockedHolding`을 신설했다(로직은 복제, 단건 경로에 대한 회귀 위험을 0으로 만드는 것을 우선). ADR-0002(도메인 간 repository 직접 주입 금지) 준수를 위해 `AccountRepository.findByIdInForUpdate`·`HoldingRepository.findByAccountIdInAndInstrumentIdForUpdate`를 각각 `AccountService.getAccountsByIdsForUpdate`·`PortfolioBuyService.findHoldingsForUpdate` 래퍼로 감싸 호출했다(plan.md 의사코드는 이 두 저장소를 `LimitOrderFillService`가 직접 주입하는 것처럼 적혀 있어 실제 구현과 다르다 — ADR-0002 위반을 피하기 위한 의도적 이탈). `compileJava`·`spotlessJavaCheck` 통과, `git status`로 의도한 3개 파일(`LimitOrderFillService`·`AccountService`·`PortfolioBuyService`)만 변경됐음을 확인. 단위 테스트는 이 항목 범위가 아니라(오케스트레이터 지시, tester 담당) 작성하지 않았다.
- 00:10~00:18(8/24) — tester가 implementer의 self-report를 독립 검증하고 tasks.md 3번째 항목의 단위 테스트를 작성했다. `LimitOrderFillService.fillBatch`·`fillOnePendingWithLockedResources`·`fillBuyWithLockedHolding`·`fillSellWithLockedHolding` 전체를 코드로 직접 읽어 확인: (1) 잠금 순서 order→account→holding 유지, 청크 트랜잭션 하나로 원자성 유지(`@Transactional(isolation = READ_COMMITTED)`, 변경 없음). (2) ID 오름차순 정렬(`orderIds.stream().sorted()`)은 `findByIdInForUpdate`(락 쿼리)에만 쓰이고, 처리 루프는 `for (Long orderId : orderIds)`로 원래 순서(requestedAt asc, id asc)를 그대로 순회함을 확인. (3) 존재하지 않는 orderId 예외 메시지("체결 대상 주문을 찾을 수 없습니다. orderId=...")가 기존 `fillOnePending`(146번째 줄)과 문자열 그대로 일치함을 직접 대조. (4) BUY에서 holding이 맵에 없으면 `Holding.create` 후 `portfolioBuyService.applyBuyTrade(...)` 반환값을 `holdingsByAccountId.put`으로 즉시 반영해 같은 청크의 다음 주문이 재사용하는 구조를 확인. (5) SELL에서 맵에 없을 때 예외 메시지를 `PortfolioSellService.getHoldingForUpdate`(51~56번째 줄) 원문과 대조해 정확히 일치함을 확인("체결 대상 holding을 찾을 수 없습니다. accountId=..., instrumentId=..."). 발견한 문제: 수정 전 `LimitOrderFillServiceTest`를 `--rerun-tasks`로 먼저 돌려본 결과 `fillBatchFillsEachOrderInGivenOrder`·`fillBatchSkipsOrderThatIsNoLongerPending` 2건이 실패했다 — 두 테스트가 구현이 더 이상 호출하지 않는 옛 단건 mock(`orderRepository.findByIdForUpdate`)을 스텁하고 있어 새 구현의 벌크 호출(`findByIdInForUpdate`)과 어긋난 것으로, `fillBatch` 자체의 결함이 아니라 테스트가 구현 변경을 따라가지 못한 것이었다. 두 테스트를 새 벌크 스텁으로 고치고, tasks.md가 요구한 "호출 순서·맵 갱신 시점" 검증을 위해 4건을 신규 추가했다: `fillBatchLocksBulkResourcesInOrderAccountHoldingSequence`(order→account→holding 벌크 호출 순서를 `InOrder`로 검증), `fillBatchLocksOrderIdsAscendingButProcessesGivenSequenceAndReusesHoldingCreatedInSameChunk`(락 쿼리에는 오름차순 정렬된 ID가, 처리 루프에는 원래 순서가 전달됨을 `ArgumentCaptor`로 검증하고, 같은 청크·같은 계좌·같은 신규 종목 2건이 먼저 처리된 주문이 만든 holding을 인스턴스 동일성으로 재사용함을 검증), `fillBatchThrowsSameMessageAsFindByIdForUpdateWhenBulkOrderLockOmitsRequestedId`·`fillBatchThrowsSameMessageAsGetHoldingForUpdateWhenSellHoldingMissingFromBulkMap`(예외 메시지 정확한 일치를 `hasMessage`로 검증). `AccountServiceTest`·`PortfolioBuyServiceTest`에는 새 래퍼 메서드(`getAccountsByIdsForUpdate`·`findHoldingsForUpdate`) 자체를 직접 호출하는 단위 테스트가 전혀 없었음을 확인해 각 1건씩 추가했다(단순 위임만 검증). `LimitOrderFillServiceTest` 17건(수정 2건 + 신규 4건 + 기존 11건)·`LimitOrderFillBatchAtomicityIntegrationTest` 2건·`LimitOrderFillAccountLockContentionIntegrationTest` 2건·`LimitOrderFillIntegrationTest`·`AccountServiceTest` 15건·`PortfolioBuyServiceTest` 7건·`OrderExecutionServiceTest` 32건을 모두 `--rerun-tasks`로 재실행해 0 failures/0 errors 확인(`build/test-results/test/*.xml`의 tests/failures/errors 속성으로 실측, Docker 기동 확인 후 Testcontainers 통합 테스트 포함). `compileJava compileTestJava spotlessJavaCheck spotbugsMain spotbugsTest` 통과.
