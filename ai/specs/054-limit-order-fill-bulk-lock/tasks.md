# Tasks: 지정가 체결 청크 내 벌크 락 최적화

- [x] **벌크 락 Repository 메서드 3종 추가** — `OrderRepository.findByIdInForUpdate`, `AccountRepository.findByIdInForUpdate`, `HoldingRepository.findByAccountIdInAndInstrumentIdForUpdate`(plan.md "데이터 모델·인터페이스 설계" 1~3). 셋 다 `PESSIMISTIC_WRITE` + ID 오름차순 `ORDER BY`. `@DataJpaTest`로 오름차순 반환·존재하지 않는 ID가 결과에서 조용히 빠지는 것을 검증한다.
- [x] **`PortfolioBuyService.applyBuyTrade` 오버로드 추가**(plan.md "5") — 호출부가 이미 잠갔거나 새로 만든 `Holding`을 받아 저장하는 형태. 기존 6-인자 시그니처는 내부에서 이 오버로드로 위임하도록 리팩터링하되 동작은 그대로 유지한다. 착수 전 `applyBuyTrade` 호출부를 전부 확인해(`grep -rn "applyBuyTrade" src/main`) 반환 타입 변경(`void`→`Holding`)이 기존 호출부를 깨지 않는지 점검한다. 단위 테스트로 새 오버로드가 `holdingRepository.save`만 호출하고 별도 조회를 하지 않음을 검증한다.
- [ ] **`LimitOrderFillService.fillBatch` 재구성**(plan.md "4", 위험 요소 2·3 대응) — order→account→holding 벌크 조회를 순서대로 호출하고, holding 맵을 가변으로 유지해 청크 안에서 신규 생성된 holding을 즉시 반영한다. 벌크 조회가 조용히 빠뜨린 주문 ID는 처리 루프에서 기존과 동일한 메시지의 `IllegalStateException`을 직접 던진다. `fillIfPending`(단건 경로)은 손대지 않는다. 단위 테스트(mock)로 호출 순서·맵 갱신 시점을 검증한다.
- [ ] **위험 요소 1·2 검증 통합 테스트 추가 + 기존 원자성 테스트 회귀 확인** — (a) 같은 계좌가 같은 청크·같은 신규 종목에 매수 주문 2건 이상 걸어둔 시나리오(유니크 제약 위반 없이 둘 다 체결), (b) 서로 다른 종목의 청크 두 개가 겹치는 계좌 집합을 동시에 체결하는 시나리오(데드락 없이 완료) — 둘 다 Testcontainers 통합 테스트로 작성한다. 기존 `LimitOrderFillBatchAtomicityIntegrationTest`를 수정 없이 실행해 그대로 통과하는지 확인한다(회귀).
- [ ] **개선 전후 처리 시간 실측 비교** — 계좌풀 소량·단일 종목·단일 가격 지정가 500건 재현 조건으로 이 spec 적용 전(현재 dev/개별 왕복 방식)과 적용 후(벌크 락) 처리 시간을 비교하고 `docs/loadtest/`에 결과 문서를 남긴다. `git show d3f5c027`(과거 배치 처리량 벤치마크, #501 사전 측정 후 범위 밖으로 제거됨)와 `docs/loadtest/limit-order-fill-lock-vs-write-phase-benchmark-result.md`(락 구간 55~59% 근거)를 재현 조건 참고용으로 쓴다.
- [ ] **ADR-0024·ADR-0025 "후속" 절 갱신** — 이 spec(054, 이슈 #501)이 두 ADR의 §결정 4(예외 처리·트랜잭션 경계) 위에 청크 내부 락 왕복만 최적화한 것임을 각 ADR의 "후속" 절에 기록한다. ADR 본문의 §결정·대안 절 자체는 고치지 않는다(CLAUDE.md 규칙 2 — 대체는 새 번호로만).
- [ ] **빌드 검증** — `./gradlew compileJava compileTestJava spotlessJavaCheck spotbugsMain spotbugsTest` + 이 spec이 건드린 테스트를 `--tests` 필터로 실행(`ai/agent-mistakes.md` §공유 작업 폴더 규칙 5의 절차). 머신이 한가하면 전체 `./gradlew build`도 시도한다.
